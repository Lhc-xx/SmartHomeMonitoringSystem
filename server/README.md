# Server 模块

放置 Ubuntu + C++11 服务器代码：

- `include/`：服务器内部头文件；
- `src/`：网络连接、业务服务、配置、日志、数据库、媒体等实现；
- `conf/`：配置文件和脱敏配置模板；
- `log/`：运行日志目录，仅保留 `.gitkeep`；
- `data/`：录像、临时文件等运行数据，仅保留 `.gitkeep`；
- `tests/`：服务器单元测试和集成测试；
- `Makefile`：不依赖第三方库的最小本地构建入口；
- `CMakeLists.txt`：统一构建入口。
