# Qt+FFmpeg的二次开发实战

# 一、环境搭建

## 1.Windows下

### `FFmpeg`开发版本sdk下载

在Windows下，要是使用`FFmpeg`做二次开发， 需要先下载编译好的`FFmpeg`版本，这里我们选择了4.4版本，下载地址为：

https://github.com/ShiftMediaProject/FFmpeg/releases/tag/4.4.r101753

![image-20250419214207530](.\assets\image-20250419214207530.png)

以上的sdk版本中，出现了**msvc16**，这代表的是Visual Studio的其中一个版本。以下是MSVC版本与Visual Studio版本的对应关系：

<img src=".\assets\image-20250419190213085.png" alt="image-20250419190213085" style="zoom:50%;" />



因为32位版本既可以在32位系统下使用，也可以在64位系统下使用，兼容性更强，所以**sdk开发我们建议选择32位版本**（前提条件是**IDE中有32位的编译器**）的。解压后的目录结构如下所示：

### 头文件

![image-20250419215536536](.\assets\image-20250419215536536.png)

### 库文件

![image-20250419215820705](.\assets\image-20250419215820705.png)



### Qt中引入FFmpeg库

实际上在Qt中引入FFmpeg库，就是增加一个第三库的过程，即配置include和lib到Qt项目中的.pro文件中。方法如下：

> 1.新建一个Qt的Widget工程后，使用Qt的图形化界面添加库。

<img src=".\assets\image-20250419221033620.png" alt="image-20250419221033620" style="zoom:50%;" />

> 2.在弹出的添加库界面中，选择外部库，点击“下一步”

<img src=".\assets\image-20250419221401683.png" alt="image-20250419221401683" style="zoom:50%;" />

> 3.在添加库的界面中，进行如下设置

<img src=".\assets\image-20250419221723190.png" alt="image-20250419221723190" style="zoom:50%;" />

> 4.最后可以看到预览界面中，有字符串会被添加到项目的.pro文件中

<img src=".\assets\image-20250419221912300.png" alt="image-20250419221912300" style="zoom:50%;" />

> 5.打开项目的.pro文件

![image-20250419222428782](.\assets\image-20250419222428782.png)

或者写成这个样子也可以

```C
win32: LIBS += -LC:\\libffmpeg_4.4.r101753_msvc16_x86\\lib\\x86

LIBS += -lavcodec       \
    	-lavdevice      \
        -lavutil        \
        -lavformat      \
        -lavfilter      \
        -lswscale       \
        -lswresample    \
        -lpostproc

INCLUDEPATH += C:\\libffmpeg_4.4.r101753_msvc16_x86\\include
DEPENDPATH += C:\\libffmpeg_4.4.r101753_msvc16_x86\\include
```









### 测试用例

```c++
#include "widget.h"

#include <QApplication>
#include <QDebug>

//ffmpeg是用C语言实现的，在C++中使用，必须要加上 extern "C"{}
extern "C" {
#include <libavutil/avutil.h>
#include <libavcodec/avcodec.h>
#include <libavformat/avformat.h>
#include <libavdevice/avdevice.h>
#include <libavfilter/avfilter.h>
#include <libswscale/swscale.h>
#include <libswresample/swresample.h>
#include <libpostproc/postprocess.h>
}


int main(int argc, char *argv[])
{
    QApplication a(argc, argv);
    Widget w;
    w.show();

    qDebug() << avcodec_version();//查看版本，整型版本号，供机器识别
    qDebug() << av_version_info();//查看版本，字符串版本号，供人查看

    return a.exec();
}
```

点击构建按钮，进行编译。如果设置是正确的，就能编译成功了。

### 动态库添加

当编译成功后，运行程序时，会发现程序无法成功，此时就差最后一步了，就是添加动态库。我们要找到编译生成的*.exe文件所在的目录，在该目录下，把上面的那8个动态库文件.dll拷贝过来即可。

![image-20250419223451566](.\assets\image-20250419223451566.png)

拷贝完成后，再次点击Qt Creator 中的项目运行按钮，能看到程序运行成功了。

![image-20250419223820069](.\assets\image-20250419223820069.png)



但是每次这样复制文件比较麻烦，可以直接将动态库路径添加到系统环境变量中，重启Qt

C:\libffmpeg_4.4.r101753_msvc16_x86\bin\x86







## 2.Ubuntu下

### apt安装

我们默认大家安装的是ubuntu22.04， 在其下安装的方式比较简单，执行以下命令即可

```shell
$ sudo apt install ffmpeg   # 如果该命令执行没有成功,则运行以下命令
$ sudo apt-get install libavcodec-dev libavformat-dev libavutil-dev libswscale-dev # 安装开发sdk
$ sudo apt update  # 更新完毕之后，再次执行上述安装命令
# 假设还是没有安装成功，则需要更新apt源之后，再执行以上命令
```

### 测试用例

编写以下测试文件`hello.c`

```c
#include <libavutil/avutil.h>
#include <libavcodec/avcodec.h>

int main(int argc, char *argv[])
{
	printf("%d\n", avcodec_version());
    return 0;
}
```

### 编译

```shell
$ gcc hello.c -lavcodec
```

运行可执行程序a.out，即可得到`FFmpeg`版本信息。

```shell
$ ./a.out
4001124
```

### 命令行查看版本

```shell
$ ffmpeg -version
```

![0d16f0d63046f252ef8943658b8cbbf4](.\assets\0d16f0d63046f252ef8943658b8cbbf4.jpeg)



# 二、夯实FFmpeg基础

## `libavutil`库

`libavutil`是一个通用的工具库，提供了许多基础的功能和数据结构，如**内存管理、数学运算、随机数生成、时间处理、日志系统**等。它是其他 FFmpeg 库的基础支撑库，为音视频处理提供了底层的辅助功能。

### 1. `FFmpeg`日志系统

`FFmpeg`中定义了日志的级别，这与我们之前学过的`log4cpp`等日志系统类似，**值越大优先级越低，值越小优先级越高**。

```c
#define AV_LOG_QUIET    -8
#define AV_LOG_PANIC     0
#define AV_LOG_FATAL     8
#define AV_LOG_ERROR    16
#define AV_LOG_WARNING  24
#define AV_LOG_INFO     32
#define AV_LOG_VERBOSE  40
#define AV_LOG_DEBUG    48
#define AV_LOG_TRACE    56
#define AV_LOG_MAX_OFFSET (AV_LOG_TRACE - AV_LOG_QUIET)
```

以下是常用的一些函数接口

```c
//获取当前日志级别
int av_log_get_level(void);
//设置当前日志级别
void av_log_set_level(int level);
//打印日志
void av_log(void *avcl, int level, const char *fmt, ...) 
//设置日志的标志位,控制日志信息的显示方式和详细程度
void av_log_set_flags(int arg);
//设置日志的定制化方案：指定回调函数
void av_log_set_callback(void (*callback)(void*, int, const char*, va_list));
```



#### av_log函数

`av_log` 是 `FFmpeg` 中用于输出日志信息的核心函数，在调试和错误处理方面非常重要。

> 参数avcl：这是一个指向 void 类型的指针，通常表示一个 FFmpeg 的上下文对象，当不需要特定的上下文信息时，直接指定为NULL。它的作用主要有以下几点：

- **标识日志来源**：通过传入具体的上下文对象，日志系统可以知道日志是从哪个组件或模块产生的。例如，在处理音视频文件时，可能会有输入上下文（AVFormatContext）和编解码器上下文（AVCodecContext），传入这些上下文可以让开发者清楚地知道日志信息是与文件读取相关，还是与编解码过程相关。
- **过滤日志**：可以根据上下文对象对日志进行过滤。如果在日志回调函数中实现了自定义的过滤逻辑，就可以根据上下文对象的不同，选择性地输出或忽略某些日志信息。
- **关联上下文信息**：当使用 `av_log_set_flags` 设置了一些标志（如显示文件名、函数名等）时，上下文对象可以帮助日志系统更准确地关联到具体的代码位置。

> 参数level：表示日志的级别，用于控制日志的详细程度。常见的日志级别有：

- AV_LOG_QUIET：不输出任何日志。
- AV_LOG_PANIC：非常严重的错误，程序可能无法继续运行。
- AV_LOG_FATAL：致命错误，程序通常会终止。
- AV_LOG_ERROR：一般错误，可能会影响程序的正常运行。
- AV_LOG_WARNING：警告信息，提示可能存在的问题，但不影响程序的基本运行。
- AV_LOG_INFO：一般信息，用于提供程序的运行状态等。
- AV_LOG_VERBOSE：详细信息，比 AV_LOG_INFO 更详细。
- AV_LOG_DEBUG：调试信息，包含大量的详细信息，通常在开发和调试阶段使用。
- AV_LOG_TRACE：跟踪信息，最详细的日志级别，包含了程序的内部执行细节。

> 参数fmt：这是一个格式化字符串，类似于 `printf` 函数中的格式化字符串，用于指定日志信息的格式。
>
> 参数`...`：可变参数列表，根据 `fmt` 中的格式化说明符，传入相应的参数。

#### 测试用例

```c++
void testLog0()
{
    printf("===============================\n");
    int loglevel = av_log_get_level();//32  默认为AV_LOG_INFO级别
    av_log_set_level(AV_LOG_DEBUG);
    loglevel = av_log_get_level();
    av_log(NULL, AV_LOG_PANIC, "Panic: mesage\n");
    av_log(NULL, AV_LOG_FATAL, "Fatal: mesage\n");
    av_log(NULL, AV_LOG_ERROR, "Error: mesage\n");
    av_log(NULL, AV_LOG_WARNING, "Warning: mesage\n");
    av_log(NULL, AV_LOG_INFO, "Info: mesage\n");
    av_log(NULL, AV_LOG_VERBOSE, "Verbose: mesage\n");
    av_log(NULL, AV_LOG_DEBUG, "Debug: mesage\n");
    av_log(NULL, AV_LOG_TRACE, "Trace: message\n");
    printf("===============================\n");
}
```

#### av_log_set_flags函数

`av_log_set_flags` 是 `FFmpeg` 中 `avutil` 库提供的用于配置日志输出行为的函数，主要作用是设置日志系统的**标志位**，控制日志信息的显示方式和详细程度。

> 参数 arg：有以下几种情况

- AV_LOG_PRINT_LEVEL：打印日志级别。
- AV_LOG_SKIP_REPEATED：跳过重复的日志信息（相同内容连续出现时仅显示一次）。

#### 测试用例

设置显式日志级别和跳过重复的日志信息

```c++
void testLog1()
{
    printf("===============================\n");
    // 设置日志级别为 DEBUG
    av_log_set_level(AV_LOG_DEBUG);
    av_log_set_flags(AV_LOG_PRINT_LEVEL | AV_LOG_SKIP_REPEATED);
    av_log(NULL, AV_LOG_DEBUG, "This is a debug message.\n");
    av_log(NULL, AV_LOG_PANIC, "Panic: mesage\n");
    av_log(NULL, AV_LOG_FATAL, "Fatal: mesage\n");
    av_log(NULL, AV_LOG_ERROR, "Error: mesage\n");
    av_log(NULL, AV_LOG_WARNING, "Warning: mesage\n");
    av_log(NULL, AV_LOG_INFO, "Info: mesage\n");
    av_log(NULL, AV_LOG_VERBOSE, "Verbose: mesage\n");
    av_log(NULL, AV_LOG_DEBUG, "Debug: mesage\n");
    av_log(NULL, AV_LOG_DEBUG, "Debug: mesage\n");
    av_log(NULL, AV_LOG_TRACE, "Trace: message\n");
    printf("===============================\n");
}
```

#### av_log_set_callback函数

av_log_set_callback 是 `FFmpeg` 中用于自定义日志输出行为的核心函数。通过它，开发者可以完全控制 FFmpeg 内部日志的输出方式，例如重定向到文件、过滤特定内容、添加额外信息（如时间戳、线程ID）或适配到自定义日志系统。

#### 测试用例 ：重定向到文件

```c++
static
void custom_log_callback(void *ptr, int level, const char *fmt, va_list vl)
{
    //fprintf(stderr, "[%s:%d] ", __FILE__, __LINE__);
    FILE * fp = fopen("simple_ffmpeg_log.txt", "a+");
    if(fp) {
        vfprintf(fp, fmt, vl);
        fflush(fp);
        fclose(fp);
    }
}

void testLog2()
{
    printf("===============================\n");
    // 设置日志级别为 DEBUG
    av_log_set_level(AV_LOG_DEBUG);
    av_log_set_flags(AV_LOG_PRINT_LEVEL | AV_LOG_SKIP_REPEATED);
    av_log_set_callback(custom_log_callback);
    av_log(NULL, AV_LOG_DEBUG, "This is a debug message.\n");
    av_log(NULL, AV_LOG_PANIC, "Panic: mesage\n");
    av_log(NULL, AV_LOG_FATAL, "Fatal: mesage\n");
    av_log(NULL, AV_LOG_ERROR, "Error: mesage\n");
    av_log(NULL, AV_LOG_WARNING, "Warning: mesage\n");
    av_log(NULL, AV_LOG_INFO, "Info: mesage\n");
    av_log(NULL, AV_LOG_VERBOSE, "Verbose: mesage\n");
    av_log(NULL, AV_LOG_DEBUG, "Debug: mesage\n");
    av_log(NULL, AV_LOG_DEBUG, "Debug: mesage\n");
    av_log(NULL, AV_LOG_TRACE, "[%s:%d] Trace: message\n", __FILE__, __LINE__);
    printf("===============================\n");
}
```



### 2. 数据结构AVDictionary

`AVDictionary` 是 `FFmpeg` 库中用于存储键值对的字典数据结构，它常被用于存储和传递元数据，例如视频文件的标题、作者、版权信息等。`AVDictionary` 的设计使得用户可以方便地对这些元数据进行添加、查找、删除等操作。

AVDictionary 本质上是一个**链表**，**每个节点包含一个键值对**，**并且这些节点按照键的字典序排列**。这种结构使得查找和插入操作的时间复杂度为 O(n)，其中 n 是字典中键值对的数量。

```c
struct AVDictionary {
    int count;
    AVDictionaryEntry *elems;
};

typedef struct AVDictionaryEntry {
    char *key;
    char *value;
} AVDictionaryEntry;

typedef struct AVDictionary AVDictionary;
```

以下是其常用的一些函数接口

```c
//往字典中添加键值对数据
int av_dict_set(AVDictionary **pm, const char *key, const char *value, int flags);
//通过key获取对应的value
AVDictionaryEntry *av_dict_get(const AVDictionary *m, const char *key,
                               const AVDictionaryEntry *prev, int flags);
//获取字典中元素的个数
int av_dict_count(const AVDictionary *m);
//释放字典
void av_dict_free(AVDictionary **m);
```

#### av_dict_set函数

`av_dict_set`向 `AVDictionary` 中添加或修改一个键值对。如果键已经存在，则更新其对应的值；如果键不存在，则创建一个新的键值对。

> 参数：
>
> - pm：指向 AVDictionary 指针的指针，用于存储更新后的字典。
> - key：要添加或修改的键。
> - value：要设置的值。**如果为 NULL，则会删除该键对应的键值对**。
> - flags：控制操作的标志位，通常使用 0。

#### av_dict_get函数

av_dict_get 可以从 `AVDictionary` 中查找指定键的值。

> 参数：
>
> - m：要查找的 AVDictionary。
> - key：要查找的键。
> - prev：用于迭代查找的上一个 AVDictionaryEntry 指针。**如果为 NULL，则从头开始查找**。
> - flags：控制查找的标志位，例如是否忽略大小写等。



#### 测试用例

```c++
void testDictionary()
{
    AVDictionary * dict = nullptr;
    AVDictionaryEntry * elem = nullptr;

    //添加字段信息
    av_dict_set(&dict, "name", "zhangsan", 0);
    av_dict_set(&dict, "age", "22", 0);
    av_dict_set(&dict, "gender", "male", 0);
    av_dict_set(&dict, "phone", "5201314", 0);
    
    char * k = av_strdup("location");//开辟了堆空间
    char * v = av_strdup("wuhan");//开辟了堆空间
    av_dict_set(&dict, k, v, AV_DICT_DONT_STRDUP_KEY | AV_DICT_DONT_STRDUP_VAL);

    printf("================================\n");
    //获取字典中的元素个数
    int cnt = av_dict_count(dict);
    printf("dict counts:%d\n", cnt);
    //printf("遍历字典,扫描每一个字段\n");//中文乱码
    qDebug() << "遍历字典,扫描每一个字段";//这个OK
    while((elem = av_dict_get(dict, "", elem, AV_DICT_IGNORE_SUFFIX)) != nullptr) {
        printf("key:%10s --> value:%s\n", elem->key, elem->value);
    }

    printf("================================\n");
    elem = av_dict_get(dict, "name", NULL, 0);//查找关键字为"name"的元素
    if(elem)
        printf("elem: %s --> %s\n", elem->key, elem->value);

    printf("================================\n");
    av_dict_set(&dict, "phone", NULL, 0);//删除元素
    cnt = av_dict_count(dict);
    printf("dict counts:%d\n", cnt);
    //elem = nullptr;
    while((elem = av_dict_get(dict, "", elem, AV_DICT_IGNORE_SUFFIX)) != nullptr) {
        printf("key:%10s --> value:%s\n", elem->key, elem->value);
    }

    av_dict_free(&dict);
    printf("================================\n");
}
```





### 3. AVParseUtil中的常用接口

```c
//解析视频分辨率
int av_parse_video_size(int *width_ptr, int *height_ptr, const char *str);
//解析视频帧率
int av_parse_video_rate(AVRational *rate, const char *str);
//解析时间为微秒
int av_parse_time(int64_t *timeval, const char *timestr, int duration);
//解析颜色
int av_parse_color(uint8_t *rgba_color, const char *color_string, int slen,
                   void *log_ctx);
//解析比率
int av_parse_ratio(AVRational *q, const char *str, int max,
                   int log_offset, void *log_ctx);
```



#### av_parse_time函数

`av_parse_time` 是 FFmpeg 库中 `avutil` 模块里的一个实用函数，其主要功能是把表示时间的字符串解析为对应的时间戳（以微秒为单位）。它支持多种时间表示格式，能处理绝对时间和相对时间的解析，这在处理音视频流的时间相关操作时极为有用，例如设置播放起始时间、指定剪辑时间段等。

> 参数：
>
> - `timeval`：这是一个指向 `int64_t` 类型的指针，用于存储解析后的时间戳（单位为微秒）。
> - `timestr`：这是需要被解析的时间字符串，它可以是绝对时间（如 `1:23:45` 表示 1 小时 23 分 45 秒），也可以是相对时间（如 `+0:0:10` 表示相对当前时间往后 10 秒）。
> - `duration`：这是一个布尔值标志，用于指定 `timestr` 是表示绝对时间还是相对时间。若 `duration` 为非零值，则表示 `timestr` 是**相对时间**；若为 0，则表示 `timestr` 是**绝对时间**。
>
> 返回值：
>
> - 解析成功，返回值为0；解析失败，返回-1并设置相应的错误码

#### av_parse_color函数

av_parse_color 是 FFmpeg 库中 avutil 模块里的一个函数，其主要作用是将各种表示颜色的字符串解析为对应的 RGBA（红、绿、蓝、透明度）颜色值。它支持多种常见的颜色表示格式，例如十六进制表示（如 #RRGGBB 或 #RRGGBBAA）、颜色名称（如 red、green 等）以及一些特殊的表示方式。这在处理音视频中的颜色相关操作，如视频滤镜中设置颜色参数、字幕渲染设置颜色等场景中非常有用。

> 参数：
>
> - `rgba_color`：一个指向长度为 4 的 `uint8_t` 数组的指针，用于存储解析后的 RGBA 颜色值。数组的四个元素依次表示红（R）、绿（G）、蓝（B）和透明度（A），取值范围为 0 - 255。
> - `color_string`：需要被解析的颜色字符串，它可以是十六进制颜色码、颜色名称等。
> - `slen`：颜色字符串的长度，**如果为 -1，则函数会自动计算字符串的长度**。
> - `log_ctx`：日志上下文指针，通常可以传入 `NULL`，用于在解析过程中输出日志信息。

#### av_parse_ratio函数

av_parse_ratio 是 FFmpeg 库中 avutil 模块里的一个函数，其主要作用是将表示比率的字符串解析为对应的 AVRational 结构体。AVRational 是 FFmpeg 中用于精确表示有理数（即分数）的数据结构，在处理音视频的帧率、采样率、像素宽高比等涉及比率的场景中非常有用。

> 参数：
>
> - **`q`**：指向 `AVRational` 结构体的指针，用于存储解析后的比率值。`AVRational` 结构体包含两个成员：`num` 表示分子，`den` 表示分母。
> - **`str`**：需要被解析的比率字符串。该字符串可以是常见的比率表示形式，例如 `"16:9"` 表示 16 比 9 的比率，也可以是小数形式，如 `"1.77778"`（近似表示 16:9）。
> - **`max`**：**分母的最大值**。在解析过程中，如果计算出的分母超过这个最大值，函数会对结果进行近似处理，以确保分母不超过该值。
> - **`log_offset`**：日志偏移量，通常用于调整日志输出的位置，一般传入 0 即可。
> - **`log_ctx`**：日志上下文指针，用于在解析过程中输出日志信息。如果不需要日志输出，可以传入 `NULL`。

#### 测试用例

```c++
void testParseUtil()
{
    char inputStr[100] = {0};
    printf("========== Parse Video Size ==========\n");
    int width = 0, height = 0;
    //strcpy(inputStr, "1920x1080");
    //strcpy(inputStr, "hd1080");//同上
    //strcpy(inputStr, "vga");//640x480
    //strcpy(inputStr, "hd720");//1280x720
    //strcpy(inputStr, "pal");//720x576
    strcpy(inputStr, "ntsc");//720x480
    av_parse_video_size(&width, &height, inputStr);
    qDebug() << "width:" << width << ", height:" << height;

    printf("========== Parse Frame Rate ==========\n");
    AVRational outputRatio = {0,0};
    //strcpy(inputStr, "15/1");
    strcpy(inputStr, "ntsc");// 30000/1001
    //strcpy(inputStr, "pal");// 25/1
    av_parse_video_rate(&outputRatio, inputStr);
    qDebug() << "frame rate:" << outputRatio.num << "/" << outputRatio.den;

    printf("========== Parse Time ==========\n");
    int64_t outputTime;
    strcpy(inputStr, "00:01:01");
    av_parse_time(&outputTime, inputStr, 1);//单位：微秒
    qDebug() << "microseconds:" << outputTime;

    printf("========== Parse Color ==========\n");
    uint8_t rgbaColor[4] = {0};
    //strcpy(inputStr, "red");
    strcpy(inputStr, "#12345678");
    av_parse_color(rgbaColor, inputStr, -1, NULL);
    printf("(r,g,b,a) is (%d,%d,%d,%d)\n",
           rgbaColor[0], rgbaColor[1], rgbaColor[2], rgbaColor[3]);

    printf("========== Parse ratio ==========\n");
    strcpy(inputStr, "16:9");
    av_parse_ratio(&outputRatio, inputStr, 10000, 0, NULL);
    qDebug() << outputRatio.num << ":" << outputRatio.den;
    printf("=================================\n");
}
```



## FFmpeg常用数据结构

`FFmpeg`中的数据结构有很多，最关键的分为以下几类了。

![image-20250421180026133](.\assets\image-20250421180026133.png)



> 协议层(file/rtmp/rtsp/http/hls)

协议层有三大数据结构，`AVIOContext`，`URLContext`，`URLProtocol` 主要存储视音频使用的协议的类型以及状态。`URLProtocol` 存储输入视音频使用的封装格式。每种协议都对应一个 URLProtocol 结构。**注意**：在`FFmpeg` 中，文件也被当做一种协议“file”。



> 封装层(flv/avi/mp4/mkv)

`AVFormatContext` 主要存储视音频封装格式中包含的信息；`AVInputFormat` 存储输入视音频使用的封装格式。每种视音频封装格式都对应一个 AVInputFormat 结构。



> 编解码层(h264/h265/aac/mp3)

- 每个 **AVStream** 存储一个**视频/音频流**的相关数据；

- 每个**AVStream**对应一个**AVCodecContext**，存储该视频/音频流使用解码方式的相关数据；

- 每个 **AVCodecContext** 中对应一个 **AVCodec**，包含该视频/音频对应的解码器；

- 每种解码器都对应一个 **AVCodec** 结构。



> 数据包

视频的话，每个结构一般是存一帧；音频可能有好几帧

- 解码前数据：`AVPacket`

- 解码后数据：`AVFrame`



### `AVFormatContext` 结构体

`AVFormatContext` 是一个**高层容器**，存储的是**“解封装后的元数据”**。当你打开一个媒体文件后，它负责记录“这个文件里有什么”。

| 字段             | 含义               | 用途                                                    |
| :--------------- | :----------------- | :------------------------------------------------------ |
| **`nb_streams`** | 流的总数量         | 告诉你文件里有几个轨道（视频、音频、字幕）              |
| **`streams`**    | `AVStream*` 数组   | 每个元素描述一个流（编码类型、分辨率、帧率、时长等）    |
| **`duration`**   | 文件总时长（微秒） | 用于进度条显示和播放控制                                |
| **`bit_rate`**   | 总码率（bps）      | 用于网络带宽估算                                        |
| **`iformat`**    | `AVInputFormat*`   | 指向解封装器（如 MP4 解封装器、FLV 解封装器）           |
| **`metadata`**   | `AVDictionary*`    | 文件的元信息（标题、作者、专辑封面等）                  |
| **`pb`**         | `AVIOContext*`     | **指向数据源！** 这是连接“仓库管理员”和“搬运工”的桥梁。 |





### `AVIOContext` 结构体

`AVIOContext` 是一个**低层 IO 管理器**，它存储的是**“如何从数据源读取/写入数据”**的信息。它不关心数据是 H.264 还是 AAC，只关心“数据能不能被读到”。

| 字段               | 含义                                                 | 用途                                     |
| :----------------- | :--------------------------------------------------- | :--------------------------------------- |
| **`buffer`**       | `unsigned char*`                                     | 内部缓冲区（存放从数据源读到的字节）     |
| **`buf_size`**     | `int`                                                | 缓冲区大小（通常 1024 ~ 4096 字节）      |
| **`read_packet`**  | 函数指针 `int (*read_packet)(void*, uint8_t*, int)`  | 读数据的回调函数（由你实现）             |
| **`write_packet`** | 函数指针 `int (*write_packet)(void*, uint8_t*, int)` | 写数据的回调函数（由你实现）             |
| **`seek`**         | 函数指针 `int64_t (*seek)(void*, int64_t, int)`      | 跳转文件位置的回调函数（由你实现）       |
| **`opaque`**       | `void*`                                              | 用户自定义数据（你传给回调函数的上下文） |









## FFmpeg4.x 解码流程图

以下为解码一个音视频文件的流程图：

<img src=".\assets\image-20250422181527844.png" alt="image-20250422181527844" style="zoom:50%;" />

**`FFmpeg`解码流程**

1. 打开输入文件：使用 avformat_open_input 函数打开输入的音视频文件，该函数会读取文件头信息，获取文件的格式和流信息。

2. 查找流信息：调用 avformat_find_stream_info 函数分析文件中的流信息，确定每个流的编解码器、帧率、采样率等参数。

3. 查找解码器：根据流的编解码 ID，使用 avcodec_find_decoder 函数查找对应的解码器。

4. 打开解码器：使用 avcodec_open2 函数打开解码器，为后续的解码操作做好准备。

5. 读取数据包：通过 av_read_frame 函数从输入文件中读取压缩的数据包（AVPacket）。

6. 解码数据包：将读取的 AVPacket 发送给解码器，使用 avcodec_send_packet 函数，然后使用 avcodec_receive_frame 函数从解

   码器中获取解码后的帧（AVFrame）。

7. 处理解码后的帧：对解码后的 AVFrame 进行处理，例如显示视频帧、播放音频帧等。

8. 关闭资源：解码完成后，关闭解码器、输入文件，释放相关的内存资源。



# 三、协议层（重点）

## **avformat_network_init函数**

`avformat_network_init` 是 FFmpeg 中用于初始化网络模块和底层依赖的核心函数，确保程序能够正常使用网络协议（如 HTTP、RTMP、RTSP 等）进行音视频数据的传输。其作用及具体工作如下：

- 平台适配的网络库初始化
  封装不同操作系统的底层网络 API 初始化逻辑。例如：

​	-  Windows：调用 WSAStartup() 初始化 Winsock，确保 Socket 相关功能可用。

​	-  Unix-like 系统（Linux/macOS）：通常无需显式初始化，但仍可能配置线程安全或兼容性参数。

- 安全传输层支持（如 OpenSSL）
  若 FFmpeg 编译时启用了 TLS/SSL 支持（如 HTTPS、RTMPS），该函数会初始化加密库（如 OpenSSL 或 GnuTLS），确保加密协议正常使用。

- 全局网络配置
  设置默认网络参数，如超时时间、缓冲区大小等（部分参数可通过 AVOptions 覆盖）。

- 协议处理器准备
  隐式注册网络相关的协议（如 tcp、udp、http），但主要协议（如 RTMP、RTSP）通常通过 `avformat_open_input`函数动态加载。

### 函数原型和实现

```c
int avformat_network_init(void)
{
#if CONFIG_NETWORK
    int ret;
    if ((ret = ff_network_init()) < 0)
        return ret;
    if ((ret = ff_tls_init()) < 0)
        return ret;
#endif
    return 0;
}
```

>  返回值：成功返回 0，失败返回负数错误码（如内存不足或初始化失败）。



## avio_alloc_context函数

`avio_alloc_context` 是 FFmpeg 中用于 创建自定义 I/O 上下文（AVIOContext） 的核心函数。它允许开发者绕过 FFmpeg 默认的文件或网络 I/O 操作，通过自定义的回调函数直接处理数据的读写，从而支持从内存、自定义协议、加密流等非标准数据源读取或写入媒体数据。

### 函数原型

```c
AVIOContext *avio_alloc_context(
    unsigned char *buffer,   // 内部缓冲区指针
    int buffer_size,         // 内部缓冲区大小
    int write_flag,          // 读写模式标志（0=读，1=写，-1=读写）
    void *opaque,            // 用户自定义指针（传递给回调函数）
    int (*read_packet)(void *opaque, uint8_t *buf, int buf_size),
    int (*write_packet)(void *opaque, uint8_t *buf, int buf_size),
    int64_t (*seek)(void *opaque, int64_t offset, int whence)
);
```

> 参数：
>
> - buffer: FFmpeg 内部使用的缓冲区指针。如果为 `NULL`，FFmpeg 会自动分配和管理缓冲区；如果非 `NULL`，需由用户预先分配内存。
> - buffer_size: 缓冲区的大小。若 `buffer` 为 `NULL`，此值决定自动分配缓冲区的大小；若 `buffer` 非 `NULL`，需确保其容量不小于此值。
> - write_flag: 模式标志
>
>   - 0：上下文用于读取（此时 read_packet 必须有效）。
>   - 1：上下文用于写入（此时 write_packet 必须有效）。
> - opaque: 用户自定义的指针，会传递给 `read_packet`、`write_packet` 和 `seek` 回调函数。常用于传递上下文信息（如文件句柄、内存指针等）。
> - read_packet: 读取回调函数：当 FFmpeg 需要从数据源读取数据时调用。
>
>   - opaque：用户自定义指针。
>   - buf：目标缓冲区，用于填充读取的数据。
>   - buf_size：期望读取的字节数。
>   返回值：实际读取的字节数（若返回 0 表示 EOF，负数表示错误）。
> - write_packet: 写入回调函数：当 FFmpeg 需要向目标写入数据时调用。
>
>   - opaque：用户自定义指针。
>   - buf：待写入的数据缓冲区。
>   - buf_size：待写入的字节数。
>   返回值：实际写入的字节数（负数表示错误）。
> - seek: 定位回调函数：用于调整读写位置（可选，对不可定位的数据源可设为 NULL）。
>   - opaque：用户自定义指针。
>   - offset：目标位置偏移量。
>   - whence：定位模式（SEEK_SET、SEEK_CUR、SEEK_END）。
>   返回值：新的绝对位置（负数表示错误）。

> **返回值**：成功返回指向 `AVIOContext` 的指针，失败返回 `NULL`（通常因内存不足）。



## avformat_alloc_context函数

avformat_alloc_context 是 FFmpeg 中用于 分配并初始化一个 AVFormatContext 结构体 的核心函数。AVFormatContext 是 FFmpeg 中管理媒体文件格式（容器格式）的顶层上下文，它包含了媒体文件的全局信息（如格式类型、流列表、元数据、I/O 上下文等），是解复用（demuxing）和复用（muxing）操作的核心数据结构。

### 函数原型

```c
#include <libavformat/avformat.h>
AVFormatContext *avformat_alloc_context(void);
```

> 返回值：成功返回指向 AVFormatContext 的指针，失败返回 NULL（通常因内存不足）。



## **avformat_open_input函数**

avformat_open_input() 是 FFmpeg 中用于打开媒体文件并初始化媒体格式解析的核心函数。它是解码流程中第一步必须调用的关键接口，负责解析文件头部信息、探测文件格式，并填充媒体文件的全局信息。

### 函数原型

```c
int avformat_open_input(
    AVFormatContext **ps,      // 格式上下文指针的指针
    const char *url,           // 输入文件路径或 URL
    AVInputFormat *fmt,        // 指定的输入格式（通常为 NULL）
    AVDictionary **options     // 额外参数选项
);
```

> 参数：
>
> - ps: 接收函数创建的 `AVFormatContext` 结构体，存储媒体文件的全局信息。如果 `*ps` 为 NULL：函数内部会自动分配内存并初始化 AVFormatContext；如果 `*ps` 非 NULL：复用已有的 AVFormatContext（需先手动初始化）。
>
> - url: 输入媒体资源的路径或 URL。
>
>   - 本地文件路径: 如“ca_wlh.mp4”, “D:\\videos\ca_wlh.mp4”
>   - 网络协议流：如“rtsp://127.0.0.1/rtsp1”，“http://127.0.0.1:8080/http1”, “rtmp://...”
>
> - fmt: 指定输入文件的容器格式，若为 NULL，则由 FFmpeg 自动探测。
>
> -   options: 指向 AVDictionary 指针的指针，用于传递一些额外的选项。可以在字典中设置一些特定的参数，如缓冲大小、超时时间等。如果不需要传递额外的选项，可以将该参数设置为 NULL。
>
> 返回值：
>
> - 等于0，成功
> - 小于0，失败



## **avformat_find_stream_info函数**

`avformat_find_stream_info` 是 FFmpeg 库中用于解析输入文件流信息的重要函数。在调用 avformat_open_input 打开输入文件后，虽然已经获取了文件的基本格式信息，但流的详细信息（如编解码器参数、帧率、采样率等）可能并未完全确定。

avformat_find_stream_info 函数的主要作用就是通过读取文件中的数据包，分析并填充 AVFormatContext 中每个 AVStream 的详细信息，从而为后续的解码操作提供必要的参数。

### 函数原型

```c
int avformat_find_stream_info(AVFormatContext *ic, AVDictionary **options);
```

> 参数：
>
> - ic: 指向 AVFormatContext 结构体的指针，该结构体表示输入文件的格式上下文。在调用 avformat_open_input 时已经对其进行了初始化，avformat_find_stream_info 函数会基于此上下文进一步分析文件中的流信息，并将结果填充到 AVFormatContext 中的各个 AVStream 结构体中。
> - options:  指向 AVDictionary 指针的指针，用于传递一些额外的选项。可以在字典中设置一些特定的参数，以影响流信息的查找过程。例如，可以设置分析时长、探测的最大数据包数量等。**如果不需要传递额外的选项，可以将该参数设置为 NULL**。

> 返回值： 
>
> - 成功： 返回一个非负整数，表示成功分析了文件中的流信息。通常返回值为 0 表示正常完成。
> - 失败：返回一个负的错误码，表示在查找流信息的过程中出现了错误。常见的错误码及可能的原因如下：
>   - AVERROR(EIO)：输入输出错误，可能是文件读取失败、网络问题等导致无法获取足够的数据进行分析。
>   - AVERROR(ENOMEM)：内存分配失败，可能是系统内存不足，无法为分析过程分配所需的内存。
>   - AVERROR_INVALIDDATA：文件数据无效，可能是文件损坏、格式不支持或数据不完整，导致无法正确解析流信息。



## **av_dump_format函数**

av_dump_format 是 FFmpeg 库中用于打印音视频文件格式详细信息的实用函数。在音视频处理流程里，当使用 avformat_open_input 打开文件并通过 avformat_find_stream_info 分析出流信息之后，借助 av_dump_format 函数，你能够直观地看到文件的整体结构、各个流的详细参数（如编解码器、帧率、采样率等），这对于调试和了解文件内容非常有帮助。

### 函数原型

```c
void av_dump_format(AVFormatContext *ic,
                    int index,
                    const char *url,
                    int is_output);
```

> 参数：
>
> - ic: 指向 AVFormatContext 结构体的指针，该结构体代表了输入或输出文件的格式上下文。
> - index: 指定要重点显示的流的索引。如果传入的 index 为 -1，函数会打印文件中所有流的信息；若传入一个有效的流索引（从 0 开始），则会突出显示该索引对应的流的详细信息。例如，在一个既有视频流又有音频流的文件中，若 index 设为 0 且 0 对应的是视频流，那么视频流的信息会更加突出显示。
> - url: 输入或输出文件的路径或 URL。这个参数主要用于在打印信息时显示文件的名称，方便用户识别是哪个文件的格式信息被打印出来。它可以是本地文件的路径，也可以是网络视频的 URL。
> - is_output: 这是一个布尔类型的参数，用于指示当前是输入文件还是输出文件。**如果 is_output 为 0，表示当前处理的是输入文件，打印的是输入文件的格式信息**；**如果 is_output 为非 0 值，表示当前处理的是输出文件**，打印的是输出文件的相关格式信息。



## avio实战之打开文件流

### 1. 打开文件流（项目强相关）

```c++
#include <stdio.h>
extern "C"
{
#include <libavformat/avformat.h>
#include <libavformat/avio.h>
#include <libavcodec/avcodec.h>
}

#include <string>
using std::string;

int testAVIO()
{
    //printf("network init.\n");
    //初始化网络
    //avformat_network_init();
    //初始化大管家婆AVFormatContext对象
    AVFormatContext * pFmtCtx = avformat_alloc_context();
    if(pFmtCtx == nullptr) {
        return EXIT_FAILURE;
    }
    printf("========== testAVIO() ==========\n");
    AVInputFormat * pInputFmt = nullptr;
    //string url("rtsp://admin:admin@192.168.1.111/live/chn=1");
    string url("D:\\videos\\output.flv");
    if(avformat_open_input(&pFmtCtx, url.data(), pInputFmt, NULL) < 0) {
        printf("avformat open failed.\n");
        goto quit;
    } else {
        printf("open stream success!\n");
    }

    if(avformat_find_stream_info(pFmtCtx, nullptr) < 0) {
        printf("av_find_stream_info error.\n");
        goto quit;
    } else {
        printf("av_find_stream_info success.\n");
        printf("nb_streams:%d\n", pFmtCtx->nb_streams);
    }

quit:
    avformat_close_input(&pFmtCtx);
    avformat_free_context(pFmtCtx);
    //avformat_network_deinit();

    return 0;
}
```

输出结果

![image-20250423110947074](.\assets\image-20250423110947074.png)



### 2. 自定义avio数据来源之文件

```c++
int read_packet(void * ptr, uint8_t * buf, int buf_size)
{
    static int total = 0;
    FILE * fp = (FILE*)ptr;
    int sz = fread(buf, 1, buf_size, fp);
    total += sz;
    printf("Read %d bytes, total:%d\n", sz, total);
    return sz;
}

int64_t seek_func(void * opaque, int64_t offset, int whence)
{
    int64_t ret = 0;
    FILE * fp = (FILE*)opaque;
    if(whence == AVSEEK_SIZE) {
        return EXIT_FAILURE;
    }
    fseek(fp, offset, whence);
    ret = ftell(fp);
    printf("ftell pos: %lld\n", ret);
    return ret;
}

int testAVIO2()
{
    printf("==============================\n");
    //FILE * fp = fopen("D:\\ac_wlh480.mp4", "rb");//可以打开文件，但读取时出错
    //FILE * fp = fopen("output.flv", "rb");//打开文件失败
    FILE * fp = fopen("D:\\videos\\output.flv", "rb");
    if(nullptr == fp) {
        printf("open file error\n");
        return EXIT_FAILURE;
    }

    int size = 1024;
    unsigned char * pBuff = (unsigned char *)malloc(size);

    AVIOContext * pIOCtx = avio_alloc_context(
                pBuff,
                size,
                0,
                fp,
                read_packet,
                0,
                seek_func);
    printf("avio_alloc_context end\n");

    AVFormatContext * pFmtCtx = avformat_alloc_context();
    if(pFmtCtx == nullptr) {
        printf("avformat_alloc_context error\n");
        return EXIT_FAILURE;
    }
    printf("avformat_alloc_context end\n");
    pFmtCtx->pb = pIOCtx;//关联绑定在一起
    pFmtCtx->flags = AVFMT_FLAG_CUSTOM_IO;

    AVInputFormat * pInputFmt = nullptr;
    if(avformat_open_input(&pFmtCtx, "", pInputFmt, nullptr) < 0) {
        printf("avformat_open_input error.\n");
        goto quit;
    } else {
        printf("open stream success.\n");
    }

    if(avformat_find_stream_info(pFmtCtx, nullptr) < 0) {
        printf("avformat_find_stream_info error\n");
        goto quit;
    } else {
        printf("avformat_find_stream_info success.\n");
        printf("nb_streams: %d\n", pFmtCtx->nb_streams);
    }

quit:
    avformat_close_input(&pFmtCtx);
    avformat_free_context(pFmtCtx);
    free(pBuff);

    return 0;
}
```

程序输出结果

![image-20250423111407972](.\assets\image-20250423111407972.png)



### 3. 自定义avio数据来源之内存映射

```c++
#include <stdio.h>
extern "C"
{
#include <libavcodec/avcodec.h>
#include <libavformat/avformat.h>
#include <libavformat/avio.h>
#include <libavutil/file.h>
}

struct buffer_data {
    uint8_t *ptr;
    size_t size; ///< size left in the buffer
};

static int read_packet(void *opaque, uint8_t *buf, int buf_size)
{
    struct buffer_data *bd = (struct buffer_data *)opaque;
    buf_size = FFMIN(buf_size, bd->size);

    if (!buf_size)
        return AVERROR_EOF;
    printf("ptr:%p size:%zu\n", bd->ptr, bd->size);

    /* copy internal buffer data to buf */
    memcpy(buf, bd->ptr, buf_size);
    bd->ptr  += buf_size;
    bd->size -= buf_size;

    return buf_size;
}

int main(int argc, char *argv[])
{
    AVFormatContext *fmt_ctx = NULL;
    AVIOContext *avio_ctx = NULL;
    uint8_t *buffer = NULL, *avio_ctx_buffer = NULL;
    size_t buffer_size, avio_ctx_buffer_size = 4096;
    const char *input_filename = NULL;
    int ret = 0;
    struct buffer_data bd;

    input_filename = "D:\\videos\\test.flv";

    /* slurp file content into buffer */
    ret = av_file_map(input_filename, &buffer, &buffer_size, 0, NULL);
    if (ret < 0)
        goto end;

    /* fill opaque structure used by the AVIOContext read callback */
    bd.ptr  = buffer;
    bd.size = buffer_size;

    if (!(fmt_ctx = avformat_alloc_context())) {
        ret = AVERROR(ENOMEM);
        goto end;
    }

    avio_ctx_buffer = (uint8_t *)av_malloc(avio_ctx_buffer_size);
    if (!avio_ctx_buffer) {
        ret = AVERROR(ENOMEM);
        goto end;
    }
    avio_ctx = avio_alloc_context(avio_ctx_buffer, avio_ctx_buffer_size,
                                  0, &bd, &read_packet, NULL, NULL);
    if (!avio_ctx) {
        ret = AVERROR(ENOMEM);
        goto end;
    }
    fmt_ctx->pb = avio_ctx;

    ret = avformat_open_input(&fmt_ctx, NULL, NULL, NULL);
    if (ret < 0) {
        fprintf(stderr, "Could not open input\n");
        goto end;
    }

    ret = avformat_find_stream_info(fmt_ctx, NULL);
    if (ret < 0) {
        fprintf(stderr, "Could not find stream information\n");
        goto end;
    } else {
        printf("fmt_ctx->nb_streams: %d\n", fmt_ctx->nb_streams);
    }

    av_dump_format(fmt_ctx, 0, input_filename, 0);

end:
    avformat_close_input(&fmt_ctx);

    /* note: the internal buffer could have changed, and be != avio_ctx_buffer */
    if (avio_ctx)
        av_freep(&avio_ctx->buffer);
    avio_context_free(&avio_ctx);

    av_file_unmap(buffer, buffer_size);

    if (ret < 0) {
        //fprintf(stderr, "Error occurred: %s\n", av_err2str(ret));
        return 1;
    }

    return 0;
}

```

输出结果：

![image-20250423172718998](.\assets\image-20250423172718998.png)



# 四、封装层

## 音视频封装格式

音视频封装格式（Container Format），又称**容器格式**，是一种将音频流、视频流、字幕、元数据（如时长、作者、编码信息等）按照特定规则组织成一个文件的技术规范。
它不直接处理音视频数据的编码压缩，而是定义了如何将多个独立的数据流（如 H.264 视频、AAC 音频）和相关信息打包成一个整体，便于存储、传输和播放。

**核心作用**：

- 整合多种媒体数据（音频、视频、字幕、图片等）。
- 记录元数据（文件信息、编码参数、章节索引等）。
- 支持索引和快速定位（如拖动进度条）。
- 适配不同场景（本地播放、流媒体传输、后期编辑等）。



### 封装格式与编解码的区别

- 封装格式（容器）：负责 “组织” 数据，如 MP4、MKV、AVI。
- 编解码格式：负责 “压缩 / 解压缩” 数据，如 H.264（视频编码）、AAC（音频编码）。
- 二者是独立概念，一个容器可包含多种编码的数据流（例如 MP4 中可同时有 H.264 视频和 AAC 音频）。



### 常见封装格式分类及特点

#### 1. 通用多媒体容器（本地播放/存储）

| 格式 | 扩展名    | 特点                                                         | 典型应用场景                          |
| ---- | --------- | ------------------------------------------------------------ | ------------------------------------- |
| MP4  | .mp4/.m4v | - 兼容性极强，支持主流设备和平台<br/>- 适合存储高清视频，支持元数据和加密<br/>- 结构紧凑，适合流媒体传输 | 网络视频（YouTube、手机视频）、短视频 |
| MKV  | .mkv      | - 开源、免费，支持无限数量的音视频轨和字幕<br/>- 支持章节、标签、无损压缩<br/>- 文件体积可能较大 | 高清影视、多语言字幕场景              |
| AVI  | .avi      | - 早期主流格式，结构简单<br/>- 不支持音视频同步修正，不适合高清或长时间视频<br/>- 逐渐被 MP4/MKV 取代 | 旧版软件兼容性、简单视频存储          |
| MOV  | .mov      | - 苹果公司标准格式，支持高质量视频和元数据<br/>- 常用于专业后期（如 Final Cut Pro）<br/>- 可封装 Apple ProRes 等编码 | 影视后期制作、苹果设备                |

#### 2. 流媒体（实时传输）

| 格式 | 扩展名 | 特点                                                         | 典型应用场景                        |
| ---- | ------ | ------------------------------------------------------------ | ----------------------------------- |
| FLV  | .flv   | - 体积小，加载快，适合 HTTP 流式传输<br/>- 曾广泛用于 Flash 播放器，现逐渐被 HLS/DASH 取代 | 早期在线视频（优酷、土豆）          |
| TS   | .ts    | - 固定每帧 188 字节，适合实时传输和容错<br/>- 常用于数字电视（DVB）、IPTV、直播流 | 电视广播、实时流媒体（如 HLS 切片） |
| M3U8 | .m3u8  | - 文本格式，记录 TS/MP4 切片的 URL 列表<br/>- 支持自适应码率，跨平台流媒体传输 | 苹果 HLS 协议、直播 / 点播流媒体    |



### 封装格式的核心组成部分

以 MP4 和 MKV 为例，典型容器的结构包含以下部分：

> - **文件头**（Header）：
  存储文件总体信息（版本、容量、时间戳、编码参数等）。
  例如 MP4 的ftyp（文件类型）和moov（媒体容器头）。
> - **数据块**（Data Chunks）：
  存储实际的音视频数据（如 MP4 的mdat数据块，MKV 的Cluster元素）。
> - **索引**（Index）：
  记录数据在文件中的位置，用于快速定位（如拖动进度条时跳过无关数据）。
  例如 MP4 的moov中的stco（样本坐标表）。
> - **元数据**（Metadata）：
  包含标题、作者、章节、字幕轨道信息等（如 MKV 的Info元素，MP4 的ilst元数据框）。



### 如何选择合适的封装格式？

- 兼容性：优先选 MP4（通用设备）或 WebM（网页）。

- 功能需求：

  > 多字幕 / 多音轨：MKV。
  > 流媒体直播：TS（HLS 切片）或 FLV（旧版 Flash）。
  > 专业后期：MOV（ProRes 编码）或 MXF。
- 文件体积：WebM/MP4（压缩效率高）＜MKV（无损支持）＜AVI（无压缩或低压缩）。



## 解封装

1.分配解复用器上下文AVFormatContext

2.根据url打开本地文件或网络流avformat_open_input

3.读取媒体的部分数据包以获取码流信息avformat_find_stream_info

4.循环读取码流信息

> a.从文件中读取数据包av_read_frame
>
> b.定位文件avformat_seek_file或av_seek_frame

5.关闭解复用器avformat_close_input



### 实战之读取媒体信息

```c++
extern "C"
{
#include <libavformat/avformat.h>
}

static double r2d(AVRational r) {
    return r.den == 0 ? 0 : (double)r.num / (double) r.den;
}

int testDemuxer1()
{
    const char * fileName = "D:\\videos\\test.mp4";
    AVDictionary * opts = nullptr;
    AVFormatContext * pFmtCtx = nullptr;

    int ret = avformat_open_input(&pFmtCtx, fileName, nullptr, &opts);
    if(ret !=0) {
        char buf[1024] = {0};
        printf("open %s failed, %s\n", fileName, buf);
        return EXIT_FAILURE;
    }

    qDebug() << "打开媒体文件成功";

    ret = avformat_find_stream_info(pFmtCtx, nullptr);
    if(ret < 0) {
        fprintf(stderr, "avformat_find_stream_info error\n");
        return EXIT_FAILURE;
    }

    qDebug() << "媒体文件名字: " << pFmtCtx->filename;
    qDebug() << "音视频流的个数:" << pFmtCtx->nb_streams;
    qDebug() << "媒体文件的平均码率: " << pFmtCtx->bit_rate << "bps";
    qDebug() << "媒体文件总时长: " << pFmtCtx->duration << "微秒";

    int tus = pFmtCtx->duration / AV_TIME_BASE;
    int thh = tus / 3600;
    int tmm = (tus % 3600) / 60;
    int tss = tus % 60;
    qDebug() << "媒体文件总时长: "<< thh << "时" << tmm << "分" << tss << "秒";

    for(int i = 0; i < pFmtCtx->nb_streams; ++i) {
        AVStream * as = pFmtCtx->streams[i];
        if(AVMEDIA_TYPE_AUDIO == as->codecpar->codec_type) {
            //如果是音频流，则打印音频流的信息
            qDebug() << "\n>> 音频信息:";
            qDebug() << "index: " << as->index;
            qDebug() << "音频采样率: " << as->codecpar->sample_rate << "Hz";
            qDebug() << "音频声道数目: " << as->codecpar->channels;

            if(AV_SAMPLE_FMT_FLTP == as->codecpar->format) {
                qDebug() << "音频采样格式: AV_SAMPLE_FMT_FLTP";
            } else if(AV_SAMPLE_FMT_S16P == as->codecpar->format) {
                qDebug() << "音频采样格式： AV_SAMPLE_FMT_S16P";
            }

            if(AV_CODEC_ID_AAC == as->codecpar->codec_id) {
                qDebug() << "音频压缩编码格式:AAC";
            } else if(AV_CODEC_ID_MP3 == as->codecpar->codec_id) {
                qDebug() << "音频压缩编码格式:MP3";
            }

            qDebug() << "as->duration:" << as->duration;//单位为毫秒
            qDebug()<< as->time_base.num << "/" << as->time_base.den;
            qDebug() << "r2d:" << r2d(as->time_base);
            int durationAudio = as->duration * r2d(as->time_base);
            int thh = durationAudio / 3600;
            int tmm = durationAudio % 3600 / 60;
            int tss = durationAudio % 60;
            qDebug() << "音频总时长:" << thh << "时" << tmm << "分" << tss << "秒";
        } else if(AVMEDIA_TYPE_VIDEO == as->codecpar->codec_type) {
            qDebug() << "\n>> 视频信息:";
            qDebug() << "index: " << as->index;
            qDebug() << "视频帧率:" << r2d(as->avg_frame_rate);
            qDebug() << "视频分辨率:" << as->codecpar->width << "x" <<as->codecpar->height;
            //qDebug() << "视频压缩编码格式:" << as->codecpar->codec_id;
            if(AV_CODEC_ID_H264 == as->codecpar->codec_id) {
                qDebug() << "视频压缩编码格式:H264";
            } else if(AV_CODEC_ID_FLV1 == as->codecpar->codec_id) {
                qDebug() << "视频压缩编码格式:FLV";
            }

            int durationVideoS = as->duration * r2d(as->time_base);
            qDebug() << "视频总时长:" << durationVideoS / 3600 << "时"
                     << durationVideoS % 3600 / 60 << "分"
                     << durationVideoS % 60 << "秒";
        }
     }

    //退出程序时，回收资源
    if(pFmtCtx) {
        avformat_close_input(&pFmtCtx);
    }

    return 0;
}
```

输出结果：

![image-20250424143234781](.\assets\image-20250424143234781.png)



### **AVFormatContext结构体[1]**

```c
typedef struct AVFormatContext {
    AVIOContext *pb;
    
    unsigned int nb_streams;
    AVStream **streams;
    
    char *url;
    int64_t duration;
    int64_t bit_rate;
}AVFormatContext;
```



### **AVStream结构体[2]**

```c
typedef struct AVStream {
  	int index; 
    AVRational time_base;
    int64_t duration;
    AVRational avg_frame_rate;
    AVCodecParameters *codecpar;
}AVStream;
```



### 像素格式

**YUV420P**是 FFmpeg、视频编码、图像处理中**最最常用的像素格式**，几乎所有 H.264/H.265 视频默认都是它。

- YUV：亮度（黑白画面） + 色度（颜色）

人眼对**亮度敏感，对颜色不敏感**，所以可以**压缩颜色数据**，体积变小，画质几乎不变。

- 420：色度采样比例（压缩颜色）

4:2:0 色度采样，每4个亮度Y像素，只保留1个U和1个V，一句话，颜色信息被压缩了 ½，但人眼看不出来区别！优点是体积最小，兼容性最好，所有编码器、播放器都支持。

- P：Planar（平面格式），数据分开存储，意思是Y/U/V数据分成三块独立存储，不混在一起！

>  data[0]:  YYYY ... 
>
> data[1]：UUUU ...
>
> data[2]：VVVV ...

对比非平面格式(YUYV)， 交替混存

> Y U Y V Y U Y V ...



### 音频采样格式

音频采样格式之平面格式（以P结尾）：

> **平面格式**：planar， 多声道数据分开连续存储。
>
> 	- 声道0： L L L L ...
> 	- 声道1： R R R R ...

音频采样格式之交错格式（非P结尾）

> **交错格式**：多声道数据交替存储。
>
> - L R L R L R ...

在FFmpeg的`libavutil/samplefmt.h` 中

```c
enum AVSampleFormat {
    AV_SAMPLE_FMT_NONE = -1,
    AV_SAMPLE_FMT_U8,   ///< 无符号8位整型（交错）
    AV_SAMPLE_FMT_S16,  ///< 有符号16位整型（交错）
    AV_SAMPLE_FMT_S32,  ///< 有符号32位整型（交错）
    AV_SAMPLE_FMT_FLT,  ///< 32位浮点型（交错）
    AV_SAMPLE_FMT_DBL,  ///< 64位双精度浮点型（交错）

    AV_SAMPLE_FMT_U8P,  ///< 无符号8位整型（平面）
    AV_SAMPLE_FMT_S16P, ///< 有符号16位整型（平面）
    AV_SAMPLE_FMT_S32P, ///< 有符号32位整型（平面）
    AV_SAMPLE_FMT_FLTP, ///< 32位浮点型（平面） ✅    
    AV_SAMPLE_FMT_DBLP, ///< 64位双精度浮点型（平面）
    ...
};
```

**AV_SAMPLE_FMT_S32P** 为FFmpeg内部的默认处理格式，兼容性最好。





### **av_find_best_stream函数**

av_find_best_stream 是 FFmpeg 库中的一个实用函数，它的主要作用是在 AVFormatContext 所代表的媒体文件的多个流中，找出指定类型（如视频流、音频流、字幕流等）的最合适的流，**并返回该流的索引**。这个函数会综合考虑流的质量、编解码器支持情况等因素，帮助开发者快速定位到最适合处理的流。

#### 函数原型

```c
int av_find_best_stream(AVFormatContext *ic,
                        enum AVMediaType type,
                        int wanted_stream_nb,
                        int related_stream,
                        AVCodec **decoder_ret,
                        int flags);
```

> 参数：
>
> - ic: 指向 `AVFormatContext` 结构体的指针，该结构体表示输入文件的格式上下文。
> - type:  指定要查找的流的类型。AVMediaType 是一个枚举类型，常见的取值有：
>   AVMEDIA_TYPE_VIDEO：表示视频流。
>   AVMEDIA_TYPE_AUDIO：表示音频流。
>   AVMEDIA_TYPE_SUBTITLE：表示字幕流。
> - wanted_stream_nb: 指定希望查找的流的索引。**如果该值为 -1，则表示不指定具体的流索引，函数会自动选择最合适的流**；如果指定了一个有效的流索引，函数会优先考虑该索引对应的流是否符合要求。
>   
> - related_stream: 用于指定一个相关的流索引。这个参数通常用于音频流和视频流之间的关联，例如在查找音频流时，可以传入一个视频流的索引，函数会优先选择与该视频流关联的音频流。**如果不需要关联，可以将其设置为 -1**。 
>   
> - decoder_ret: 指向 AVCodec 指针的指针，用于存储找到的流对应的解码器。如果该参数不为 NULL，函数会查找并返回适合该流的解码器指针。开发者可以使用这个解码器来进行后续的解码操作。
> - flags: 控制查找过程的标志位。目前常用的标志位有 AV_FIND_BEST_STREAM_SCORE，该标志表示在选择流时，会综合考虑流的分数（如分辨率、帧率、比特率等因素），选择分数最高的流。如果不使用该标志，函数会按照默认规则选择流。
>
> 返回值： 
>
> - 成功：返回找到的流的索引，该索引是一个非负整数。
> - 失败： 返回一个负的错误码，表示没有找到符合要求的流。常见的错误码包括 AVERROR_STREAM_NOT_FOUND（未找到指定类型的流）等。



### 实战之读取媒体信息2

```c++
int testDemuxer2()
{
    const char * fileName = "D:\\videos\\test.mp4";
    AVDictionary * opts = nullptr;
    AVFormatContext * pFmtCtx = nullptr;

    int ret = avformat_open_input(&pFmtCtx, fileName, nullptr, &opts);
    if(ret !=0) {
        char buf[1024] = {0};
        printf("open %s failed, %s\n", fileName, buf);
        return EXIT_FAILURE;
    }

    qDebug() << "打开媒体文件成功";

    ret = avformat_find_stream_info(pFmtCtx, nullptr);
    if(ret < 0) {
        fprintf(stderr, "avformat_find_stream_info error\n");
        return EXIT_FAILURE;
    }

    qDebug() << "媒体文件名字: " << pFmtCtx->url;
    qDebug() << "音视频流的个数:" << pFmtCtx->nb_streams;
    qDebug() << "媒体文件的平均码率: " << (int64_t)pFmtCtx->bit_rate /1024 << "kbps";
    qDebug() << "媒体文件总时长: " << pFmtCtx->duration << "微秒";

    int tus = pFmtCtx->duration / AV_TIME_BASE;
    int thh = tus / 3600;
    int tmm = (tus % 3600) / 60;
    int tss = tus % 60;
    qDebug() << "媒体文件总时长: "<< thh << "时" << tmm << "分" << tss << "秒";

    //读取音频信息
    int audioIndex = av_find_best_stream(pFmtCtx, AVMEDIA_TYPE_AUDIO, -1, -1, nullptr, 0);
    if(audioIndex < 0) {
        qDebug() << "av_find_best_stream error:" << av_get_media_type_string(AVMEDIA_TYPE_AUDIO);
        return EXIT_FAILURE;
    }
    AVStream * as = pFmtCtx->streams[audioIndex];
    qDebug() <<"\n=== 音频信息 ===";
    qDebug() << "index: " << as->index;
    qDebug() << "音频采样率: " << as->codecpar->sample_rate << "Hz";
    qDebug() << "音频声道数目: " << as->codecpar->channels;

    if(AV_SAMPLE_FMT_FLTP == as->codecpar->format) {
        qDebug() << "音频采样格式: AV_SAMPLE_FMT_FLTP";
    } else if(AV_SAMPLE_FMT_S16P == as->codecpar->format) {
        qDebug() << "音频采样格式： AV_SAMPLE_FMT_S16P";
    }

    if(AV_CODEC_ID_AAC == as->codecpar->codec_id) {
        qDebug() << "音频压缩编码格式:AAC";
    } else if(AV_CODEC_ID_MP3 == as->codecpar->codec_id) {
        qDebug() << "音频压缩编码格式:MP3";
    }

    if(as->duration != AV_NOPTS_VALUE) {
        int durationAudio = as->duration * r2d(as->time_base);
        int thh = durationAudio / 3600;
        int tmm = durationAudio % 3600 / 60;
        int tss = durationAudio % 60;
        qDebug() << "音频总时长:" << thh << "时" << tmm << "分" << tss << "秒";
    }

    //读取视频信息
    int videoIndex = av_find_best_stream(pFmtCtx, AVMEDIA_TYPE_VIDEO, -1, -1, nullptr, 0);
    if(videoIndex < 0) {
        qDebug() << "av_find_best_stream error:" << av_get_media_type_string(AVMEDIA_TYPE_VIDEO);
        return EXIT_FAILURE;
    }
    as = pFmtCtx->streams[videoIndex];
    qDebug() << "\n=== 视频信息===";
    qDebug() << "index: " << as->index;
    qDebug() << "视频帧率:" << r2d(as->avg_frame_rate);
    qDebug() << "视频分辨率:" << as->codecpar->width << "x" <<as->codecpar->height;
    //qDebug() << "视频压缩编码格式:" << as->codecpar->codec_id;
    if(AV_CODEC_ID_H264 == as->codecpar->codec_id) {
        qDebug() << "视频压缩编码格式:H264";
    } else if(AV_CODEC_ID_FLV1 == as->codecpar->codec_id) {
        qDebug() << "视频压缩编码格式:FLV";
    }

    int durationVideoS = as->duration * r2d(as->time_base);
    qDebug() << "视频总时长:" << durationVideoS / 3600 << "时"
             << durationVideoS % 3600 / 60 << "分"
             << durationVideoS % 60 << "秒";

    //退出程序时，回收资源
    if(pFmtCtx) {
        avformat_close_input(&pFmtCtx);
    }

    return 0;
}
```

输出结果：

![image-20250424145800288](.\assets\image-20250424145800288.png)





### av_packet_alloc函数

av_packet_alloc 的主要功能是在堆上动态分配一个 AVPacket 结构体实例，并对其进行初始化。AVPacket 是 FFmpeg 中用于存储压缩的音频或视频数据的基本数据结构，它包含了编码后的音视频数据、时间戳、数据流索引等信息。通过 av_packet_alloc 分配的 AVPacket 可以用于后续的音视频数据读取、解码等操作。

#### 函数原型

```c
AVPacket *av_packet_alloc(void);
```

当av_read_frame 用于从输入文件中读取一个 AVPacket。在调用 av_read_frame 之前，需要先使用 av_packet_alloc 分配一个 AVPacket 实例。



### **AVPacket结构体[3]**

```c
typedef struct AVPacket {
    AVBufferRef *buf;
    int64_t pts;//
    int64_t dts;//
    uint8_t *data;// H264/MP3
    int size;
    int stream_index;
    int flags;
    AVPacketSideData *side_data;
    int side_data_elems;
    int64_t duration;//
    int64_t pos;//
}AVPacket;
```

> 字段含义：
>
> - buf:  指向**引用计数**缓冲区的指针，用于存储数据包的数据。如果该指针为 NULL，则表示数据包的数据不是引用计数的。**引用计数机制允许多个 AVPacket 共享同一份数据，提高内存使用效率。**
>
> - pts: 显示时间戳（Presentation Timestamp），以 `AVStream->time_base` 为单位。它表示解压缩后的数据包将在何时呈现给用户。如果文件中未存储该时间戳，则其值为 `AV_NOPTS_VALUE`。pts 必须大于或等于 dts，因为显示不能在解压缩之前发生。
>
> - dts: 解码时间戳（Decompression Timestamp），以 `AVStream->time_base` 为单位。它表示数据包将在何时进行解压缩。同样，如果文件中未存储该时间戳，则其值为 `AV_NOPTS_VALUE`。在AVPacket中，dts是递增的
>
> - **data**: 指向存储**压缩音频或视频数据**的缓冲区的指针。**这是数据包的实际数据内容**， **例如H264编码的视频帧**。
>
> - **size**: 表示 data 指针所指向的缓冲区中数据的大小，单位为字节。
>
> - stream_index: 表示该数据包所属的流的索引。在一个媒体文件中可能包含多个流（如视频流、音频流、字幕流等），通过该索引可以确定数据包属于哪个流。
>
> - flags: 这是一个标志位，是 AV_PKT_FLAG 值的组合。常见的标志位有：
>   `AV_PKT_FLAG_KEY`：表示该数据包是一个关键帧（I 帧）。
>   `AV_PKT_FLAG_DISCARD`：表示该数据包可以被丢弃，无需进行解码和处理。
>
> - side_data: 指向 AVPacketSideData 数组的指针，用于存储额外的数据包数据，这些数据可以由容器提供。数据包可以包含多种类型的附加信息，如字幕信息、视频旋转信息等。
>   
> - side_data_elems: 表示 `side_data` 数组中元素的数量。
>   
> - duration: 表示该数据包的持续时间，以 `AVStream->time_base` 为单位。如果未知，则值为 0。它等于下一个数据包的 pts 减去当前数据包的 pts（按呈现顺序）。
>
> - pos: 表示该**数据包在流中的字节位置**，如果未知则为 -1。



### av_read_frame函数

av_read_frame 是 FFmpeg 库中用于从输入文件中读取一个**完整的压缩数据包**（AVPacket）的函数。在音视频处理流程里，当使用 avformat_open_input 打开输入文件并通过 avformat_find_stream_info 分析流信息之后，就可以借助 av_read_frame 函数逐个读取文件中的压缩数据包，这些数据包包含了编码后的音频或视频数据。之后可以将读取到的数据包传递给解码器进行解码操作，从而得到原始的音视频帧。

#### 函数原型

```c
int av_read_frame(AVFormatContext *s, AVPacket *pkt);
```

> 参数：
>
> - s: 指向 AVFormatContext 结构体的指针，该结构体表示输入文件的格式上下文。
> - pkt: 指向 AVPacket 结构体的指针，用于存储读取到的压缩数据包。
>
> 返回值：
>
> - 成功： 返回值为 0，表示成功从输入文件中读取到一个完整的数据包，并存储在 pkt 中。
> - 文件结束： 返回值为 AVERROR_EOF，表示已经读取到文件末尾，没有更多的数据包可供读取。
> - 失败：返回一个负的错误码，表示在读取过程中出现了错误。常见的错误码包括 AVERROR(EIO)（输入输出错误，可能是文件读取失败、网络问题等）、AVERROR(ENOMEM)（内存分配失败）等。



### av_packet_unref函数

av_packet_unref 函数的核心功能是将 AVPacket 结构体重置为初始状态，同时释放其内部引用的数据缓冲区。当一个 AVPacket 不再需要使用时，调用该函数可以确保其占用的资源被正确释放，防止出现内存泄漏问题。

#### 函数原型

```c
void av_packet_unref(AVPacket *pkt);
```



### 实战之读取媒体信息3

```c++
int testDemuxer3()
{
    const char * fileName = "D:\\videos\\test.mp4";
    AVDictionary * opts = nullptr;
    AVFormatContext * pFmtCtx = nullptr;

    int ret = avformat_open_input(&pFmtCtx, fileName, nullptr, &opts);
    if(ret !=0) {
        char buf[1024] = {0};
        printf("open %s failed, %s\n", fileName, buf);
        return EXIT_FAILURE;
    }

    qDebug() << "打开媒体文件成功";

    ret = avformat_find_stream_info(pFmtCtx, nullptr);
    if(ret < 0) {
        fprintf(stderr, "avformat_find_stream_info error\n");
        return EXIT_FAILURE;
    }

    //打印媒体文件中流的信息
    av_dump_format(pFmtCtx, -1, fileName, 0);

    int audioIndex = av_find_best_stream(pFmtCtx, AVMEDIA_TYPE_AUDIO, -1, -1, nullptr, 0);
    int videoIndex = av_find_best_stream(pFmtCtx, AVMEDIA_TYPE_VIDEO, -1, -1, nullptr, 0);
    qDebug() << "audio index:" << audioIndex << ", video index:" << videoIndex << "\n";

    //提取码流
    AVPacket * packet = av_packet_alloc();
    int count = 50;
    while(count-- > 0) {
        ret = av_read_frame(pFmtCtx, packet);
        if(ret < 0) {
            qDebug() << "av_read_frame end";
            break;
        }

        if(packet->stream_index == audioIndex) {
            qDebug() << "audio pts: " << packet->pts;
            qDebug() << "audio dts: " << packet->dts;
            qDebug() << "audio size:" << packet->size;
            qDebug() << "audio pos: " << packet->pos;
            qDebug() << "audio duration:" <<
                    packet->duration * av_q2d(pFmtCtx->streams[audioIndex]->time_base) << "\n";
        } else if(packet->stream_index == videoIndex) {
            qDebug() << "video pts: " << packet->pts;
            qDebug() << "video dts: " << packet->dts;
            qDebug() << "video size:" << packet->size;
            qDebug() << "video pos: " << packet->pos;
            qDebug() << "video duration:" <<
                    packet->duration * av_q2d(pFmtCtx->streams[videoIndex]->time_base) << "\n";
        } else {
            qDebug() << "unknow stream index:" << packet->stream_index;
        }
        //释放数据包中的数据
        av_packet_unref(packet);
    }//end of while

    if(packet) {
        av_packet_free(&packet);//释放AVPacket
    }
    if(pFmtCtx) {
        avformat_close_input(&pFmtCtx);
    }
    return 0;
}
```



## 转封装(录像功能)

所谓的封装格式转换，就是在AVI、FLV、MP4、MKV这些格式之间转换，对应的就是*.avi*  *.flv*   *.mp4*  *.mkv* 文件。需要注意的是，在格式转换时，并不需要做编码和解码的工作，而是直接将音视频压缩编码从一种封装格式中取出来，再打包成另外一种封装格式即可。

转码程序的工作原理示意图如下所示：

![image-20250425154407546](.\assets\image-20250425154407546.png)



### avformat_alloc_output_context2函数

avformat_alloc_output_context2 是 FFmpeg 库中一个重要的函数，其主要作用是分配并初始化一个 AVFormatContext 结构体实例，该结构体代表了输出文件的格式上下文。在进行音视频文件的输出操作时，比如将编码后的音视频数据封装成特定格式的文件（如 MP4、AVI 等），就需要使用这个函数来创建一个合适的输出上下文，为后续的文件输出操作做好准备。

#### 函数原型

```c
int avformat_alloc_output_context2(AVFormatContext **ctx, 
                                   ff_const59 AVOutputFormat *oformat, 
                                   const char *format_name, 
                                   const char *filename);
```

> 参数：
>
> - ctx： 指向 AVFormatContext 指针的指针。函数会在堆上分配一个 AVFormatContext 结构体实例，并将该实例的地址存储在 *ctx 中。因此，在调用该函数之前，ctx 应该是一个有效的指针，调用结束后，通过 *ctx 就可以访问到新分配的 AVFormatContext 结构体。
> - oformat:  指向 AVOutputFormat 结构体的指针，用于指定输出文件的格式。如果该参数为 NULL，函数会根据 format_name 或 filename 的扩展名来自动推断输出格式。AVOutputFormat 结构体包含了关于输出文件格式的各种信息，如支持的编解码器、文件扩展名等。
> - format_name:  指定输出文件的格式名称，如 "mp4"、"avi" 等。如果 oformat 为 NULL，函数会根据这个名称来查找对应的 AVOutputFormat。如果该参数也为 NULL，则会根据 filename 的扩展名来推断输出格式。
> - filename:  指定输出文件的名称。如果 oformat 和 format_name 都为 NULL，函数会根据该文件名的扩展名来推断输出格式。同时，该文件名也会被存储在 AVFormatContext 中，用于后续的文件打开和写入操作。
>
> 返回值：
>
> - **成功**：返回 0，表示成功分配并初始化了 `AVFormatContext` 结构体。
> - **失败**：返回一个负的错误码，表示在分配或初始化过程中出现了问题。常见的错误码包括 AVERROR(ENOMEM)（内存分配失败）等。



### avformat_new_stream函数

在 FFmpeg 里，avformat_new_stream 函数的主要作用是在 AVFormatContext 中创建一个新的流（AVStream）。当你需要将音视频数据封装到输出文件时，往往要为不同类型的数据（像视频、音频、字幕等）分别创建对应的流，然后把数据写入这些流中，最后完成文件的封装。avformat_new_stream 函数就是用来创建这些流的关键工具。

#### 函数原型

```c
AVStream *avformat_new_stream(AVFormatContext *s, const AVCodec *c);
```

> 参数：
>
> - s: 向 AVFormatContext 结构体的指针，该结构体代表了输入或输出文件的格式上下文。在调用 avformat_alloc_output_context2 分配输出格式上下文之后，就可以使用这个上下文指针来创建新的流。新创建的流会被添加到 s 所指向的 AVFormatContext 的流列表中。
> - c: 指向 AVCodec 结构体的指针，用于指定该流所使用的编解码器。如果该参数为 NULL，则不会为流指定特定的编解码器，后续可以手动设置流的编解码器参数。在实际应用中，若已经确定了要使用的编解码器，可传入相应的 AVCodec 指针，这样流会根据该编解码器的默认参数进行初始化。
>
> 返回值：
>
> - **成功**：返回一个指向新创建的 AVStream 结构体的指针，通过该指针可以访问和设置新流的各种属性，如编解码器参数、时间基等。
> - **失败**：返回 NULL，表示创建新流失败，可能是由于内存分配失败等原因导致的。



### **AVCodecParameters结构体[4]**

```c
typedef struct AVCodecParameters {
    enum AVMediaType codec_type;
    enum AVCodecID   codec_id;
    int64_t bit_rate;
    int width;
    int height;
    int format;
    
    AVRational sample_aspect_ratio;
    int video_delay;
    enum AVColorRange                  color_range;
    enum AVColorPrimaries              color_primaries;
    enum AVColorTransferCharacteristic color_trc;
    enum AVColorSpace                  color_space;
    enum AVChromaLocation              chroma_location;
    
    int      channels;
    int      sample_rate;
    int      block_align;
    int      frame_size;
};
```



### **avcodec_parameters_copy函数**

avcodec_parameters_copy 是 FFmpeg 库中的一个实用函数，主要用于将一个 AVCodecParameters 结构体中的编解码器参数复制到另一个 AVCodecParameters 结构体中。在音视频处理流程里，当需要在不同的流或者不同的上下文中复用相同的编解码器参数时，使用这个函数可以避免手动逐个复制参数，提高代码的效率和可维护性。例如，在进行音视频文件的转码或者封装操作时，可能需要将输入流的编解码器参数复制到输出流中。

#### 函数原型

```c
int avcodec_parameters_copy(AVCodecParameters *dst, const AVCodecParameters *src);
```

>  参数:
>
> - dst:  指向目标 AVCodecParameters 结构体的指针。该结构体用于接收从源 AVCodecParameters 结构体复制过来的编解码器参数。在调用 avcodec_parameters_copy 之前，dst 应该已经被正确分配内存，通常可以使用 avcodec_parameters_alloc 函数来分配 AVCodecParameters 结构体。
> - src:  指向源 AVCodecParameters 结构体的指针。该结构体包含了要复制的编解码器参数，如编解码器 ID、比特率、采样率、分辨率等。src 应该已经被正确初始化，通常在打开输入文件并分析流信息后，可以从 AVStream 结构体中获取到 AVCodecParameters 结构体。
>
> 返回值：
>
> - **成功**：返回 0，表示编解码器参数复制成功。
> - **失败**：返回一个负的错误码，表示在复制过程中出现了问题。常见的错误码包括 `AVERROR(ENOMEM)`（内存分配失败）等。



### avio_open函数

avio_open 是 FFmpeg 库中用于打开输入 / 输出（I/O）上下文的函数。在 FFmpeg 的音视频处理流程中，音视频数据的读写操作往往依赖于 AVIOContext 这个 I/O 上下文。avio_open 函数的主要作用就是为指定的文件或 URL 分配并初始化一个 AVIOContext，并将其与 AVFormatContext 关联起来，以便后续进行音视频数据的读写操作。

#### 函数原型

```c
int avio_open(AVIOContext **s, const char *url, int flags);
```

> 参数：
>
> - s：指向 AVIOContext 指针的指针。函数会在堆上分配一个 AVIOContext 结构体实例，并将该实例的地址存储在 *s 中。因此，在调用该函数之前，s 应该是一个有效的指针，调用结束后，通过 *s 就可以访问到新分配的 AVIOContext 结构体。这个结构体包含了文件或 URL 的 I/O 操作所需的各种信息，如文件指针、缓冲区等。
> - url：指定要打开的文件或 URL 的名称。可以是本地文件的路径，也可以是网络资源的 URL。FFmpeg 支持多种协议，包括文件系统协议（如 file://）、网络协议（如 http://、rtsp:// 等），可以根据不同的协议来访问不同的资源。
> - flags：用于指定打开文件或 URL 的模式，是一个标志位的组合。常见的标志位如下：
>   AVIO_FLAG_READ：以只读模式打开文件或 URL，用于读取数据。
>   AVIO_FLAG_WRITE：以只写模式打开文件或 URL，用于写入数据。如果文件不存在，会尝试创建该文件；如果文件已存在，会将其内容截断为零。
>   AVIO_FLAG_READ_WRITE：以读写模式打开文件或 URL，既可以读取数据，也可以写入数据。
>   AVIO_FLAG_APPEND：以追加模式打开文件或 URL，用于在文件末尾追加数据。
>
> 返回值：
>
> - **成功**：返回 0，表示成功打开并初始化了 `AVIOContext` 结构体。
> - **失败**：返回一个负的错误码，表示在打开过程中出现了问题。常见的错误码包括 `AVERROR(EIO)`（输入输出错误，可能是文件不存在、权限不足等）、`AVERROR(ENOMEM)`（内存分配失败）等。



### avformat_write_header函数

avformat_write_header 是 FFmpeg 库中用于向输出文件写入文件头信息的函数。在进行音视频文件封装时，文件头包含了关于文件整体结构、编码格式、流信息等重要元数据，这些信息对于播放器正确解析和播放文件至关重要。avformat_write_header 函数会根据 AVFormatContext 中的配置信息生成合适的文件头，并将其写入到输出文件中，为后续写入音视频数据做好准备。

#### 函数原型

```c
int avformat_write_header(AVFormatContext *s, AVDictionary **options);
```

> 参数：
>
> - s:  指向 AVFormatContext 结构体的指针，该结构体代表了输出文件的格式上下文。在调用 avformat_alloc_output_context2 分配输出格式上下文后，需要对 AVFormatContext 进行一系列的配置，如设置流信息、编解码器参数等。avformat_write_header 函数会根据 s 中的这些配置信息来生成并写入文件头。
> - options:   指向 AVDictionary 指针的指针，用于传递一些额外的选项。AVDictionary 是 FFmpeg 中用于存储键值对的字典结构，可以在其中设置一些特定的参数，以影响文件头的写入过程。例如，可以设置一些与文件格式相关的选项，如比特率、帧率等。如果不需要传递额外的选项，可以将该参数设置为 NULL。
>
> 返回值：
>
> - 成功：返回 0，表示成功写入文件头。
> - 失败：返回一个负的错误码，表示在写入文件头过程中出现了问题。常见的错误码包括 AVERROR(EIO)（输入输出错误，可能是文件写入失败）、AVERROR(ENOMEM)（内存分配失败）等。



### av_write_trailer函数

av_write_trailer 是 FFmpeg 库中的一个重要函数，其主要作用是向输出文件写入文件尾信息并完成文件的封装操作。在音视频处理流程中，当所有的音视频数据包都已经写入到输出文件后，需要调用该函数来完成文件的收尾工作，包括写入索引信息、更新文件头中的某些元数据等，以确保输出文件的完整性和可播放性。

#### 函数原型

```c
int av_write_trailer(AVFormatContext *s);
```

>  参数:
>
> - s: 指向 AVFormatContext 结构体的指针，该结构体代表了输出文件的格式上下文。在整个音视频封装过程中，AVFormatContext 存储了关于输出文件的各种信息，如文件格式、流信息、编解码器参数等。av_write_trailer 函数会根据 s 中的这些信息来完成文件尾的写入操作。
>
> 返回值：
>
> - **成功**：返回 0，表示成功写入文件尾并完成文件的封装操作。
> - **失败**：返回一个负的错误码，表示在写入文件尾过程中出现了问题。常见的错误码包括 `AVERROR(EIO)`（输入输出错误，可能是文件写入失败）等。



### 转封装实战之MP4转FLV

流程图如下：

![image-20250425172455667](.\assets\image-20250425172455667.png)

```c++
#define __STDC_FORMAT_MACROS

extern "C"
{
#include <libavutil/mathematics.h>
#include <libavformat/avformat.h>
#include <libavutil/timestamp.h>
}
const char * my_ts2str(int64_t t)
{
    char buff[AV_TS_MAX_STRING_SIZE] = {0};
    av_ts_make_string(buff, t);
    return buff;
}

const char * my_ts2timestr(int64_t t, AVRational * tb)
{
    char buff[AV_TS_MAX_STRING_SIZE] = {0};
    av_ts_make_time_string(buff, t, tb);
    return buff;
}

const char * my_err2str(int ret)
{
    char buff[AV_ERROR_MAX_STRING_SIZE] = {0};
    av_make_error_string(buff, AV_ERROR_MAX_STRING_SIZE, ret);
    return buff;
}

static void log_packet(const AVFormatContext * fmt_ctx, const AVPacket * pkt, const char * tag)
{
    AVRational * time_base = &fmt_ctx->streams[pkt->stream_index]->time_base;
    printf("%s: pts:%s, pts_time:%s, dts:%s dts_time:%s, duration:%s dtime:%s index:%d\n",
            tag,
            my_ts2str(pkt->pts), my_ts2timestr(pkt->pts, time_base),
            my_ts2str(pkt->dts), my_ts2timestr(pkt->dts, time_base),
            my_ts2str(pkt->duration), my_ts2timestr(pkt->duration, time_base),
           pkt->stream_index);
}

static void log_packet2(const AVPacket * pkt, const char * tag)
{
    printf("%s: pts: %lld, dts: %lld, duration:%lld, index:%d\n",
           tag,
           pkt->pts,
           pkt->dts,
           pkt->duration,
           pkt->stream_index);
}

int test0()
{
    const char * inFilename = "D:\\videos\\lldq-test480.mp4";
    const char * outFilename = "D:\\videos\\lldq-test480.flv";
    AVPacket packet;
    int cnt = 0;

    //1. 打开文件
    AVFormatContext * inFmtCtx = nullptr, * outFmtCtx = nullptr;
    AVOutputFormat * outFmt = nullptr;
    int ret = avformat_open_input(&inFmtCtx, inFilename, nullptr, nullptr);
    if(ret < 0) {
        fprintf(stderr, "avformat_open_input file %s error\n", inFilename);
        return EXIT_FAILURE;
    }

    //2. 查找流媒体信息
    if((ret = avformat_find_stream_info(inFmtCtx, nullptr)) < 0) {
        fprintf(stderr, "avformat_find_stream_info error\n");
        return EXIT_FAILURE;
    }

    int streamMappingSize = 0;
    int * streamMapping = nullptr;


    //3. 打印输入文件的媒体信息
    av_dump_format(inFmtCtx, -1, inFilename, 0);

    //开辟流映射空间
    int streamIndex = 0;
    streamMappingSize = inFmtCtx->nb_streams;

     //4. 分配输出的AVFormatContext,并进行填充
    avformat_alloc_output_context2(&outFmtCtx, nullptr, nullptr, outFilename);
    if(!outFmtCtx) {
        fprintf(stderr, "could not create output context\n");
        ret =AVERROR_UNKNOWN;
        goto end;
    }

    //AVOutputFormat指向AVFormatContext中的oformat(AVOutputFormat)
    outFmt = outFmtCtx->oformat;//

    streamMapping = (int*)av_mallocz_array(streamMappingSize, sizeof(int));
    if(!streamMapping) {
        ret = AVERROR(ENOMEM);
        goto end;
    }

    for(unsigned int i = 0; i < inFmtCtx->nb_streams; ++i) {
        AVStream * inStream = inFmtCtx->streams[i];
        AVCodecParameters * inCodecpar = inStream->codecpar;

        if(inCodecpar->codec_type != AVMEDIA_TYPE_AUDIO &&
           inCodecpar->codec_type != AVMEDIA_TYPE_VIDEO &&
           inCodecpar->codec_type != AVMEDIA_TYPE_SUBTITLE)
        {
            streamMapping[i] = -1;
            continue;
        }

        streamMapping[i] = streamIndex++;
        //4.1 填充输出流的数据AVStream
        AVStream * outStream = nullptr;
        outStream = avformat_new_stream(outFmtCtx, nullptr);
        if(!outStream) {
            fprintf(stderr, "Failed allocating output stream\n");
            ret = AVERROR_UNKNOWN;
            goto end;
        }
        //4.2 拷贝输入流的编码参数到输出流的空间
        ret = avcodec_parameters_copy(outStream->codecpar, inStream->codecpar);
        if(ret < 0) {
            fprintf(stderr, "Failed to copy codec parameters.\n");
            goto end;
        }
        // codec_tag 提供了一种比 codec_id 更为具体的编解码器标识方式。
        //codec_id 是一个枚举值，用于表示通用的编解码器类型（如 H.264、AAC 等），
        //而 codec_tag 则能进一步区分同一编解码器的不同实现或变体。
        outStream->codecpar->codec_tag = 0;//初始值为0
    }//end of for loop
    printf("\n\n------- dump:outFmtCtx -------\n");
    av_dump_format(outFmtCtx, -1, outFilename, 1);


    if(!(outFmt->flags & AVFMT_NOFILE)) {
        //4.3 打开写入文件
        ret = avio_open(&outFmtCtx->pb, outFilename, AVIO_FLAG_WRITE);
        if(ret < 0) {
            fprintf(stderr, "Could not open output file '%s'\n", outFilename);
            goto end;
        }
    }

    //4.4 填写头部信息
    ret = avformat_write_header(outFmtCtx, nullptr);
    if(ret < 0) {
        fprintf(stderr, "Error occurred when writing header\n");
        goto end;
    }

    printf("======= outFmtCtx is ready =======\n\n");

    while(1){
        AVStream * inStream, * outStream;
        //4.5 读取一帧音视频压缩包存入packet
        ret = av_read_frame(inFmtCtx, &packet);
        if(ret < 0) {
            break;
        }

        inStream = inFmtCtx->streams[packet.stream_index];
        if(packet.stream_index >= streamMappingSize ||
           streamMapping[packet.stream_index] < 0) {
            av_packet_unref(&packet);
            continue;
        }
		//packet作为写入的一包数据
        packet.stream_index = streamMapping[packet.stream_index];//???
        outStream = outFmtCtx->streams[packet.stream_index];
        //log_packet(inFmtCtx, &packet, "in");
        log_packet2(&packet, "in");
        //copy packet
        AVRational bq = inStream->time_base;//获取输入的time_base
        AVRational cq = outStream->time_base;//获取输出的time_base
        //4.5 进行转换
        packet.pts = av_rescale_q_rnd(packet.pts, bq, cq, 
                AVRounding(AV_ROUND_NEAR_INF|AV_ROUND_PASS_MINMAX));
        packet.dts = av_rescale_q_rnd(packet.dts, bq, cq, 
        		AVRounding(AV_ROUND_NEAR_INF|AV_ROUND_PASS_MINMAX));
        packet.duration = av_rescale_q(packet.duration, bq, cq);
        packet.pos = -1;
        log_packet2(&packet, "out");

        //4.6 交织写音视频包
        ret = av_interleaved_write_frame(outFmtCtx, &packet);
        if(ret < 0) {
            fprintf(stderr, "Error muxing packet\n");
            break;
        }
        av_packet_unref(&packet);//释放包中的数据
        ++cnt;
        printf("%dth packet is processed.\n", cnt);
    }//end of while(1)
	
    //4.7 写入文件尾信息，完成封装操作
    av_write_trailer(outFmtCtx);
    printf("copy stream over!!!\n");
end:
    if(ret < 0 && ret != AVERROR_EOF)
        fprintf(stderr, "Error occurred: %s\n", my_err2str(ret));
    if(streamMapping) {
        av_freep(streamMapping);
    }
    if(outFmtCtx && !(outFmt->flags & AVFMT_NOFILE))
        avio_closep(&outFmtCtx->pb);
    avformat_free_context(outFmtCtx);

    return 0;
}
```



# 五、编解码层

音视频编解码流程：

![image-20250427144033958](.\assets\image-20250427144033958.png)

从上图可以看成，大流程可以划分为**输入**、**输出**、**转码**和**播放**四大块。其中**转码**涉及比较多的处理环节，从图中可以看出，转码功能在整个功能图中占比很大。**转码的核心功能在解码和编码两个部分**，但在一个可用的示例程序中，编码解码与输入输出是难以分割的。**解复用器**为解码器提供输入，**解码器会输出原始帧**，**对原始帧可进行各种复杂的滤镜处理**，**滤镜处理后的帧经编码器生成编码帧**，多路流的编码帧经复用器输出到输出文件。

## 编码操作实战

### **avcodec_send_frame函数**

avcodec_send_frame 是 FFmpeg 库中的一个重要函数，它主要用于将**未压缩的视频帧或音频帧**发送到**编码器**进行编码处理。下面详细介绍它的作用、参数和使用场景。

#### 函数原型

```c
int avcodec_send_frame(AVCodecContext *avctx, const AVFrame *frame);
```

> 参数：
>
> - avctx：指向 AVCodecContext 结构体的指针，该结构体包含了编码器的上下文信息，如编码格式、帧率、比特率等。它定义了编码器的工作参数和状态。
> - frame：指向 AVFrame 结构体的指针，该结构体包含了未压缩的音视频帧数据。如果传入 NULL，则表示发送一个结束信号，通知编码器没有更多的帧需要编码，编码器会将内部缓存的所有数据进行编码输出。
>
> 返回值：
>
> - 成功：返回 0
> - 失败：返回一个负的错误码，常见的错误码包括：
>   **AVERROR(EAGAIN)**：表示编码器当前无法接受新的帧，可能是因为编码器内部的缓冲区已满。此时需要先调用 avcodec_receive_packet 函数从编码器中取出已编码的数据包，直到 avcodec_send_frame 不再返回 AVERROR(EAGAIN) 为止。
>   **AVERROR_EOF**：表示编码器已经被刷新，不再接受新的帧。
>   **AVERROR(EINVAL)**：表示编码器上下文没有正确初始化，或者编码器不支持编码操作。
>   其他负的错误码表示其他类型的错误。



### **avcodec_receive_packet函数**

avcodec_receive_packet 函数的主要作用是从编码器（在编码场景下）获取已经编码好的数据包（AVPacket），或者从解码器（在解码场景下）获取已经解码后的数据包。在编码时，当你调用 avcodec_send_frame 把未压缩的音视频帧发送给编码器后，编码器会对这些帧进行处理，将其转换为压缩格式，而 avcodec_receive_packet 就是用来从编码器的内部缓冲区中取出这些已编码好的数据包。

#### 函数原型

```c
int avcodec_receive_packet(AVCodecContext *avctx, AVPacket *avpkt);
```

> 参数：
>
> - avctx: 指向 AVCodecContext 结构体的指针。该结构体存储了编解码器的上下文信息，涵盖了编解码所需的各类参数和状态，像编码格式、帧率、比特率等。
> - avpkt: 指向 AVPacket 结构体的指针。AVPacket 用于存储编码后的数据包，调用该函数后，编码好的数据会被填充到这个 AVPacket 中。在使用前，需要确保 avpkt 已经被正确分配内存，通常使用 av_packet_alloc 函数进行分配。
>
> 返回值：
>
> - 成功：返回值为 0，表示成功从编码器中接收到一个已编码的数据包。
> - 失败：返回一个负的错误码，常见错误码如下：
>   **AVERROR(EAGAIN)**：表明编码器目前还没有准备好输出编码后的数据包，可能是因为编码器还在处理之前发送的帧，或者内部缓冲区还没有足够的数据来生成一个完整的数据包。此时，你需要继续调用 avcodec_send_frame 发送更多的帧，或者等待编码器处理完成。
>   **AVERROR_EOF**：意味着编码器已经完成了所有输入帧的编码，并且没有更多的数据包可以输出。通常在调用 avcodec_send_frame 传入 NULL 以刷新编码器之后会出现这个错误码。
>   **AVERROR(EINVAL)**：表示编码器上下文没有正确初始化，或者编码器不支持当前的操作。
>   其他负的错误码代表其他类型的错误。





### avcodec_find_encoder_by_name函数

在 FFmpeg 中，存在众多不同类型的编码器，用于处理各种音视频编码格式，如 H.264、H.265、AAC 等。avcodec_find_encoder_by_name 函数允许开发者通过编码器的名称，在系统中查找并获取对应的编码器对象，以便后续使用该编码器进行音视频编码操作。

#### 函数原型

```c
AVCodec *avcodec_find_encoder_by_name(const char *name);
```

> 参数：
>
> - `name`：这是一个指向以 null 结尾的字符串的指针，表示要查找的编码器的名称。常见的编码器名称包括 `"libx264"`（用于 H.264 编码）、`"libx265"`（用于 H.265 编码）、`"aac"`（用于 AAC 音频编码）等。
>
> 返回值：
>
> - 如果找到了与指定名称匹配的编码器，则返回一个指向 `AVCodec` 结构体的指针。`AVCodec` 结构体包含了编码器的各种信息和操作函数，后续可以使用该结构体来初始化编码器上下文并进行编码操作。
> - 如果未找到匹配的编码器，则返回 `NULL`。



### avcodec_alloc_context3函数

在 FFmpeg 里进行音视频编解码时，需要一个上下文来存储编解码器的各种参数和状态信息。`avcodec_alloc_context3` 函数的作用就是为指定的编解码器分配一个 `AVCodecContext` 结构体，并对其进行基本的初始化。这个上下文结构体包含了编解码器所需的各种参数，如编码格式、帧率、比特率等，后续可以通过设置这些参数来配置编解码器的行为。

#### 函数原型

```c
AVCodecContext *avcodec_alloc_context3(const AVCodec *codec);
```

> 参数：
>
> - `codec`：指向 `AVCodec` 结构体的指针。`AVCodec` 结构体代表一个具体的编解码器，通常通过 `avcodec_find_encoder`、`avcodec_find_decoder` 或 `avcodec_find_encoder_by_name` 等函数来获取。如果传入 `NULL`，则分配一个通用的编解码器上下文，后续需要手动指定具体的编解码器。
>
> 返回值：
>
> - **成功**：返回一个指向新分配并初始化的 `AVCodecContext` 结构体的指针。可以使用这个指针来访问和修改编解码器上下文的参数。
> - **失败**：返回 `NULL`，表示内存分配失败，可能是由于系统内存不足。



### **avcodec_open2函数**

在使用 FFmpeg 进行音视频编解码时，首先要找到合适的编解码器（如使用 avcodec_find_encoder 或 avcodec_find_decoder 函数），并为其分配上下文（使用 avcodec_alloc_context3 函数），接着设置上下文参数（如编码格式、帧率、比特率等）。而 avcodec_open2 函数的作用就是根据这些设置好的上下文参数，对编解码器进行初始化，使其能够正常处理音视频数据。

#### 函数原型

```c
int avcodec_open2(AVCodecContext *avctx, const AVCodec *codec, AVDictionary **options);
```

> 参数：
>
> - **`avctx`**：指向 `AVCodecContext` 结构体的指针。`AVCodecContext` 结构体包含了编解码器的上下文信息，存储着编解码所需的各种参数和状态，像编码格式、帧率、比特率等。在调用 `avcodec_open2` 之前，需要对这个结构体进行正确的初始化和参数设置。
> - **`codec`**：指向 `AVCodec` 结构体的指针。`AVCodec` 结构体代表一个具体的编解码器，通常通过 `avcodec_find_encoder`、`avcodec_find_decoder` 或 `avcodec_find_encoder_by_name` 等函数来获取。
> - **`options`**：指向 `AVDictionary` 指针的指针。`AVDictionary` 是一个键值对的集合，可用于传递一些额外的编解码选项，例如设置编码的质量、速度等。如果不需要传递额外选项，可以将其设为 `NULL`。在函数调用完成后，`options` 中未被使用的选项会保留下来，你可以根据需要进行处理。
>
> 返回值：
>
> - **成功**：返回值为 0，表示编解码器成功打开，可以开始进行编码或解码操作。
> - **失败**：返回一个负的错误码，常见错误码如下：
>   - `AVERROR(EINVAL)`：表示传入的参数无效，比如 `avctx` 或 `codec` 为 `NULL`，或者 `avctx` 中的某些参数设置不合法。
>   - `AVERROR(ENOMEM)`：表示内存分配失败，可能是由于系统内存不足，无法为编解码器分配所需的资源。
>   - 其他负的错误码代表其他类型的错误，具体含义可参考 FFmpeg 的错误码文档。



### av_frame_get_buffer函数

在 FFmpeg 中，AVFrame 结构体用于存储未压缩的音视频帧数据。当你创建一个 AVFrame 对象后，它本身并不包含实际存储音视频数据的内存空间，需要调用 av_frame_get_buffer 函数来为其分配合适大小的缓冲区。该函数会根据 AVFrame 中设置的格式（如像素格式、采样格式）、尺寸（如视频的宽度和高度、音频的声道数和采样率）等信息，自动计算所需的内存大小，并分配相应的缓冲区。

#### 函数原型

```c
int av_frame_get_buffer(AVFrame *frame, int align);
```

> 参数：
>
> - frame：指向 AVFrame 结构体的指针。在调用该函数之前，需要先对 AVFrame 结构体进行一些基本的设置，例如设置视频的宽度、高度、像素格式，或者音频的声道数、采样率、采样格式等。这些设置信息将用于确定所需缓冲区的大小和布局。
> - align：内存对齐的字节数。该参数指定了分配的内存缓冲区的起始地址需要按照多少字节进行对齐。通常可以将其设置为 0，此时函数会根据系统的默认规则进行内存对齐，以提高内存访问的效率。
>
> 返回值：
>
> - 成功： 返回值为 0，表示内存缓冲区分配成功。
> - 失败： 返回一个负的错误码，常见错误码如下：
>   - `AVERROR(ENOMEM)`：表示内存分配失败，可能是由于系统内存不足，无法为 `AVFrame` 分配所需的缓冲区。
>   - 其他负的错误码代表其他类型的错误，具体含义可参考 FFmpeg 的错误码文档。



### av_opt_set函数

FFmpeg 的 AVOptions 系统为许多对象（如编解码器上下文、滤镜等）提供了一种统一的方式来设置和获取属性。av_opt_set 函数允许开发者通过指定对象、属性名和属性值，来动态地配置这些对象的行为。这样可以方便地调整各种参数，以满足不同的应用需求。

```c
int av_opt_set(void *obj, const char *name, const char *val, int search_flags);
```

> 参数：
>
> - **`obj`**：指向要设置属性的对象的指针。这个对象必须是支持 AVOptions 系统的，例如 `AVCodecContext`、`AVFilterContext` 等。
> - **`name`**：要设置的属性的名称，是一个以 null 结尾的字符串。属性名是区分大小写的，需要根据具体对象的文档来确定正确的属性名。
> - **`val`**：要设置的属性值，同样是一个以 null 结尾的字符串。根据属性的类型不同，字符串的格式也有所不同。例如，对于整数类型的属性，字符串可以是一个十进制数字；对于布尔类型的属性，字符串可以是 `"0"`（表示 false）或 `"1"`（表示 true）等。
> - **`search_flags`**：搜索标志，用于指定在查找属性时的搜索范围和行为。**通常可以设置为 0 表示使用默认的搜索方式**。常见的标志有 `AV_OPT_SEARCH_CHILDREN`，表示在对象的子对象中也进行搜索。
>
> 返回值：
>
> - **成功**：返回 0，表示属性设置成功。
> - **失败**：返回一个负的错误码，常见错误码如下：
>   - `AVERROR_OPTION_NOT_FOUND`：表示指定的属性名未找到。
>   - `AVERROR_INVALIDDATA`：表示提供的属性值格式不正确，无法转换为属性所需的类型。
>   - 其他负的错误码代表其他类型的错误，具体含义可参考 FFmpeg 的错误码文档。



### **代码实战**

```c++
//#include <QCoreApplication>

extern "C"
{
#include <libavformat/avformat.h>
#include <libavcodec/avcodec.h>
#include <libavutil/opt.h>
}


static void encode(AVCodecContext * encCtx, AVFrame * frame, AVPacket *pkt, FILE * outFile)
{
    printf("===== encode =====\n");
    int ret;
    if(frame) {
        printf("Send frame %lld\n", frame->pts);
    }

    //获取一个原始数据包frame后，交给编码器进行编码
    ret = avcodec_send_frame(encCtx, frame);
    if(ret < 0) {
        fprintf(stderr, "Error sending a frame for encoding\n");
        exit(1);
    }
    //1个原始数据包可能会对应多个编码包,这里才使用了while循环
    while(ret >= 0) {
        //将压缩编码后的数据放入AVPacket中进行保存
        ret = avcodec_receive_packet(encCtx, pkt);
        if(ret == AVERROR(EAGAIN) || ret == AVERROR_EOF)
            return;
        else if(ret < 0) {
            fprintf(stderr, "Error during encoding.\n");
            exit(1);
        }

        //写入文件进行保存
        printf("Write packet %lld"" (size=%5d)\n", pkt->pts, pkt->size);
        fwrite(pkt->data, 1, pkt->size, outFile);

        av_packet_unref(pkt);//释放packet中的数据
    }
}


int main(int argc, char *argv[])
{
    //QCoreApplication a(argc, argv);
    //return a.exec();

    AVCodecContext * codecCtx = nullptr;
    const  AVCodec * codec = nullptr;

    uint8_t endcode[] = {0,0,1,0xb7};

    const char * codecName = "libx264";//可以更换为mpeg1video
    //获取编码器
    codec = avcodec_find_encoder_by_name(codecName);
    if(!codec) {
        fprintf(stderr, "Codec '%s' not found\n", codecName);
        exit(1);
    }

    //分配AVPacket的空间
    AVPacket* pkt = av_packet_alloc();
    if(!pkt) {
        exit(1);
    }

    //分配AVCodecContext空间
    codecCtx = avcodec_alloc_context3(codec);
    if(!codecCtx) {
        fprintf(stderr, "Could not allocate AVCodecCtx\n");
        exit(1);
    }
    //填充AVCodecContext的字段
    codecCtx->bit_rate = 400000;
    codecCtx->width = 640;
    codecCtx->height = 480;
    codecCtx->time_base = (AVRational){1, 25};
    codecCtx->framerate = (AVRational){25, 1};
    codecCtx->gop_size = 10;//每10帧就会有一个关键帧
    codecCtx->max_b_frames = 1;
    codecCtx->pix_fmt = AV_PIX_FMT_YUV420P;

    if(codec->id == AV_CODEC_ID_H264)
        av_opt_set(codecCtx->priv_data, "preset", "slow", 0);

    // 打开编码器
    int ret = avcodec_open2(codecCtx, codec, nullptr);
    if(ret < 0) {
        fprintf(stderr, "Could not open codec\n");
        exit(1);
    }

    const char * outFileName = "testencode.mp4";
    FILE * fp = fopen(outFileName, "wb");
    if(!fp) {
        fprintf(stderr, "Could not open %s\n", outFileName);
        exit(1);
    }

    //分配一个AVFrame的空间
    AVFrame * frame = av_frame_alloc();
    if(!frame) {
        fprintf(stderr, "Could not allocate AVFrame\n");
        exit(1);
    }
    frame->format = codecCtx->pix_fmt;
    frame->width = codecCtx->width;
    frame->height = codecCtx->height;

    //为AVFrame分配缓冲区
    ret = av_frame_get_buffer(frame, 0);
    if(ret < 0) {
        fprintf(stderr, "Could not allocate buffer of AVFrame\n");
        exit(1);
    }

    int i, x, y;
    /* encode 1 second of video */
    for (i = 0; i < 25; i++) {
        fflush(stdout);

        /* make sure the frame data is writable */
        ret = av_frame_make_writable(frame);
        if (ret < 0)
            exit(1);

        /* prepare a dummy image */
        /* Y */
        for (y = 0; y < codecCtx->height; y++) {
            for (x = 0; x < codecCtx->width; x++) {
                frame->data[0][y * frame->linesize[0] + x] = x + y + i * 3;
            }
        }

        /* Cb and Cr */
        for (y = 0; y < codecCtx->height/2; y++) {
            for (x = 0; x < codecCtx->width/2; x++) {
                frame->data[1][y * frame->linesize[1] + x] = 128 + y + i * 2;
                frame->data[2][y * frame->linesize[2] + x] = 64 + x + i * 5;
            }
        }

        frame->pts = i;

        /* encode the image */
        encode(codecCtx, frame, pkt, fp);
    }

    //刷新编码器
    encode(codecCtx, nullptr, pkt, fp);

    if(codec->id == AV_CODEC_ID_MPEG1VIDEO ||
       codec->id == AV_CODEC_ID_MPEG2VIDEO) {
        printf(" ==== write end code\n");
        fwrite(endcode, 1, sizeof(endcode), fp);
    }
    fclose(fp);
    avcodec_free_context(&codecCtx);
    av_frame_free(&frame);
    av_packet_free(&pkt);
    printf("===encode test end ===\n");

    return 0;
}

```

输出结果：

![image-20250427170403970](.\assets\image-20250427170403970.png)



关于编码的两个API涉及到编码的核心操作，**要注意**：

1） 按 pts 递增的顺序向编码器送入原始帧 frame，编码器按 dts 递增的顺序输出编码帧packet，实际上编码器关注输入 frame 的 pts 不关注其 dts，它只管依次处理收到的 frame，按需缓冲和编码

2） avcodec_receive_packet() 输出 packet 时，会设置 packet.dts，从 0 开始，每次输出的packet 的 dts 加 1，这是视频层的 dts，用户写输出前应将其转换为容器层的 dts

3） avcodec_receive_packet() 输出 packet 时，packet.pts 拷贝自对应的 frame.pts，这是视频层的 pts，用户写输出前应将其转换为容器层的 pts

4）avcodec_send_frame() 发送 NULL指针的 frame 时，编码器进入 flush 模式；avcodec_send_frame() 发送第一个 NULL 会 返 回 成功， 后续的NULL 会 返 回AVERROR_EOF

5）avcodec_send_frame() 多次发送 NULL 并不会导致编码器中缓存的帧丢失，使用avcodec_flush_buffers() 可 以 立 即 丢 掉 编 码 器 中 缓 存 帧 。 因 此 编 码 完 毕 时 应 使 用avcodec_send_frame(NULL) 来 取 完 缓 存 的 帧 ， 而 SEEK 操 作 或切换流时应调 用
avcodec_flush_buffers() 来直接丢弃缓存帧

6）编码器通常的冲洗方法：调用一次 avcodec_send_frame(NULL)(返回成功)，然后不停调用 avcodec_receive_packet() 直 到 其 返 回 AVERROR_EOF ， 取 出 所 有 缓 存 帧 ，avcodec_receive_packet() 返回 AVERROR_EOF 这一次是没有有效数据的，仅仅获取到一个结束标志

7）对 音 频来说 ， 如 果 AV_CODEC_CAP_VARIABLE_FRAME_SIZE(在AVCodecContext.codec.capabilities 变量中，只读)标志有效，表示编码器支持可变尺寸音频帧，送入编码器的音频帧可以包含任意数量的采样点。如果此标志无效，则每一个音频帧的采样点数目(frame->nb_samples)必须等于编码器设定的音频帧尺寸(avctx->frame_size)，最后一帧除外，最后一帧音频帧采样点数可以小于 avctx->frame_size



### **AVCodecContext结构体[5]**

```c
typedef struct AVCodecContext {
    enum AVMediaType codec_type;
    const struct AVCodec * codec;
    enum AVCodecID codec_id;
    void * priv_data;
    int64_t bit_rate;
    AVRational time_base;
    int width, height;
    int gop_size;
    enum AVPixelFormat pix_fmt;
    int max_b_frames;
    int sample_rate;//音频采样率
	int channels;//音频声道数
};
```



#### gop_size字段含义

> AVCodecContext中的**gop_size** 代表的是 “Group of Pictures”（画面组）的大小，也就是在视频编码序列里，两个相邻的关键帧（I 帧）之间所包含的帧数。一个画面组（GOP）由一个关键帧（I 帧）以及若干个预测帧（P 帧和 B 帧）构成。
>
> 
>
> **关键帧（I 帧）**：它是一种独立的帧，不需要参考其他帧就能进行解码。I 帧可以看作是一个完整的图像，解码时从 I 帧开始可以独立恢复出画面。
> **预测帧（P 帧和 B 帧）**：P 帧是前向预测帧，它参考前面的 I 帧或 P 帧来进行解码；B 帧是双向预测帧，它参考前后的 I 帧或 P 帧来进行解码。
> gop_size 的作用：合理设置 gop_size 能够在视频质量和编码效率之间找到平衡。**较小的 gop_size 意味着有更多的关键帧，这样在视频出现丢帧或者随机访问时，能更快地恢复画面，但会增加视频的比特率；较大的 gop_size 可以减少关键帧的数量，降低比特率，但会使视频在丢帧或随机访问时的恢复能力变差。**



#### max_b_frames字段含义

> AVCodecContext中的**max_b_frames**表示在两个非 B 帧（关键帧 I 帧或者预测帧 P 帧）之间允许插入的最大双向预测帧（B 帧）数量。B 帧是双向预测帧，它在解码时会参考前后的 I 帧或者 P 帧来重构画面。其作用是：
>
> - **编码效率提升**：B 帧通过参考前后帧的信息进行编码，能够更有效地去除视频序列中的时间冗余，从而显著提高编码效率，降低视频的比特率，减小视频文件的大小。
> - **画面质量优化**：合理使用 B 帧可以更精准地预测和重构画面，减少画面中的噪声和失真，进而提升视频的画面质量。
>
> ### 设置影响
>
> - `max_b_frames` 值较大
>   - **优点**：能更充分地利用视频序列中的时间相关性，进一步提高编码效率，减少视频的比特率。因为更多的 B 帧可以更精细地描述画面的变化，使得编码器能用更少的数据来表示相同的画面内容。
>   - **缺点**：会增加解码的延迟。这是由于 B 帧需要参考前后的帧，解码器在解码 B 帧之前，需要先解码其前后的参考帧，所以会导致解码过程的延迟增加。此外，在网络传输过程中，如果出现丢帧情况，由于 B 帧对前后帧的依赖性较强，可能会影响后续帧的解码，降低视频的容错性。
> - `max_b_frames` 值较小
>   - **优点**：可以减少解码的延迟，提高视频的实时性。同时，在网络不稳定或者丢帧较多的情况下，较小的 `max_b_frames` 能降低 B 帧丢帧对解码的影响，增强视频的容错性。
>   - **缺点**：编码效率会有所降低，视频的比特率会相对较高。因为较少的 B 帧意味着编码器不能充分利用视频序列中的时间相关性，需要用更多的数据来表示画面内容。



#### priv_data字段含义

> `priv_data` 是 `AVCodecContext` 结构体中的一个成员，它指向编解码器的私有数据。不同的编解码器可能会有不同的私有数据结构，这些私有数据包含了该编解码器特有的配置信息和状态。
>
> **av_opt_set(codecCtx->priv_data, "preset", "slow", 0);**
>
> 在视频编码中，特别是使用像 H.264、H.265 这类编码器时，**preset** 是一个常见的属性，它代表编码预设。编码预设定义了一组编码参数的组合，用于平衡编码速度和编码质量。
>
> 对于 preset 属性来说，"**slow**" 是一种预设选项。通常，"**slow**" 预设会花费更多的时间进行编码，但能生成更高质量、更低比特率的视频。与之相对的，还有 "ultrafast"、"fast" 等预设，它们编码速度快，但视频质量可能会稍差一些。





## 解码操作实战（项目强相关）

在后续的实战中，对MP4文件解码后，最终得到的是视频数据yuv和音频数据pcm。

### avcodec_find_decoder函数

在进行音视频解码操作时，需要先找到合适的解码器。`avcodec_find_decoder` 函数就是为了实现这一目的而设计的。它会在 FFmpeg 所支持的编解码器列表中查找与指定编解码器 ID 相匹配的解码器，若找到则返回该解码器的指针，后续就可以利用这个解码器来进行音视频数据的解码工作。

#### 函数原型

```c
AVCodec *avcodec_find_decoder(enum AVCodecID id);
```

> 参数：
>
> - `id`：这是一个 `AVCodecID` 枚举类型的参数，代表要查找的编解码器的 ID。`AVCodecID` 枚举定义了 FFmpeg 支持的各种编解码器的唯一标识符，常见的编解码器 ID 包括 `AV_CODEC_ID_H264`（H.264 视频解码器）、`AV_CODEC_ID_AAC`（AAC 音频解码器）等。
>
> 返回值：
>
> - **成功**：若在 FFmpeg 支持的编解码器列表中找到了与指定 ID 匹配的解码器，函数会返回一个指向 `AVCodec` 结构体的指针。`AVCodec` 结构体包含了解码器的各种信息和操作函数，后续可以使用该结构体来初始化解码器上下文并进行解码操作。
> - **失败**：若未找到匹配的解码器，函数将返回 `NULL`。



### avcodec_parameters_to_context函数

在处理音视频流时，AVCodecParameters 结构体存储了音视频流的编解码器相关参数，例如视频的分辨率、像素格式，音频的采样率、声道数等。而 AVCodecContext 是编解码器的上下文，在进行编解码操作时需要使用这些参数来配置编解码器。avcodec_parameters_to_context 函数的作用就是将 AVCodecParameters 中的参数复制到 AVCodecContext 中，这样可以方便快捷地完成编解码器上下文的参数设置。

#### 函数原型

```c
int avcodec_parameters_to_context(AVCodecContext *codec_context, const AVCodecParameters *par);
```

> 参数：
>
> - **`codec_context`**：指向 `AVCodecContext` 结构体的指针，该结构体用于存储编解码器的上下文信息，后续的编解码操作会基于这些信息进行。
> - **`par`**：指向 `AVCodecParameters` 结构体的指针，该结构体包含了音视频流的编解码器参数。
>
> 返回值：
>
> - **成功**：返回 0，表示参数复制成功，`AVCodecContext` 中的参数已更新为 `AVCodecParameters` 中的值。
> - **失败**：返回一个负的错误码，常见错误码如 `AVERROR(EINVAL)`，表示传入的参数无效，例如 `codec_context` 或 `par` 为 `NULL`。

### **avcodec_send_packet函数**

avcodec_send_packet 是 FFmpeg 库中用于视频 / 音频**解码**的核心函数之一，属于新解码 API（FFmpeg 4.0+）。它与 avcodec_receive_frame 配合使用，替代了旧的 avcodec_decode_video2/avcodec_decode_audio4 函数，提供更灵活的异步解码机制。

#### 函数原型

```c
int avcodec_send_packet(AVCodecContext *avctx, const AVPacket *avpkt);
```

> 参数：
>
> - **`AVCodecContext *avctx`**: 编解码器上下文，需提前通过 `avcodec_open2()` 初始化。
>
> - **`const AVPacket *avpkt`**: 输入的压缩数据包（如 H.264 视频帧、AAC 音频帧）。
>   - 特殊值
>     - 传入 `NULL` 表示刷新解码器（读取所有缓冲帧）。
>     - 传入空包（`avpkt->size = 0`）等价于传入 `NULL`。
>
> 返回值：
>
> - **0**：成功将数据包送入解码器。
> - 负数错误码：
>   - `AVERROR(EAGAIN)`：解码器缓冲区已满，需先调用 `avcodec_receive_frame()` 读取帧。
>   - `AVERROR_EOF`：解码器已关闭，无法再接收数据。
>   - `AVERROR(EINVAL)`：解码器未打开、不支持异步解码或参数无效。
>   - 其他错误（如内存不足、数据格式不支持）。

#### 核心机制

> 1. **异步解码**：
>    - `avcodec_send_packet()` 仅将数据包送入解码器队列，不保证立即解码。
>    - 需循环调用 `avcodec_receive_frame()` 获取解码后的帧。
> 2. **缓冲机制**：
>    - 解码器可能缓存多个数据包以处理 B 帧或依赖关系（如 H.264 的参考帧）。
>    - 刷新时（传入 `NULL`），解码器会输出所有缓冲的剩余帧。

#### **关键注意事项**

>1. **数据包顺序**：
>   - 必须按解码顺序（DTS）发送数据包，否则可能导致解码错误。
>2. **资源管理**：
>   - `avcodec_send_packet()` 成功后，可立即释放 `AVPacket`（数据已复制）。
>   - `AVFrame` 需在使用后通过 `av_frame_unref()` 释放。
>3. **部分帧处理**：
>   - 若数据包不完整（如网络流中断），需调用 `avcodec_flush_buffers()` 重置解码器。
>4. **多线程支持**：
>   - 新 API 更适合多线程解码（如一个线程发送，多个线程接收）。



### **avcodec_receive_frame 函数**

`avcodec_receive_frame` 是 FFmpeg 中用于视频 / 音频**解码**的核心函数，与 `avcodec_send_packet` 配合构成新的异步解码 API。该函数从解码器中获取已解码的原始帧（如视频的 YUV 帧或音频的 PCM 数据），替代了旧版的 `avcodec_decode_video2`/`avcodec_decode_audio4`。

#### 函数原型

```c
int avcodec_receive_frame(AVCodecContext *avctx, AVFrame *frame);
```

> 参数：
>
> - **`AVCodecContext *avctx`**：编解码器上下文，需提前通过 `avcodec_open2()` 初始化。
>
> - **`AVFrame *frame`**
>
>   - 输出参数，用于存储解码后的原始帧数据。
>
>   - 使用前需通过 `av_frame_alloc()` 分配，使用后通过 `av_frame_unref()` 释放。
>
> 返回值：
>
> - **0**：成功解码并填充一帧数据到 `frame`。
> - 负数错误码：
>   - `AVERROR(EAGAIN)`：当前没有可获取的帧，需继续发送数据包（`avcodec_send_packet`）。
>   - `AVERROR_EOF`：解码已完成（已发送 `NULL` 包且所有缓冲帧已输出）。
>   - `AVERROR(EINVAL)`：解码器未打开或不支持异步解码。
>   - 其他错误（如内存不足、格式不匹配）。

#### 核心机制

> 1. **异步解码流程**：
>    - 先调用 `avcodec_send_packet()` 将压缩数据包送入解码器。
>    - 循环调用 `avcodec_receive_frame()` 直到返回 `AVERROR(EAGAIN)` 或错误。
> 2. **缓冲与延迟**：
>    - 解码器可能缓存多个数据包以处理 B 帧或依赖关系（如 H.264 的参考帧）。
>    - 需通过发送 `NULL` 包（刷新操作）获取所有剩余帧。
> 3. **多帧输出**：
>    - 一个数据包可能产生多帧输出（如音频的多个采样帧）。

#### 关键注意事项

> 1. **显式缓冲区管理**：
>    - 通过 `EAGAIN` 错误提示解码器缓冲区状态，便于控制内存使用。
> 2. **刷新机制**：
>    - 发送 `NULL` 包后，持续调用 `avcodec_receive_frame()` 直到返回 `AVERROR_EOF`，确保所有缓冲帧被输出。
> 3. **错误恢复**：
>    - 若解码出错，可继续发送后续数据包（无需重置上下文）。



### av_image_alloc函数

av_image_alloc 是 FFmpeg 库中用于分配**图像数据存储空间**的函数。在音视频处理中，常常需要处理图像帧，而该函数能够为图像数据分配连续的内存空间，并设置好图像的相关参数，如各平面的指针、每行的字节数等。这对于后续的图像编码、解码、滤波等操作非常关键。该函数位于 **libavutil/imgutils.h** 头文件中，使用前需要包含此头文件。

#### 函数原型

```c
int av_image_alloc(uint8_t *pointers[4], int linesizes[4],
                   int w, int h, enum AVPixelFormat pix_fmt, int align);
```

> 参数：
>
> - pointers[4]: 这是一个指针数组，**用于存储图像各平面数据的起始地址**。不同的像素格式可能有不同数量的平面，例如 **YUV420P 格式有 3 个平面（Y、U、V）**，**RGB24 格式通常只有 1 个平面**。函数执行后，pointers 数组会被填充为分配好的各平面内存的起始地址。
> - linesizes[4]: **该数组用于存储图像各平面每行数据的字节数**。每行的字节数可能会因为对齐等因素而有所不同。函数执行后，linesizes 数组会被填充为各平面每行的实际字节数。
> - w: 表示图像的宽度，以像素为单位。
> - h: 表示图像的高度，以像素为单位。
> - pix_fmt: **指定图像的像素格式**，如 AV_PIX_FMT_YUV420P、AV_PIX_FMT_RGB24 等。不同的像素格式决定了图像数据的存储方式和占用空间。
> - align: **用于指定内存对齐的字节数**。内存对齐可以提高内存访问效率，通常设置为 1、2、4、8 等 2 的幂次方。
>
> 返回值：
>
> - 成功：返回**分配的图像数据总字节数**。
>
> - 失败：返回一个负的错误码，例如 AVERROR(ENOMEM) 表示内存分配失败。



### av_frame_alloc函数

在 FFmpeg 里，`AVFrame` 结构体用于存储未压缩的音视频帧数据，像视频的像素数据、音频的采样数据等。`av_frame_alloc` 函数的主要作用是为 `AVFrame` 结构体分配内存空间，并且对其进行基本的初始化，这样后续就可以利用这个 `AVFrame` 来存储和处理音视频帧数据。

#### 函数原型

```c
AVFrame *av_frame_alloc(void);
```

> 返回值：
>
> - **成功**：返回一个指向新分配并初始化的 `AVFrame` 结构体的指针。可以使用这个指针来访问和操作 `AVFrame` 结构体中的各个成员。
> - **失败**：返回 `NULL`，这表明内存分配失败，可能是由于系统内存不足。

 

### **AFrame结构体[6]**

> ```c
> typedef struct AVFrame {
> #define AV_NUM_DATA_POINTERS 8
>         uint8_t *data[AV_NUM_DATA_POINTERS];
>         int linesize[AV_NUM_DATA_POINTERS];
>         uint8_t **extended_data;
>         int width, height;
>         int nb_samples;
>         int format;
>         int key_frame;
>         enum AVPictureType pict_type;
>         AVRational sample_aspect_ratio;
>         int64_t pts;
>     
>         attribute_deprecated
>         int64_t pkt_pts;
>     
>         int64_t pkt_dts;
>         int coded_picture_number;
>         int display_picture_number;
>         int quality;
>         void *opaque;
>         int repeat_pict;
>         int interlaced_frame;
>         int top_field_first;
>         int palette_has_changed;
>     
>         int channels;
>     
>         size_t crop_top;
>         size_t crop_bottom;
>         size_t crop_left;
>         size_t crop_right;
>     
>         AVDictionary *metadata;
> };
> ```

#### **数据存储字段**

> 1. **`uint8_t *data[AV_NUM_DATA_POINTERS]`**
>    - 指向原始媒体数据的指针数组。
>      - **视频**：通常包含多个平面（如 YUV 格式的 Y、U、V 平面）。
> 2. **`int linesize[AV_NUM_DATA_POINTERS]`**
>    - 每个平面的行字节数（即一行数据的宽度，可能大于实际图像宽度）。
>    - 用于处理内存对齐问题。
> 3. **`AVBufferRef *buf[AV_NUM_DATA_POINTERS]`**
>    - 引用计数的内存缓冲区，管理 `data` 的内存，支持零拷贝数据共享。
> 4. **extended_data**
>    - 存放音频数据
>    - planar格式，每个声道独立存放
>    - packed格式，声道交错存放

#### **时间戳与持续时间**

>1. **`int64_t pts`**
>  - **显示时间戳**（Presentation Time Stamp），单位由流的 `time_base` 定义。
>  - 表示该帧应被显示的时间点。
>2. **`int64_t pkt_dts`**
>  - 源自输入数据包（AVPacket）的解码时间戳（DTS），用于追溯帧的解码顺序。
>3. **`int repeat_pict`**
>  - 视频帧重复标志：
>    - `0`：不重复；
>    - `1`：重复一次（相当于帧持续时间加倍）。
>  - 用于控制帧率（如 PAL 制电视的隔行扫描）。
>
>4. **`int64_t best_effort_timestamp`**
>  - 最佳估计的显示时间戳，在没有明确 `pts` 时使用。

#### **视频专用字段**

>1. **`int width, height`**
>   - 视频帧的宽度和高度（像素）。
>2. **`enum AVPixelFormat format`**
>   - 像素格式（如 `AV_PIX_FMT_YUV420P`、`AV_PIX_FMT_RGB24`）。
>3. **`int key_frame`**
>   - 是否为关键帧（I 帧）：`1` 是，`0` 否。   补偿帧 ： B帧、P帧
>4. **`enum AVPictureType pict_type`**
>   - 帧类型（如 `AV_PICTURE_TYPE_I`、`AV_PICTURE_TYPE_P`、`AV_PICTURE_TYPE_B`）。
>5. **`AVRational sample_aspect_ratio`**
>   - 像素宽高比（如 `{16, 9}` 表示宽高比为 16:9）。

#### **音频专用字段**

>1. **`int nb_samples`**
>   - 音频帧中的样本数（如每个声道的采样点数）。
>2. **`enum AVSampleFormat format`**
>   - 音频样本格式（如 `AV_SAMPLE_FMT_S16`、`AV_SAMPLE_FMT_FLTP`）。
>3. **`int channels`**
>   - 声道数（如立体声为 2）。
>4. **`uint64_t channel_layout`**
>   - 声道布局（如 `AV_CH_LAYOUT_STEREO`、`AV_CH_LAYOUT_5POINT1`）。
>5. **`int sample_rate`**
>   - 采样率（如 44100 Hz）。
>
>

#### **其他元数据**

> 1. **`AVDictionary *metadata`**
>    - 帧级元数据（如字幕文本、场景信息）。
> 2. **`int flags`**
>    - 标志位：
>      - `AV_FRAME_FLAG_KEY`：关键帧标志；
>      - `AV_FRAME_FLAG_CORRUPT`：帧数据可能已损坏。
> 3. **`AVRational time_base`**
>    - 时间戳的时间基（单位：秒），用于将 `pts` 转换为实际时间。

#### **内部与高级字段**

> 1. **`int crop_top/bottom/left/right`**
>    - 用于视频裁剪的边界像素数。
> 2. **`int interlaced_frame`**
>    - 是否为隔行扫描帧：`1` 是，`0` 否。
> 3. **`int top_field_first`**
>    - 隔行扫描时，顶场是否优先：`1` 是，`0` 否。
> 4. **`void *opaque`**
>    - 保留给应用程序使用的指针。

### av_image_copy函数

`av_image_copy` 函数的主要作用是将一个图像的数据从源缓冲区复制到目标缓冲区。在视频处理过程中，经常需要对视频帧进行复制操作，比如在视频编辑、滤镜处理等场景下，可能需要备份原始帧数据或者将处理后的帧数据复制到新的缓冲区中。该函数可以方便地处理不同像素格式和尺寸的图像数据复制。

#### 函数原型

```c
void av_image_copy(uint8_t *dst_data[4], int dst_linesize[4],
                   const uint8_t *src_data[4], const int src_linesize[4],
                   enum AVPixelFormat pix_fmt, int width, int height);
```

> 参数：
>
> - **`dst_data`**：目标图像数据的缓冲区指针数组。对于不同的像素格式，图像数据可能会存储在多个平面中，因此使用数组来表示。数组的每个元素指向一个数据平面的起始地址。
> - **`dst_linesize`**：目标图像每行数据的字节数数组。同样，由于可能存在多个数据平面，使用数组来表示每个平面每行数据的字节数。这个值表示在内存中每行数据的实际跨度，可能会因为内存对齐等原因而大于图像的实际宽度。
> - **`src_data`**：源图像数据的缓冲区指针数组，与 `dst_data` 类似，用于指定源图像数据的存储位置。
> - **`src_linesize`**：源图像每行数据的字节数数组，与 `dst_linesize` 类似，用于指定源图像每行数据的实际跨度。
> - **`pix_fmt`**：图像的像素格式，是 `AVPixelFormat` 枚举类型的值。常见的像素格式有 `AV_PIX_FMT_YUV420P`、`AV_PIX_FMT_RGB24` 等，该参数用于告诉函数图像数据的存储格式。
> - **`width`**：图像的宽度，以像素为单位。
> - **`height`**：图像的高度，以像素为单位。
>
> 返回值：
>
> - 该函数没有返回值，因为它只是执行图像数据的复制操作，不涉及返回结果的判断。



### av_sample_fmt_is_planar函数

在音频处理中，采样格式有两种主要类型：打包（packed）格式和平面（planar）格式。打包格式是指所有声道的数据交错存储在一个缓冲区中；而平面格式是指每个声道的数据分别存储在不同的缓冲区中。av_sample_fmt_is_planar 函数的作用就是帮助开发者判断给定的音频采样格式属于哪种类型，以便在处理音频数据时采取正确的操作。

#### 函数原型

```c
int av_sample_fmt_is_planar(enum AVSampleFormat sample_fmt);
```

> 参数：
>
> - sample_fmt：这是一个 AVSampleFormat 枚举类型的参数，表示要判断的音频采样格式。AVSampleFormat 枚举定义了 FFmpeg 支持的各种音频采样格式，例如 AV_SAMPLE_FMT_S16（有符号 16 位整数采样格式）、AV_SAMPLE_FMT_FLTP（平面浮点采样格式）等。
>
> 返回值：
>
> - **1**：表示指定的采样格式是平面格式，即每个声道的数据分别存储在不同的缓冲区中。
> - **0**：表示指定的采样格式是打包格式，即所有声道的数据交错存储在一个缓冲区中。



### **实战代码**

```c++
extern "C"
{
#include <libavformat/avformat.h>
#include <libavcodec/avcodec.h>
#include <libavutil/opt.h>
#include <libavutil/imgutils.h>
}

FILE * outVideoFp = nullptr, * outAudioFp = nullptr;

static uint8_t *video_dst_data[4] = {NULL};
static int      video_dst_linesize[4];
static int video_dst_bufsize;
static int video_frame_count = 0;
static int audio_frame_count = 0;

AVPixelFormat pix_fmt;

static int openCodecContext(int *stream_idx,
                              AVCodecContext **dec_ctx,
                              AVFormatContext *fmt_ctx,
                              enum AVMediaType type);
static int decodePacket(AVCodecContext *dec, const AVPacket *pkt);
static int outputVideoFrame(AVFrame *frame);
static int outputAudioFrame(AVFrame *frame);
static int get_format_from_sample_fmt(const char **fmt,
                                      enum AVSampleFormat sample_fmt);

int main()
{
    int ret = 0;

    const char * srcFilename = "D:\\videos\\lldq-test480.mp4";
    const char * videoDstFilename = "lldq.yuv";
    const char * audioDstFilename = "lldq.pcm";

    outVideoFp = fopen(videoDstFilename, "wb");
    if(!outVideoFp) {
        fprintf(stderr, "fopen file %s error\n", videoDstFilename);
        return EXIT_FAILURE;
    }

    outAudioFp = fopen(audioDstFilename, "wb");
    if(!outAudioFp) {
        fprintf(stderr, "fopen file %s error\n", audioDstFilename);
        return EXIT_FAILURE;
    }

    AVFormatContext  * inFmtCtx = nullptr;
    ret = avformat_open_input(&inFmtCtx, srcFilename, nullptr, nullptr);
    if(ret < 0) {
        fprintf(stderr, "Could not open source file %s\n", srcFilename);
        exit(1);
    }

    ret = avformat_find_stream_info(inFmtCtx, nullptr);
    if(ret < 0) {
        fprintf(stderr, "Could not find stream information.\n");
        exit(1);
    }

    int videoStreamIdx, audioStreamIdx;
    AVCodecContext * videoCodecCtx = nullptr, *audioCodecCtx = nullptr;
    AVStream * videoStream, * audioStream;
    //打开视频解码器
    if(openCodecContext(&videoStreamIdx, &videoCodecCtx, inFmtCtx, AVMEDIA_TYPE_VIDEO) >=0) {
        videoStream = inFmtCtx->streams[videoStreamIdx];

        int width = videoCodecCtx->width;
        int height = videoCodecCtx->height;
        pix_fmt = videoCodecCtx->pix_fmt;

        ret = av_image_alloc(video_dst_data, video_dst_linesize,
                             width, height, pix_fmt, 1);
        if(ret < 0) {
            fprintf(stderr, "Could not allocate raw video buffer\n");
            return EXIT_FAILURE;
        }
        video_dst_bufsize = ret;
    }

    //打开音频解码器
    if(openCodecContext(&audioStreamIdx, &audioCodecCtx, inFmtCtx, AVMEDIA_TYPE_AUDIO) >= 0) {
        audioStream = inFmtCtx->streams[audioStreamIdx];
    }
    // 打印输入流媒体信息
    av_dump_format(inFmtCtx, -1, srcFilename, 0);

    if(!videoStream && !audioStream) {
        fprintf(stderr, "Could not find audio or video stream in the input.\n");
        exit(1);
    }

    //分配AVFrame的空间
    AVFrame * frame = av_frame_alloc();
    if(!frame) {
        fprintf(stderr, "Could not allocate AVFrame\n");
        exit(1);
    }
    //初始化AVPacket
    AVPacket * pkt = av_packet_alloc();
    if(!pkt) {
        fprintf(stderr, "Could not allocate AVPacket\n");
        exit(1);
    }
    int count = 0;

    while(av_read_frame(inFmtCtx, pkt) >= 0) {
        printf("count: %d\n", ++count);
        if(pkt->stream_index == videoStreamIdx)
            ret = decodePacket(videoCodecCtx, pkt);//解码视频
        else if(pkt->stream_index == audioStreamIdx)
            ret = decodePacket(audioCodecCtx, pkt);//解码音频

        av_packet_unref(pkt);
        if(ret < 0)
            break;
    }

    //刷新解码器
    if(videoCodecCtx)
    {    decodePacket(videoCodecCtx, nullptr);  }
    if(audioCodecCtx)
    {    decodePacket(audioCodecCtx, nullptr);  }

    printf("====== Demuxing succeeded. ======\n");

    if(videoStream) {
        printf(">> Play the output video file with the command:\n"
               "ffplay -f rawvideo -pix_fmt %s -video_size %dx%d %s\n",
               av_get_pix_fmt_name(pix_fmt),
               videoCodecCtx->width, videoCodecCtx->height, videoDstFilename);
    }

    if(audioStream) {
        enum AVSampleFormat sfmt = audioCodecCtx->sample_fmt;
        int nChannels = audioCodecCtx->channels;
        const char * fmt;

        //判断给定的音频采样格式属于哪种类型
        if(av_sample_fmt_is_planar(sfmt)) {
            const char * packed = av_get_sample_fmt_name(sfmt);
            printf("Warning: the sample format the decoder produced is planar "
                   "(%s). This example will output the first channel only.\n",
                   packed ? packed: "?");
            sfmt = av_get_packed_sample_fmt(sfmt);
            nChannels = 1;
        }

        ret = get_format_from_sample_fmt(&fmt, sfmt);
        if(ret < 0) {
            goto end;
        }

        printf(">>Play the output audio file with the command:\n"
               "ffplay -f %s -ac %d -ar %d %s\n",
               fmt, nChannels,
               audioCodecCtx->sample_rate,
               audioDstFilename);
    }

end:
    avcodec_free_context(&videoCodecCtx);
    avcodec_free_context(&audioCodecCtx);
    avformat_close_input(&inFmtCtx);
    av_frame_free(&frame);
    av_free(video_dst_data[0]);
    return 0;
}


static int openCodecContext(int *stream_idx,
                              AVCodecContext **dec_ctx,
                              AVFormatContext *fmt_ctx,
                              enum AVMediaType type)
{
    int ret, index;
    AVStream * st;
    AVCodec * dec = nullptr;
    AVDictionary * opts = nullptr;

    ret = av_find_best_stream(fmt_ctx, type, -1, -1, nullptr, 0);
    if(ret < 0) {
        fprintf(stderr, "Could not find %s stream\n",
                av_get_media_type_string(type));
        return ret;
    }

    index = *stream_idx = ret;
    st = fmt_ctx->streams[index];//找到对应的流

    //查找解码器
    dec = avcodec_find_decoder(st->codecpar->codec_id);
    if(!dec){
        fprintf(stderr, "Failed to find %s codec\n",
                av_get_media_type_string(type));
        return AVERROR(EINVAL);
    }

    //分配输出的AVCdoecContext空间
    *dec_ctx = avcodec_alloc_context3(dec);
    if(!*dec_ctx) {
        fprintf(stderr, "Failed to allocate the %s codec context\n",
                av_get_media_type_string(type));
        return AVERROR(ENOMEM);
    }

    //将流中的编解码参数拷贝到输出的AVCodecContext中
    ret = avcodec_parameters_to_context(*dec_ctx, st->codecpar);
    if(ret < 0) {
        fprintf(stderr, "Failed to copy %s codec parameters to decoder context\n",
                av_get_media_type_string(type));
        return ret;
    }

    //打开解码器
    ret = avcodec_open2(*dec_ctx, dec, &opts);
    if(ret < 0) {
        fprintf(stderr, "Failed to open %s codec\n",
                av_get_media_type_string(type));
        return ret;
    }

    return 0;
}

static int decodePacket(AVCodecContext *dec, const AVPacket *pkt)
{
    //将压缩编码的数据交给解码器
    int ret = avcodec_send_packet(dec, pkt);
    if(ret < 0) {
        fprintf(stderr, "Error ocurred when avcodec_send_packet\n");
        return ret;
    }

    AVFrame * frame = av_frame_alloc();
    if(!frame) {
        fprintf(stderr, "Could not allocate AVFrame\n");
        exit(1);
    }

    //从解码器中获取所有的每一帧数据
    while(ret >= 0) {
        ret = avcodec_receive_frame(dec, frame);
        if(ret < 0) {
            if(ret == AVERROR_EOF || ret == AVERROR(EAGAIN))
                return 0;
            fprintf(stderr, "Error during decoding\n");
            return ret;
        }

        //解码成功了,就将数据写入到输出文件
        if(dec->codec->type == AVMEDIA_TYPE_VIDEO) {
            ret = outputVideoFrame(frame);
        } else if(dec->codec->type == AVMEDIA_TYPE_AUDIO) {
            ret = outputAudioFrame(frame);
        }

        av_frame_unref(frame);
        if(ret < 0)
        {    return ret;    }
    }
    av_frame_free(&frame);
    return 0;
}

static int outputVideoFrame(AVFrame *frame)
{
    printf("video_frame n: %d coded_n:%d\n",
           video_frame_count++, frame->coded_picture_number);

    av_image_copy(video_dst_data, video_dst_linesize,
                  (const uint8_t**)frame->data, frame->linesize,
                  pix_fmt, frame->width, frame->height);

    fwrite(video_dst_data[0], 1, video_dst_bufsize, outVideoFp);
    return 0;
}


static int outputAudioFrame(AVFrame *frame)
{
    size_t unpadded_linesize = frame->nb_samples * av_get_bytes_per_sample((AVSampleFormat)frame->format);

    printf("audio frame n: %d, nb_samples:%d, pts:%lld\n",
           audio_frame_count++, frame->nb_samples, frame->pts);

    fwrite(frame->extended_data[0], 1, unpadded_linesize, outAudioFp);
    return 0;
}


static int get_format_from_sample_fmt(const char **fmt,
                                      enum AVSampleFormat sample_fmt)
{
    int i;
    struct sample_fmt_entry {
        enum AVSampleFormat sample_fmt; const char *fmt_be, *fmt_le;
    } sample_fmt_entries[] = {
        { AV_SAMPLE_FMT_U8,  "u8",    "u8"    },
        { AV_SAMPLE_FMT_S16, "s16be", "s16le" },
        { AV_SAMPLE_FMT_S32, "s32be", "s32le" },
        { AV_SAMPLE_FMT_FLT, "f32be", "f32le" },
        { AV_SAMPLE_FMT_DBL, "f64be", "f64le" },
    };
    *fmt = NULL;

    for (i = 0; i < FF_ARRAY_ELEMS(sample_fmt_entries); i++) {
        struct sample_fmt_entry *entry = &sample_fmt_entries[i];
        if (sample_fmt == entry->sample_fmt) {
            *fmt = AV_NE(entry->fmt_be, entry->fmt_le);
            return 0;
        }
    }

    fprintf(stderr,
            "sample format %s is not supported as output format\n",
            av_get_sample_fmt_name(sample_fmt));
    return -1;
}
```

输出结果：
![image-20250428165446859](.\assets\image-20250428165446859.png)





# 六、音视频同步播放



在 Windows 平台下搭配 `FFmpeg 4.4`时，`SDL 2.0` 系列的稳定版本（尤其是 `2.0.14` 至 `2.26.x` 之间的版本） 是最佳选择，其中最推荐 `SDL 2.0.20` 或 `SDL 2.26.5`。这两个版本与 `FFmpeg 4.4` 的兼容性、稳定性经过大量开发者验证，且能满足音视频同步播放的核心需求（如音频输出、视频渲染、事件循环）。



## 版本搭配的核心原则

`FFmpeg 4.4`（2021 年发布的长期稳定版）调用 `SDL` 时，依赖的是 **SDL 2.0 系列的标准 API**（如音频回调 `SDL_AudioSpec`、视频渲染 `SDL_Renderer`/`SDL_Texture`），而 SDL 2.0 系列的 API 自 2.0.0 以来未发生**破坏性变更**（即后续版本向下兼容）。因此，版本搭配的核心是：

1. **避免使用 SDL 1.2**：SDL 1.2 已停止维护（最后更新于 2012 年），缺少现代平台适配（如 Windows 10/11 优化），且 FFmpeg 4.4 对 SDL 1.2 的支持已弱化，可能出现音频延迟、视频花屏等问题。
2. **避免使用 SDL 3.0（开发中）**：SDL 3.0 目前处于预览阶段（截至 2024 年 5 月），API 尚未完全稳定，FFmpeg 4.4 未针对其适配，可能出现接口不兼容（如部分函数废弃）。
3. **优先选择 SDL 2.0 系列的 “中期稳定版”**：既避免早期 2.0 版本（如 2.0.0-2.0.13）的小 bug，也避免过于激进的新版本（如 2.30+）可能存在的适配问题。

### **首选：SDL 2.26.5**

- **2023 年发布**

- 核心优势
  - 修复了 SDL 2.0 系列的多个稳定性问题（如 Windows 下音频设备切换崩溃、高分辨率屏幕视频渲染错位）；
  - 对 Windows 11 的适配优化（如支持 Direct3D 12 渲染、高 DPI 自动缩放）；
  - 与 FFmpeg 4.4 的音频 / 视频数据交互无兼容性问题（如 FFmpeg 解码的 YUV 数据可直接通过 `SDL_UpdateTexture` 传给 SDL 渲染）。
- **适用场景**：Windows 10/11 平台、需要高稳定性的音视频播放器开发。

### 备选：SDL 2.0.20

- **2021 年发布**，与 FFmpeg 4.4 同期

- 核心优势
  - 与 FFmpeg 4.4 发布时间相近，是当时开发者的 “默认搭配”，社区案例（如音视频同步、格式解码）极多；
  - 体积轻量，编译依赖少，适合对包体积敏感的小型应用（如嵌入式 Windows 设备）。
- **适用场景**：需要参考大量开源案例、追求 “经受过时间考验” 的稳定搭配。



## SDL函数接口

### SDL_OpenAudio 函数

`SDL_OpenAudio` 是 SDL2 音频子系统的核心函数，用于**打开系统音频设备并配置播放参数**，是 SDL 播放 PCM 音频数据的 “入口”—— 它会根据你指定的音频格式（采样率、位深、声道数等）初始化音频设备，并关联音频回调函数，最终让 SDL 能够持续从回调中获取 PCM 数据并输出到扬声器。

简单来说，它的核心能力是：

1. 与系统底层音频驱动（ALSA/PulseAudio/DirectSound/CoreAudio 等）交互，打开可用的音频输出设备；
2. 校验并适配你指定的音频参数（若设备不支持，可返回兼容的参数）；
3. **启动音频播放线程**，**关联回调函数**（回调会被 SDL 自动触发，用于填充播放数据）；
4. 为后续的音频播放（`SDL_PauseAudio`）、暂停、关闭等操作奠定基础。

**函数原型**

```c
int SDL_OpenAudio(SDL_AudioSpec *desired, SDL_AudioSpec *obtained);
```

> 参数详解：
>
> - desired：「期望的」音频参数配置（由开发者填写），是一个 `SDL_AudioSpec` 结构体，包含采样率、格式、声道数、回调函数等核心信息。
>
> - obtained：「实际获取的」音频参数（由 SDL 填充）：
>
>   - 若为 `NULL`，SDL 会强制使用 `desired` 的参数（不匹配则失败）；
>
>   - 若不为 `NULL`，SDL 会返回设备实际支持的参数（可能与 `desired` 略有差异，比如采样率适配）；
>
> 返回值：
>
>   - 成功：返回 `0`；
>   - 失败：返回 `-1`，可通过 `SDL_GetError()` 获取具体错误信息（如 “无可用音频设备”“参数不支持”）。

**SDL_AudioSpec** 结构体

```c
typedef struct SDL_AudioSpec {
    int freq;                   // 采样率（如 44100、48000 Hz）
    SDL_AudioFormat format;     // 音频样本格式（如 AUDIO_S16SYS：16位有符号，系统字节序）
    Uint8 channels;             // 声道数（1=单声道，2=立体声）
    Uint8 silence;              // 静音值（SDL 自动填充，无需手动设置）
    Uint16 samples;             // 每次回调的样本数（缓冲区大小，建议为 2 的幂，如 1024、2048）
    Uint16 padding;             // 预留字段，无需关注
    Uint32 size;                // 音频缓冲区总字节数（SDL 自动计算，= samples × channels × 字节/样本）
    SDL_AudioCallback callback; // 音频回调函数（SDL 会持续调用此函数获取待播放的 PCM 数据）
    void *userdata;             // 传递给回调函数的自定义数据（可选）
} SDL_AudioSpec;
```

**SDL_AudioFormat** 常用音频格式

> AUDIO_U8:   8 位无符号整数 PCM， 低端设备、简单音频
>
> AUDIO_S16SYS：16 位有符号整数 PCM（系统字节序），最常用（兼容绝大多数设备）
>
> AUDIO_S32SYS：32 位有符号整数 PCM，高保真音频
>
> AUDIO_F32SYS：32 位浮点 PCM，专业音频处理



### SDL_PauseAudio函数

`SDL_PauseAudio` 是 SDL 音频子系统中用于**控制音频设备暂停 / 恢复**的核心函数，其核心作用是切换音频设备的运行状态，直接决定音频回调函数是否被触发，进而控制音频播放的启停。

**函数原型**

```c
void SDL_PauseAudio(int pause_on);
```

> 参数详解：
>
> - pause_on：有2个值，分别为0和1
>   - 0： 恢复（解除暂停）音频设备；
>   - 1： 暂停音频设备（默认状态，SDL_OpenAudio 后）；





## 音频pcm数据准备工作

```shell
# 从视频文件中获取一段20秒的PCM数据
ffmpeg -i ac_wlh.mp4 -t 20 -acodec pcm_s16le -ar 44100 -ac 2 -f s16le ac_wlh20s.pcm
# 测试PCM音频数据生成的是否正确
ffplay -ar 44100 -ac 2 -f s16le ac_wlh20s.pcm
```





## 示例代码1

```c
#include <stdio.h>
#include <SDL2/SDL.h>

// 音频回调函数，用于填充音频缓冲区
void audio_callback(void *userdata, Uint8 *stream, int len) {
    // 这里的userdata指向音频数据，将音频数据填充到stream中
    Uint8 *audio = (Uint8 *)userdata;
    // 计算剩余的音频数据长度
    int audio_len = strlen((char *)audio);
    // 取较小值，避免越界
    int copy_len = (len < audio_len)? len : audio_len;
    // 复制音频数据到输出缓冲区
    SDL_memcpy(stream, audio, copy_len);
    // 更新剩余音频数据指针和长度
    audio += copy_len;
    audio_len -= copy_len;
}

int main(int argc, char *argv[]) {
    // 初始化SDL的音频子系统
    if (SDL_Init(SDL_INIT_AUDIO) < 0) {
        printf("SDL could not initialize! SDL_Error: %s\n", SDL_GetError());
        return 1;
    }

    // 设置音频参数
    SDL_AudioSpec wanted_spec;
    wanted_spec.freq = 44100;  // 采样率，常见的有44100Hz、48000Hz等
    wanted_spec.format = AUDIO_S16SYS;  // 音频格式，16位有符号整数，系统原生字节序
    wanted_spec.channels = 2;  // 声道数，2表示立体声
    wanted_spec.samples = 1024;  // 音频缓冲区大小，较小的值能减少延迟，但可能导致卡顿
    wanted_spec.callback = audio_callback;  // 音频回调函数，当需要更多音频数据时被调用
    // 这里将音频数据作为userdata传入回调函数，实际应用中应读取真实音频文件数据
    wanted_spec.userdata = "一些模拟的音频数据"; 

    // 打开音频设备
    SDL_AudioSpec obtained_spec;
    if (SDL_OpenAudio(&wanted_spec, &obtained_spec) < 0) {
        printf("Unable to open audio device! SDL_Error: %s\n", SDL_GetError());
        SDL_Quit();
        return 1;
    }

    // 开始播放音频
    SDL_PauseAudio(0);

    // 让程序持续运行，保持音频播放，这里简单使用SDL_Delay模拟
    SDL_Delay(5000);  // 播放5秒

    // 停止播放并关闭音频设备
    SDL_PauseAudio(1);
    SDL_CloseAudio();

    // 退出SDL
    SDL_Quit();

    return 0;
}
```

### **代码解释**

1. **初始化 SDL**：使用 `SDL_Init(SDL_INIT_AUDIO)` 初始化 SDL 库，并指定只初始化音频子系统。如果初始化失败，打印错误信息并退出程序。
2. **设置音频参数**
   - `wanted_spec.freq`：指定音频采样率，如 44100Hz 表示每秒采样 44100 次。
   - `wanted_spec.format`：设置音频格式，`AUDIO_S16SYS` 表示 16 位有符号整数，使用系统原生字节序。
   - `wanted_spec.channels`：定义声道数，2 代表立体声。
   - `wanted_spec.samples`：设置音频缓冲区大小，值越小延迟越低，但可能增加卡顿风险。
   - `wanted_spec.callback`：指定音频回调函数，当音频设备需要更多数据时，该函数会被调用。
   - `wanted_spec.userdata`：传递给回调函数的用户数据，示例中只是简单模拟，实际应用中会是从音频文件读取的数据。
3. **打开音频设备**：通过 `SDL_OpenAudio` 尝试打开音频设备，并获取实际使用的音频参数（存储在 `obtained_spec` 中）。若打开失败，打印错误信息，退出 SDL 并结束程序。
4. **播放音频**：调用 `SDL_PauseAudio(0)` 开始播放音频。
5. **保持播放**：示例中使用 `SDL_Delay` 让程序运行一段时间（这里是 5 秒），以保持音频播放，实际应用中可能是通过事件循环等方式来控制播放时长。
6. **停止播放并清理**：调用 `SDL_PauseAudio(1)` 停止播放，然后使用 `SDL_CloseAudio` 关闭音频设备，最后调用 `SDL_Quit` 退出 SDL 库。





## 示例代码2

以下是两者搭配实现 “**视频解码 + SDL 渲染**” 的简化代码片段，体现版本兼容性。

### Linux平台

1. **双线程架构**：
   - 音频线程：负责音频解码、重采样和播放
   - 视频线程：负责视频解码、格式转换和渲染
   - 主线程：处理 SDL 事件循环

2. **同步机制**：
   - 以音频时钟为基准进行同步（人耳对音频同步更敏感）
   - 使用互斥锁保护共享的时钟变量
   - 通过计算音视频时间戳差异来调整播放节奏：
     - 视频超前时：通过休眠等待
     - 音频超前时：跳过视频帧加速

3. **技术细节**：
   - 使用 FFmpeg 4.4 进行音视频解码
   - 使用 SDL 2.26.5 进行音频播放和视频渲染
   - 音频重采样为 SDL 支持的格式（S16）
   - 视频转换为 RGB24 格式用于 SDL 纹理更新
   - 支持窗口关闭和 ESC 键退出

4. SDL函数用法

   1）

5. **测试用例**

```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <pthread.h>
#include <libavformat/avformat.h>
#include <libavcodec/avcodec.h>
#include <libswscale/swscale.h>
#include <libswresample/swresample.h>
#include <libavutil/time.h>
#include <SDL.h>

// 全局变量
AVFormatContext *fmt_ctx = NULL;
int video_stream_idx = -1, audio_stream_idx = -1;
AVCodecContext *video_dec_ctx = NULL, *audio_dec_ctx = NULL;
AVCodec *video_dec = NULL, *audio_dec = NULL;

// 同步时钟
double audio_clock = 0.0;
double video_clock = 0.0;
pthread_mutex_t clock_mutex = PTHREAD_MUTEX_INITIALIZER;
pthread_cond_t cond = PTHREAD_COND_INITIALIZER;
int quit = 0;

// SDL相关
SDL_Window *window = NULL;
SDL_Renderer *renderer = NULL;
SDL_Texture *texture = NULL;
SDL_AudioSpec wanted_spec, spec;
uint8_t *audio_buf = NULL;
uint32_t audio_len = 0;
uint8_t *audio_pos = NULL;

// 音频重采样参数
SwrContext *swr_ctx = NULL;
enum AVSampleFormat audio_dst_fmt = AV_SAMPLE_FMT_S16;
int audio_dst_sample_rate = 44100;
int audio_dst_nb_channels = 2;

// 计算时间戳（秒）
static double get_pts(AVFrame *frame, AVStream *stream) {
    if (frame->pts != AV_NOPTS_VALUE) {
        return av_q2d(stream->time_base) * frame->pts;
    }
    return 0.0;
}

// 音频回调函数
void fill_audio(void *udata, Uint8 *stream, int len) {
    SDL_memset(stream, 0, len);
    
    if (audio_len == 0)
        return;
    
    len = (len > audio_len ? audio_len : len);
    
    SDL_MixAudio(stream, audio_pos, len, SDL_MIX_MAXVOLUME);
    audio_pos += len;
    audio_len -= len;
    
    // 更新音频时钟
    pthread_mutex_lock(&clock_mutex);
    audio_clock += (double)len / (double)(spec.freq * 2 * 2);
    pthread_mutex_unlock(&clock_mutex);
}

// 音频处理线程
void *audio_thread(void *arg) {
    AVStream *audio_stream = fmt_ctx->streams[audio_stream_idx];
    AVPacket *pkt = av_packet_alloc();
    AVFrame *frame = av_frame_alloc();
    uint8_t *audio_out_buffer = NULL;
    
    // 计算输出缓冲区大小
    int out_buffer_size = av_samples_get_buffer_size(NULL, audio_dst_nb_channels, 
                                                    1024, audio_dst_fmt, 1);
    audio_out_buffer = (uint8_t *)av_malloc(out_buffer_size);
    
    while (!quit) {
        if (av_read_frame(fmt_ctx, pkt) < 0) {
            // 文件读取完毕
            break;
        }
        
        if (pkt->stream_index == audio_stream_idx) {
            // 发送数据包到解码器
            if (avcodec_send_packet(audio_dec_ctx, pkt) < 0) {
                fprintf(stderr, "Error sending audio packet for decoding\n");
                break;
            }
            
            // 接收解码后的帧
            while (avcodec_receive_frame(audio_dec_ctx, frame) == 0) {
                // 计算音频帧的时间戳
                double pts = get_pts(frame, audio_stream);
                if (pts != 0.0) {
                    pthread_mutex_lock(&clock_mutex);
                    audio_clock = pts;
                    pthread_mutex_unlock(&clock_mutex);
                }
                
                // 计算输出样本数
                int dst_nb_samples = av_rescale_rnd(
                    swr_get_delay(swr_ctx, audio_dec_ctx->sample_rate) + frame->nb_samples,
                    audio_dst_sample_rate, audio_dec_ctx->sample_rate, AV_ROUND_UP);
                
                // 重采样
                swr_convert(swr_ctx, &audio_out_buffer, dst_nb_samples,
                           (const uint8_t **)frame->data, frame->nb_samples);
                
                // 等待音频播放缓冲区
                while (audio_len > 0 && !quit) {
                    SDL_Delay(1);
                }
                
                if (quit) break;
                
                // 播放音频
                pthread_mutex_mutex_lock(&clock_mutex);
                audio_buf = audio_out_buffer;
                audio_len = av_samples_get_buffer_size(NULL, audio_dst_nb_channels,
                                                      dst_nb_samples, audio_dst_fmt, 1);
                audio_pos = audio_buf;
                pthread_mutex_unlock(&clock_mutex);
            }
        }
        
        av_packet_unref(pkt);
    }
    
    // 清理
    av_free(audio_out_buffer);
    av_frame_free(&frame);
    av_packet_free(&pkt);
    
    return NULL;
}

// 视频处理线程
void *video_thread(void *arg) {
    AVStream *video_stream = fmt_ctx->streams[video_stream_idx];
    AVPacket *pkt = av_packet_alloc();
    AVFrame *frame = av_frame_alloc();
    AVFrame *rgb_frame = av_frame_alloc();
    struct SwsContext *sws_ctx = NULL;
    
    // 分配RGB缓冲区
    int num_bytes = av_image_get_buffer_size(AV_PIX_FMT_RGB24, 
                                           video_dec_ctx->width, video_dec_ctx->height, 1);
    uint8_t *rgb_buffer = (uint8_t *)av_malloc(num_bytes * sizeof(uint8_t));
    av_image_fill_arrays(rgb_frame->data, rgb_frame->linesize, rgb_buffer,
                        AV_PIX_FMT_RGB24, video_dec_ctx->width, video_dec_ctx->height, 1);
    
    // 创建图像转换上下文
    sws_ctx = sws_getContext(video_dec_ctx->width, video_dec_ctx->height, video_dec_ctx->pix_fmt,
                            video_dec_ctx->width, video_dec_ctx->height, AV_PIX_FMT_RGB24,
                            SWS_BILINEAR, NULL, NULL, NULL);
    
    while (!quit) {
        if (av_read_frame(fmt_ctx, pkt) < 0) {
            // 文件读取完毕
            break;
        }
        
        if (pkt->stream_index == video_stream_idx) {
            // 发送数据包到解码器
            if (avcodec_send_packet(video_dec_ctx, pkt) < 0) {
                fprintf(stderr, "Error sending video packet for decoding\n");
                break;
            }
            
            // 接收解码后的帧
            while (avcodec_receive_frame(video_dec_ctx, frame) == 0) {
                // 计算视频帧的时间戳
                video_clock = get_pts(frame, video_stream);
                
                // 获取当前音频时钟
                pthread_mutex_lock(&clock_mutex);
                double audio_time = audio_clock;
                pthread_mutex_unlock(&clock_mutex);
                
                // 计算时间差
                double diff = video_clock - audio_time;
                
                // 根据时间差进行同步
                if (diff > 0.1) {
                    // 视频超前，等待
                    av_usleep((int)(diff * 1000000));
                } else if (diff < -0.1) {
                    // 音频超前，跳过此帧
                    av_frame_unref(frame);
                    continue;
                }
                
                // 转换为RGB格式
                sws_scale(sws_ctx, (const uint8_t *const *)frame->data,
                         frame->linesize, 0, video_dec_ctx->height,
                         rgb_frame->data, rgb_frame->linesize);
                
                // 更新纹理并渲染
                SDL_UpdateTexture(texture, NULL, rgb_frame->data[0], rgb_frame->linesize[0]);
                SDL_RenderClear(renderer);
                SDL_RenderCopy(renderer, texture, NULL, NULL);
                SDL_RenderPresent(renderer);
                
                av_frame_unref(frame);
                
                if (quit) break;
            }
        }
        
        av_packet_unref(pkt);
    }
    
    // 清理
    sws_freeContext(sws_ctx);
    av_free(rgb_buffer);
    av_frame_free(&rgb_frame);
    av_frame_free(&frame);
    av_packet_free(&pkt);
    
    return NULL;
}

int main(int argc, char *argv[]) {
    if (argc != 2) {
        fprintf(stderr, "用法: %s <输入文件>\n", argv[0]);
        return -1;
    }
    
    // 初始化FFmpeg
    av_register_all();
    avformat_network_init();
    
    // 打开输入文件
    if (avformat_open_input(&fmt_ctx, argv[1], NULL, NULL) != 0) {
        fprintf(stderr, "无法打开输入文件 '%s'\n", argv[1]);
        return -1;
    }
    
    // 获取流信息
    if (avformat_find_stream_info(fmt_ctx, NULL) < 0) {
        fprintf(stderr, "无法获取流信息\n");
        return -1;
    }
    
    // 查找音视频流
    for (int i = 0; i < fmt_ctx->nb_streams; i++) {
        if (fmt_ctx->streams[i]->codecpar->codec_type == AVMEDIA_TYPE_VIDEO && video_stream_idx == -1) {
            video_stream_idx = i;
        } else if (fmt_ctx->streams[i]->codecpar->codec_type == AVMEDIA_TYPE_AUDIO && audio_stream_idx == -1) {
            audio_stream_idx = i;
        }
    }
    
    if (video_stream_idx == -1 || audio_stream_idx == -1) {
        fprintf(stderr, "找不到视频或音频流\n");
        return -1;
    }
    
    // 初始化视频解码器
    AVCodecParameters *video_codecpar = fmt_ctx->streams[video_stream_idx]->codecpar;
    video_dec = avcodec_find_decoder(video_codecpar->codec_id);
    video_dec_ctx = avcodec_alloc_context3(video_dec);
    avcodec_parameters_to_context(video_dec_ctx, video_codecpar);
    if (avcodec_open2(video_dec_ctx, video_dec, NULL) < 0) {
        fprintf(stderr, "无法打开视频解码器\n");
        return -1;
    }
    
    // 初始化音频解码器
    AVCodecParameters *audio_codecpar = fmt_ctx->streams[audio_stream_idx]->codecpar;
    audio_dec = avcodec_find_decoder(audio_codecpar->codec_id);
    audio_dec_ctx = avcodec_alloc_context3(audio_dec);
    avcodec_parameters_to_context(audio_dec_ctx, audio_codecpar);
    if (avcodec_open2(audio_dec_ctx, audio_dec, NULL) < 0) {
        fprintf(stderr, "无法打开音频解码器\n");
        return -1;
    }
    
    // 初始化SDL
    if (SDL_Init(SDL_INIT_VIDEO | SDL_INIT_AUDIO | SDL_INIT_TIMER) != 0) {
        fprintf(stderr, "SDL初始化失败: %s\n", SDL_GetError());
        return -1;
    }
    
    // 创建窗口和渲染器
    window = SDL_CreateWindow("FFmpeg + SDL 音视频播放器",
                             SDL_WINDOWPOS_CENTERED, SDL_WINDOWPOS_CENTERED,
                             video_dec_ctx->width, video_dec_ctx->height,
                             SDL_WINDOW_SHOWN);
    if (!window) {
        fprintf(stderr, "无法创建窗口: %s\n", SDL_GetError());
        return -1;
    }
    
    renderer = SDL_CreateRenderer(window, -1, SDL_RENDERER_ACCELERATED | SDL_RENDERER_PRESENTVSYNC);
    if (!renderer) {
        fprintf(stderr, "无法创建渲染器: %s\n", SDL_GetError());
        return -1;
    }
    
    // 创建纹理
    texture = SDL_CreateTexture(renderer, SDL_PIXELFORMAT_RGB24,
                              SDL_TEXTUREACCESS_STREAMING,
                              video_dec_ctx->width, video_dec_ctx->height);
    if (!texture) {
        fprintf(stderr, "无法创建纹理: %s\n", SDL_GetError());
        return -1;
    }
    
    // 设置音频参数
    wanted_spec.freq = audio_dst_sample_rate;
    wanted_spec.format = AUDIO_S16SYS;
    wanted_spec.channels = audio_dst_nb_channels;
    wanted_spec.silence = 0;
    wanted_spec.samples = 1024;
    wanted_spec.callback = fill_audio;
    wanted_spec.userdata = audio_dec_ctx;
    
    if (SDL_OpenAudio(&wanted_spec, &spec) < 0) {
        fprintf(stderr, "无法打开音频设备: %s\n", SDL_GetError());
        return -1;
    }
    
    // 初始化音频重采样器
    swr_ctx = swr_alloc_set_opts(NULL,
                                av_get_default_channel_layout(audio_dst_nb_channels), audio_dst_fmt, audio_dst_sample_rate,
                                av_get_default_channel_layout(audio_dec_ctx->channels), audio_dec_ctx->sample_fmt, audio_dec_ctx->sample_rate,
                                0, NULL);
    if (!swr_ctx || swr_init(swr_ctx) < 0) {
        fprintf(stderr, "无法初始化音频重采样器\n");
        return -1;
    }
    
    // 创建线程
    pthread_t audio_tid, video_tid;
    pthread_create(&audio_tid, NULL, audio_thread, NULL);
    pthread_create(&video_tid, NULL, video_thread, NULL);
    
    // 启动音频播放
    SDL_PauseAudio(0);
    
    // 事件循环
    SDL_Event event;
    while (!quit) {
        SDL_WaitEvent(&event);
        switch (event.type) {
            case SDL_QUIT:
                quit = 1;
                break;
            case SDL_KEYDOWN:
                if (event.key.keysym.sym == SDLK_ESCAPE) {
                    quit = 1;
                }
                break;
        }
    }
    
    // 等待线程结束
    pthread_join(audio_tid, NULL);
    pthread_join(video_tid, NULL);
    
    // 清理资源
    swr_free(&swr_ctx);
    SDL_CloseAudio();
    SDL_DestroyTexture(texture);
    SDL_DestroyRenderer(renderer);
    SDL_DestroyWindow(window);
    SDL_Quit();
    
    avcodec_close(video_dec_ctx);
    avcodec_close(audio_dec_ctx);
    avformat_close_input(&fmt_ctx);
    avformat_network_deinit();
    
    pthread_mutex_destroy(&clock_mutex);
    pthread_cond_destroy(&cond);
    
    return 0;
}
```





## av_rescale_rnd函数

```c
int64_t av_rescale_rnd(
    int64_t src,        // 输入值（要转换的原始数值，如 PTS、样本数）
    int64_t dst_base,   // 目标基数（分母的倒数，如目标时间基的分子）
    int64_t src_base,   // 源基数（分母，如源时间基的分子）
    enum AVRounding rnd // 舍入规则（控制除法后的取整方式）
);
```

`av_rescale_rnd` 是 FFmpeg 核心工具函数（属于 `libavutil`），用于**整数比例缩放 + 可控舍入**，解决不同时间基 / 数值尺度的转换问题（比如把音频帧的 PTS 从「流时间基」转换为「毫秒」，或把视频帧的时间戳从「AV_TIME_BASE」转换为「SDL 播放时钟」）。

简单来说，它的核心公式是：`dst = src × dst_base / src_base`

但区别于普通的整数乘除，它通过**指定舍入规则**避免精度丢失或溢出，是 FFmpeg 中时间戳、采样数、帧率等数值转换的核心工具（音视频同步场景几乎必用）。

> **参数详解**：
>
> - src： 待转换的原始值（如音频帧的 PTS：10000，单位是「源时间基」）；
>
> - dst_base：目标尺度的基数（如要转换为毫秒，目标基数 = 1000，对应「1 秒 = 1000 毫秒」）；
>
> - src_base：源尺度的基数（如源时间基 = AV_TIME_BASE=1000000，对应「1 秒 = 1000000 单位」）；
>
> - rnd：舍入规则（枚举类型 `AVRounding`），核心取值如下：
>
>   \- `AV_ROUND_NEAR_INF`：四舍五入（最常用）；
>
>   \- `AV_ROUND_DOWN`：向下取整（截断，如 1.9→1）；
>
>   \- `AV_ROUND_UP`：向上取整（如 1.1→2）；
>
>   \- `AV_ROUND_ZERO`：向零取整（如 -1.9→-1）；
>
> **返回值**：
>
>   - 成功：转换后的目标值（int64_t 类型，避免溢出）；
>   - 逻辑：本质是 `(src × dst_base + 偏移量) / src_base`，偏移量由舍入规则决定。

### 核心应用场景（音视频同步）

FFmpeg 中最常见的用途是**时间戳（PTS/DTS）转换**：FFmpeg 解码出的帧的 PTS 是以「流的时间基（AVStream->time_base）」为单位的整数，而播放时需要转换为「毫秒」「秒」或「SDL 音频时钟」等易理解的单位，此时必须用 `av_rescale_rnd` 保证精度。

#### 场景 1：PTS 从「流时间基」转换为「毫秒」

```c++
// 假设：音频流的时间基是 AVStream->time_base = {1, 44100}（1/44100 秒）
// 音频帧的 PTS = 10000（单位：1/44100 秒），转换为毫秒
AVStream *audio_stream = fmt_ctx->streams[audio_stream_idx];
int64_t pts = 10000;
// 公式：毫秒 = pts × 1000 / (audio_stream->time_base.den / audio_stream->time_base.num)
// 简化为 av_rescale_rnd：
int64_t pts_ms = av_rescale_rnd(
    pts,
    1000,                              // 目标基数：1秒=1000毫秒
    av_q2d(audio_stream->time_base) * 1000000 ? audio_stream->time_base.den : 1, // 等价于 1/(time_base) → 简化为 time_base.den / time_base.num
    AV_ROUND_NEAR_INF                  // 四舍五入
);
// 更简洁的写法（用 AVRational 直接计算）：
int64_t pts_ms = av_rescale_q_rnd(
    pts,
    audio_stream->time_base,           // 源时间基
    (AVRational){1, 1000},             // 目标时间基：1/1000 秒（毫秒）
    AV_ROUND_NEAR_INF
);
printf("PTS（毫秒）：%lld\n", pts_ms);
```

#### 场景 2：音频样本数转换为播放时间（秒）

```c++
// 已知：采样率 44100Hz（1秒=44100个样本），已播放 10240 个样本 → 转换为秒
int64_t samples = 10240;
int64_t time_us = av_rescale_rnd(
    samples,
    1000000,   // 目标基数：1秒=1000000 微秒
    44100,     // 源基数：1秒=44100 样本
    AV_ROUND_DOWN // 向下取整，避免播放时间超前
);
double time_sec = time_us / 1000000.0;
printf("已播放时间：%.3f 秒\n", time_sec); // 输出：0.232 秒
```

#### 场景 3：音视频同步（调整音频播放速度）

```c++
// 假设：视频时钟是 1.5 秒，音频时钟是 1.4 秒 → 计算需要补偿的样本数
int64_t audio_clock_ms = 1400;  // 音频时钟（毫秒）
int64_t video_clock_ms = 1500;  // 视频时钟（毫秒）
int64_t diff_ms = video_clock_ms - audio_clock_ms; // 差值：100 毫秒

// 转换为需要多播放的音频样本数（44100Hz）
int64_t compensate_samples = av_rescale_rnd(
    diff_ms,
    44100,     // 源基数：1秒=44100 样本
    1000,      // 目标基数：1秒=1000 毫秒
    AV_ROUND_NEAR_INF
);
printf("需要补偿的样本数：%lld\n", compensate_samples); // 输出：4410
```

### 关键特性与注意事项

#### 1. 避免溢出

FFmpeg 内部用 `int64_t` 计算，即使 `src × dst_base` 超过 32 位整数范围，也能避免溢出（普通 `(src*dst_base)/src_base` 易溢出）。

#### 2. 舍入规则的选择

- **音视频同步**：优先用 `AV_ROUND_NEAR_INF`（四舍五入），保证时间戳精准；
- **缓冲区填充**：用 `AV_ROUND_DOWN`（向下取整），避免缓冲区溢出；
- **延迟计算**：用 `AV_ROUND_UP`（向上取整），保证延迟足够。

#### 3. 简化版函数

- `av_rescale(src, dst_base, src_base)`：等价于 `av_rescale_rnd(src, dst_base, src_base, AV_ROUND_NEAR_INF)`（默认四舍五入）；
- `av_rescale_q(src, src_q, dst_q)`：直接基于 `AVRational`（分数）转换，无需手动拆分分子分母（更常用）；
- `av_rescale_q_rnd(src, src_q, dst_q, rnd)`：`av_rescale_q` + 自定义舍入规则。

#### 4. AVRational 与 av_rescale_q（更易用）

FFmpeg 中时间基用 `AVRational` 表示（如 `{1, 44100}` 代表 1/44100 秒），`av_rescale_q` 是更贴合场景的封装：

```c++
// 示例：PTS 从流时间基转换为毫秒
AVRational src_tb = audio_stream->time_base; // 源时间基：{1, 44100}
AVRational dst_tb = {1, 1000};               // 目标时间基：{1, 1000}（毫秒）
int64_t pts_ms = av_rescale_q_rnd(pts, src_tb, dst_tb, AV_ROUND_NEAR_INF);
```

### 小结

`av_rescale_rnd` 是 FFmpeg 中**数值比例转换的 “瑞士军刀”**，核心解决：

1. 整数乘除的精度问题（舍入规则）；
2. 大数运算的溢出问题（int64_t 计算）；
3. 时间基 / 尺度转换的标准化（适配音视频同步、采样数 / 时间转换等场景）。

在 SDL+FFmpeg 音视频播放中，它是实现「音频 PTS 转播放时间」「音视频时钟同步」「采样数转播放时长」的核心工具，几乎所有时间相关的转换都离不开它。



## av_sample_get_buffer_size 函数

```c
int av_sample_get_buffer_size(
    int *linesize,        // 输出：单声道样本的字节数（可传 NULL，无需获取）
    int nb_channels,      // 声道数（1=单声道，2=立体声，6=5.1声道等）
    int nb_samples,       // 样本数（单声道的样本数量，非总样本数）
    enum AVSampleFormat fmt, // 样本格式（如 AV_SAMPLE_FMT_S16、AV_SAMPLE_FMT_FLTP）
    int align             // 内存对齐参数（通常传 1 表示不对齐，或 32/64 优化内存访问）
);
```

`av_sample_get_buffer_size` 是 FFmpeg `libavutil` 库中用于**计算音频样本缓冲区所需字节数**的工具函数 —— 根据音频的声道数、样本数、样本格式，快速算出存储这些音频数据需要的缓冲区大小，避免手动计算出错（尤其是平面格式、不同位深的场景）。

简单来说，它解决了 “给定 N 个音频样本，需要分配多大的内存缓冲区” 的问题，是 FFmpeg 音频处理中分配缓冲区、校验数据长度的核心函数（比如解码后分配 PCM 缓冲区、重采样前预估输出缓冲区大小）。

>  **参数详解：**
>
>  - linesize：输出参数，单声道样本数据占用的字节数（比如 1024 个 16 位样本 → linesize=2048）；若不需要该值，传 `NULL` 即可；
>
>  - nb_channels：声道数（必须是合法值，如 1/2/6 等，不能为 0）；
>
>  - nb_samples：单声道的样本数量（核心！比如立体声要存储 1024 个样本对，该值传 1024，而非 2048）；
>
>  - fmt：样本格式（FFmpeg 枚举类型，区分「打包格式」和「平面格式」，如：
>
>   \- `AV_SAMPLE_FMT_S16`：16 位打包格式（立体声连续存放 LRLRLR...）；
>
>   \- `AV_SAMPLE_FMT_FLTP`：浮点平面格式（左声道、右声道分开存放）；
>
>  - align：内存对齐（优化内存读写速度）：
>
>    \- 传 `0`：表示默认对齐方式；
>
>    \- 传 `1`：无对齐（通用场景）；
>
>    \- 传 `32/64`：按 32/64 字节对齐（高性能场景）； 
>
>  **返回值**
>
>    - 成功：返回缓冲区总字节数（存放指定音频数据所需的最小内存大小）；
>    - 失败：返回负数（错误码，如 `AVERROR(EINVAL)` 表示参数非法）。



​        























​                        





































