# 音视频理论与开发

如今我们所处的时代，是移动互联网时代，也可以说是视频时代。从优酷到爱奇艺，从搜狐到芒果，从西瓜到B站，从快手到抖音，从钉钉到腾讯会议，我们的生活和工作已经被越来越多的**音视频元素**所影响。而这一切都离不开音视频拍摄技术的不断升级，还有视频制作产业的日益强大。

![image-20250408173011267](.\assets\image-20250408173011267.png)

此外，也离不开通信技术的飞速发展和进步。试想一下，如果咱们上网还是当年的电话拨号上网，只有几十KB的下载速度，或者手机还只是`2G`时代，咱们还能享受到现在动辄`1080P`甚至是`4K`的视频体验吗？

目前我们处于`5G+`的时代，是一个万物互联的时代，也是一个AI时代。音视频产业的发展规律一直是**需求**和**技术创新**双轮驱动，并且互为因果。在AI时代，技术创新将不断挖掘音视频产业的潜力，使其有更强的信息承载能力和更具潜力的应用价值，从而不断推进产业升级。今天，**直播场景**掀起了线上互动的浪潮，保障实时传输音视频数据时的稳定性、流畅度、清晰度，达到完美的用户体验，是对实时音视频技术的考验。

![image-20250408145359125](.\assets\image-20250408145359125.png)

随着`5G`与音视频领域的融合发展，未来音视频产业发展将会迎来三个新转变：

- 一是音视频产业会有质的飞跃，新阶段技术创新将扮演更为重要的作用；
- 二是音视频产业会面临全新的生态，随着知识产权和版权保护数字技术发展，数字音视频会实现爆发式增长，未来会形成一个全域的音视频服务生态，达到数万亿级的市场规模；
- 三是音视频产业会处于一个大的创新期，发展方式将有规模竞争、资本竞争转变为一人才为核心的创新能力的提升。



除了网络通信技术和视频拍摄工具有了很大提升之外，我们能享受到音视频带来的便利和乐趣，还有一个重要因素，就是**音视频编码技术**的突飞猛进。

以下是针对音视频开发（尤其是C++方向）的核心知识点总结，结合基础理论与实际开发需求：


## 一、音频开发核心知识点
#### 1.声音三要素

- 音调：由声音频率决定，频率越高音调越高。例如，钢琴的高音区频率更高。

- 音量（响度）：由声波振幅决定，振幅越大响度越高。
- 音色：由波形和谐波成分决定，不同乐器的音色差异源于谐波组合。



#### 2.音频的模数转换

- 采样（Sampling）：按一定时间间隔采集模拟信号的瞬时值。根据**奈奎斯特定理**，采样率需至少为信号最高频率的2倍（例如人耳可听范围`20Hz-20kHz`，常用采样率`44.1kHz`）。
- 量化（Quantization）：将连续幅度值转换为离散数值，位深（如`16bit`、`24bit`）决定精度。`16bit`可表示65536个等级，满足CD音质需求。
- 编码（Encoding）：将量化后的数据转换为二进制码流，如`PCM`（脉冲编码调制）。



#### 3. 音频参数与压缩

- 数字音频参数  
  - 采样率（`44.1kHz/48kHz`）、量化位数（`16/24/32位`）、声道数（`单声道/立体声/多声道`）。
  - **字节序**：注意大端（`BE`）和小端（`LE`）的处理（如`WAV`文件为小端）。
- 压缩技术
  - `PCM`：原始无压缩格式（如`WAV`的底层数据）。
  - 有损压缩：如`MP3`（`MPEG-1 Layer III`）、`AAC`，通过去除人耳不易察觉的高频信息减小体积。
  - 无损压缩：如`FLAC`、`ALAC`，保留原始数据但压缩率较低。




#### 4. C++开发实践
- 音频库与工具链  
  - **`FFmpeg`**：音频编解码（如`libavcodec`）、格式处理（如`libavformat`）。
  - **`PortAudio`**：跨平台音频输入输出（低延迟处理）。
  - **`libsndfile`**：读写`WAV/FLAC`等格式。
  - **`SPIRV-Tools`**：音频算法优化（如`FFT`、滤波）。
- 关键开发场景  
  - **音频采集与播放**：通过`PortAudio`或`alsa/pulseaudio API`实现。
  - **编解码**：使用`FFmpeg`的`avcodec_encode_audio2`和`avcodec_decode_audio4`。
  - **格式转换**：`PCM`转`MP3/AAC`，或不同采样率/声道数转换。
  - **算法实现**：回声消除（`AEC`）、降噪（`NS`）、自动增益控制（`AGC`）。




## 二、视频开发核心知识点
#### 1. 图像与视频参数  
- 图像：图像是二维视觉信息的数字化表示，由像素（Pixel）组成。
- 像素（`Pixel`）：图像的最小单位，每个像素包含颜色、亮度等信息。
- 色彩空间：`RGB`红绿蓝三原色，常用于显示设备；`YUV`亮度（Y）+色度（U/V），常用于视频编码。
- 像素深度：每个像素存储的位数（如 8 位、16 位、24位），决定颜色数量，8 位支持 256 色，24 位支持约 1600 万色（真彩色）。
- 帧（`Frame`）：视频由连续静态图像组成，一张图像就称为一帧。
- 帧率：表示每秒播放的帧数（`Frame Per Second`），如`24/30/60fps`.
- 分辨率：图像像素数量（如`1080p`为1920×1080），影响清晰度。



#### 2. 视频编解码标准  

- **`H.264/AVC`**：广泛应用的编码标准（如`libx264`编码器）。
- **`H.265/HEVC`**：更高压缩率，适合`4K/8K`视频。
- **`VP8/VP9`**：开源编码，常用于`WebRTC`。
- **`AV1`**：开源高效编码，适合低带宽场景。



#### 3.视频容器与封装格式  

- `AVI`
- `FLV`
- `MP4`
- `MKV`
- `TS`

#### 4. C++开发实践
- **视频库与工具链**  
  - **`libvlc`**:   视频开发 
  - **`FFmpeg`**：视频编解码（`libavcodec`）、格式处理（`libavformat`）、滤镜（`libavfilter`）。
  - **`OpenCV`**：图像处理、视频分析（如目标检测、光流计算）。
  - **`WebRTC`**：实时视频传输（`VP8/VP9`编码、`SVC`分层编码）。
  - **`SDL2`**：跨平台视频渲染。
  
- **关键开发场景**  
  - **视频采集与渲染**：通过`OpenCV`或`FFmpeg`读取摄像头数据，`SDL2`显示。
  - **编解码与转码**：使用`FFmpeg`实现`H.264`编码/解码，或格式转换（如`MP4`转`FLV`）。
  - **视频处理**：滤镜（缩放、裁剪）、水印添加、码率控制（`CBR/VBR`）。
  - **流媒体传输**：`RTMP`推流（如通过`librtmp`)、`RTSP`拉流。


## 三、音视频开发通用技能
1. **时间戳与同步**  
   - 音频和视频通过`PTS（Presentation Time Stamp）`对齐，处理音画不同步问题。
2. **`FFmpeg`深度应用**  
   - 掌握`FFmpeg`的`API`流程（打开文件→解复用→解码→处理→编码→复用）。
3. **性能优化**  
   - 多线程处理（如解码/编码分离）、`SIMD`加速（如`x264`的汇编优化）。
4. **调试工具**  
   - `Wireshark`抓包分析流媒体协议，`GDB`调试内存泄漏，`Valgrind`检测性能瓶颈。


## 四、学习建议
1. **入门项目**  
   - 实现简单播放器：用`SDL2`渲染视频，`FFmpeg`解码。
   - 开发实时视频流传输工具（如`RTSP`客户端）。
2. **推荐资源**  
   - 《`FFmpeg`从入门到精通》（**雷霄骅**）、《音视频开发进阶指南》。
   - `GitHub`项目：`FFmpegExamples`、`WebRTC`官方示例。
3. **社区与工具**  
   - 国内论坛：音视频开发网、`CSDN`；国外：`Stack Overflow`、`FFmpeg邮件列表`。


通过结合理论知识与C++实战，逐步深入可掌握音视频开发的核心技术。建议从`FFmpeg`的基础`API`入手，逐步扩展到复杂场景（如流媒体、编解码优化）。

# 音视频基础知识



## 一、音频是什么？

**声音**是由物体振动产生的**机械波**，通过介质（如空气、水等）传播到我们的耳朵，引起耳膜振动，进而被我们的听觉系统感知。而**音频**则是将这种物理声音转化为电信号或数字信号的形式，以便于存储、传输和处理。所以**音频也可以认为是存储在计算机中的声音**。      

在自然界中，声音是连续不断地，是一种**模拟信号**，那怎样才能把声音保存下来呢？那就是需要将**声音数字化**，转换为**数字信号**。我们知道声音是一种波，有自己的振幅和频率，那么要保存声音，就要保存声音在各个时间点上的振幅。而数字信号并不能连续保存所有时间点上的振幅，事实上，也并不需要保存连续的信号，就可以还原到人耳可以接受的声音。

而音频数据的承载方式最常用的就是**脉冲编码调制**，即`PCM`.

根据**奈奎斯特采样定理**，为了不失真地恢复模拟信号，采样频率应该不小于模拟信号频谱中最高频率的2倍。

`PCM`的采集步骤分为以下几个步骤：

>  模拟信号 --> 采样 --> 量化 --> 编码 --> 数字信号



#### 声音的三要素

声音的三要素为：频率、振幅、波形。

> 声波的**频率**，即声音的**音调**，音调主要由物体振动的频率决定。频率越高，音调越高；频率越低，音调越低。例如，在音乐中，高音区的音符振动频率高，发出的声音尖锐刺耳，音调就高；低音区的音符振动频率低，发出的声音低沉浑厚，音调就低。

那**什么是频率呢**？**频率是描述周期运动频繁程度的量，是指物质在 1 秒内完成周期性变化的次数**，常用符号
`f`表示，单位为赫兹，简称 “赫”，符号为`Hz`。例如，当摆锤在摆动的时候，10 秒钟内摆动了 50 次，那么平均下来每秒摆动 5 次，其频率就是 5 次 / 秒，即` 5Hz`。

> 声波的**振幅**，即声音的**响度**，也就是声音的高低，一般男生的声音振幅大于女生。振动幅度越大，响度越大；振动幅度越小，响度越小。比如，用力敲鼓，鼓面振动幅度大，发出的声音响度大，听起来很响亮；轻轻敲鼓，鼓面振动幅度小，声音响度小，听起来比较微弱。此外，响度还与距离发声体的远近有关，距离越远，响度越小。

<img src=".\assets\image-20250408171033538.png" alt="image-20250408171033538" style="zoom: 33%;" />

以上就是一个声波的示意图，其中：

`A0`： 表示振幅。

`T`：表示完成一个振动循环所需要的时间。

波长(`λ`)：是指声波在媒介中传播完一个完整周期所走的距离，通常单位为米，用字母`m`表示，它也可以表示为周期波上一点到下一个周期波相同位置的距离。

在空气中，声音传播的速度(声速`v`)约为`343m/s`，在固体和液体中，声音传播的速度更快，一般是一个固定的值。波长与频率的关系可以用一个数学公式来表示：`λ` =  `v / f`.

从以上式可以看出，声波频率越高，波长越短。低频声波波长长，高频声波波长短，如下图所示。

<img src=".\assets\声波波形图.png" style="zoom:50%;" />



> 声波的**波形**，即声音的音色，同样的频率和振幅下，钢琴和小提琴的声音听起来就是完全不同的，因为他们的音色不同。波形决定了其所代表的声音的音色，音色不同是因为他们的截止所产生的波形不同。**音色由发声体的材料、结构以及发声方式等因素决定**。不同的发声体由于材料、结构不同，发出声音的音色就不同。



<img src=".\assets\image-20250408160035879.png" alt="image-20250408160035879" style="zoom:50%;" />



#### 采样率

采样率，即采样的频率。人类听觉的频率（音调）范围为`20Hz —— 20kHz`。根据**奈奎斯特采样定理**，采样率要大于原声波频率的2倍，人耳能听到的最高频率为`20kHz`，所以为了满足人耳的听觉要求，采样率至少为`40kHz`，通常为`44.1kHz`，代表1秒会采样44100次，更高的通常为`48kHz`。

![](.\assets\声音的频率.png)

#### 量化

波形振幅在模拟信号上是连续的样本值，而在数字信号中，信号一般是不连续的，所以模拟信号**量化**以后，只能取一个近似的整数值。为了记录这些振幅值，采样器会采用一个固定的位数来记录这些振幅值，通常为**8位、16位、32位**。**位数越多，记录的值越准确，还原度越高，但是占用的磁盘空间越大**。

| 位数 | 最小值      | 最大值     |
| ---- | ----------- | ---------- |
| 8    | 0           | 255        |
| 16   | -32768      | 32767      |
| 32   | -2147483648 | 2147483647 |



#### 音频编码

每一个量化都是一个采样，将这么多采样点的数据存储下来，就是编码了，最后得到的数据就是数字信号了。所谓**编码**，就是按照一定的格式记录采样和量化后的数字数据，比如顺序存储或者压缩存储。

通常所说的音频裸数据格式就是**脉冲编码调制**数据，即`PCM(Pulse - Code Modulation)`数据。



整个过程分为以下几个过程：

> 模拟信号 --> 采样 --> 量化 --> 编码 --> 数字信号

如下为示意图：

![](.\assets\PCM编码步骤.png)

#### 声道

声道是指声音在**录制或播放时所采用的通道数量及相关技术方式**，它对于声音的传播和表现有着重要作用。

- 单声道：是指所有声音都通过一个通道进行录制和播放。这种方式下，声音信号是单一的，所有的音频信息都混合在一起，没有左右声道的区分。其优点是简单、成本低，缺点是声音的空间感和立体感较差，无法准确地表现声音的方位和层次感。
- 双声道：也称为**立体声**，它使用两个独立的声道来录制和播放声音，通常分别称为**左声道**和**右声道**。两个声道的声音信号略有不同，通过不同的扬声器或耳机播放时，能够营造出声音的空间感和立体感，让听众可以分辨出声音的来源方向和位置，增强了听觉的真实感和沉浸感。例如，在欣赏音乐时，立体声可以让听众感觉到乐器和歌手在不同的位置上发声，仿佛置身于音乐会现场。
- 多声道：是在双声道的基础上，增加了更多的声道，以提供更丰富的声音信息和更逼真的环绕声效果。常见的多声道系统有 5.1 声道、7.1 声道等。以 5.1 声道为例，“5” 表示有五个主要声道，分别是**前置左声道**、**前置中置声道**、**前置右声道**、**后置左声道**和**后置右声道**，“1” 表示一个**超低音声道**，专门用于播放低频声音，增强音效的震撼力。这种声道布局可以让声音从多个方向包围听众，营造出全方位的环绕声效果，在观看电影或玩游戏时，能让用户更强烈地感受到声音的空间感和动态感。

#### 码率

码率又称**比特率**，是指单位时间内传输或处理的比特数，用于衡量数据传输或处理的速度，单位为`bps`（`bit per second`）。

在音频领域，码率用于描述音频数据的压缩程度和质量。一般来说，**码率越高，音频的质量就越好**，因为更高的码率意味着能够更精确地表示原始音频信号的细节和动态范围。例如，`CD` 音质的音频码率通常为 `1411.2kbps`，而常见的 `MP3` 音频文件，码率可能在 `128kbps - 320kbps` 之间。

码率的计算公式： 码率 = 采样率 * 采样位数 * 声道数

#### 音频格式

音频格式是指**用于存储和传输音频数据的不同文件格式**，它规定了音频数据的编码方式、存储结构以及相关的元数据等。常见的音频格式有以下几种：
- **WAV**
    - **特点**：是一种无损音频格式，通常采用脉冲编码调制（PCM）技术对音频信号进行采样、量化和编码，能够保留原始音频的所有信息，音质非常好。它的优点是兼容性强，几乎所有的音频编辑软件和播放设备都支持该格式；缺点是文件体积较大，因为它没有经过压缩或者采用的是无损压缩方式。
    - **应用场景**：常用于专业音频录制、音频编辑和对音质要求较高的场合，如音乐制作、电影配乐等。
- **MP3**
    - **特点**：是一种**有损压缩**音频格式，它利用人耳的听觉特性，对音频信号中的一些人耳不太敏感的部分进行压缩，从而大大减小了文件体积，同时又能保持相对较好的音质。MP3格式的压缩比可以根据需要进行调整，常见的码率有`128kbps`、`192kbps`、`320kbps`等，码率越高，音质越好，文件体积也越大。
    - **应用场景**：由于其文件体积小、音质相对较好，兼容性强，被广泛应用于音乐下载、在线音乐播放、移动设备音乐存储等领域，是目前最流行的音频格式之一。
- **WMA**
    - **特点**：是微软公司开发的一种音频格式，它也采用了有损压缩技术。`WMA`格式在相同的码率下，音质通常比`MP3`更好，而且还支持数字版权管理（`DRM`）技术，可以对音频文件进行加密和版权保护，防止未经授权的复制和传播。
    - **应用场景**：在Windows操作系统中应用较为广泛，常用于在线音乐服务、数字音乐销售等领域，一些微软的设备和软件对`WMA`格式有很好的支持。
- **FLAC**
    - **特点**：是一种无损压缩音频格式，它能够在不损失任何音频信息的前提下，将音频文件压缩到原来大小的50% - 70%左右。FLAC格式的音质与WAV等无损格式相当，但文件体积更小，便于存储和传输。
    - **应用场景**：受到很多音乐发烧友的喜爱，常用于高品质音乐的存储和分享，一些专业的音频设备也支持FLAC格式的播放。
- **AAC**
    - **特点**：是一种高级音频编码格式，也是MPEG - 2和MPEG - 4标准的一部分。**AAC在低码率下能够提供比MP3更好的音质**，它采用了更先进的音频编码技术，能够更准确地还原原始音频信号。此外，AAC格式还支持多声道音频，可用于环绕声音乐和视频的音频编码。
    - **应用场景**：广泛应用于数字音频广播（DAB）、数字电视音频、在线音乐服务以及苹果设备等。例如，苹果的`iTunes`商店中的音乐大多采用`AAC`格式进行编码。

#### 音频帧

音频和视频不太一样，视频的每一帧就是一副图像，但音频是流式的，本身没有一帧的概念，但是咱们可以规定一帧的概念，比如`amr`帧比较简单，它规定每`20ms`的音频为一帧。



#### 音频文件大小的计算

假设单位为字节，则文件大小 = 采样率 * 采样位数 * 通道数  * 录音时间 / 8

比如对于`PCM`流来说，采样率为`44100Hz`，采样位数为16，通道数为2，那么**一秒**的音频固定大小就是44100 * 16 * 2 bit，计算出来就是176400个字节。

#### 音频使用场景及应用

在现实生活中，音频（audio）主要用在两大场景中：语音（voice）和音乐（music）。语音主要用于沟通通信，如打电话，现在由于语音识别的发展，人机语音交互也是语音的一个应用，目前正在风口上，好多大厂都推出了**智能音箱**。**音乐**主要用于欣赏，如音乐播放。

音频开发的主要应用：

- 音频播放器
- 录音机
- 语音电话
- 音视频监控
- 音视频直播
- 音频编辑/处理软件
- 蓝牙耳机/音箱



## 二、视频是什么？

#### 图像

在聊视频之前，我们先来说说图像。**图像**是一种视觉信息的载体，它通过各种方式对客观世界中的物体、场景等进行视觉呈现，人们可以通过视觉系统来感知和理解图像所传达的信息。我们常见的各种图片就是图像。一副图像的构成有以下几个要素：

- **像素**：是图像的最小单位，每个像素都包含了颜色、亮度等信息。下面的这幅图像，就可以展示一个像素点的效果，众多像素点按照一定的排列顺序组合在一起，就构成了图像的整体画面。例如，在高分辨率的图像中，像素数量较多，图像细节就更加丰富、清晰；而低分辨率图像像素数量少，可能会出现模糊、锯齿等现象。像素的英文叫`Pixel`，缩写为`PX`，这个单词是有`Picture`（图像）和`Element`（元素）这两个单词的字母所组成的。
- **颜色**：是图像的重要视觉要素之一，通过不同的颜色组合和搭配，可以传达出不同的情感、氛围和信息。图像中的颜色可以用多种色彩模型来表示，如常见的 `RGB`（红、绿、蓝）模型、`CMYK`（青、品红、黄、黑）模型等。在 `RGB` 模型中，通过调整红、绿、蓝三种颜色的强度，可以混合出各种不同的颜色。
- **亮度和对比度**：亮度决定了图像的明亮程度，而对比度则是指图像中不同区域之间的亮度差异。合适的亮度和对比度可以使图像更加清晰、生动，突出图像的主体和细节。例如，在一幅夜景图像中，通过调整亮度和对比度，可以更好地表现出黑暗中的灯光、建筑物的轮廓等细节。

![](.\assets\像素点.jpg)

#### 颜色的表示

每个像素点必须要有颜色，才能组成缤纷绚丽的图片。那么颜色在计算机中该如何表示呢？大家都知道，我们生活中的颜色，可以拥有无数种类别。比如

<img src=".\assets\image-20250410103848983.png" alt="image-20250410103848983" style="zoom:50%;" />

在计算机系统里，我们不可能用文字来表述颜色。不然，就算我们不疯，计算机也会疯掉的。在数字时代，当然是用数字来表述颜色。
这就牵出了“**彩色分量数字化**”的概念，从而产生了颜色空间的概念

#### 颜色空间

颜色空间是一种用于表示和描述颜色的数学模型或系统，它定义了颜色的表示方式、范围以及颜色之间的关系。常见的颜色空间类型有`RGB`、`YUV`、`HSV`、`CMYK`等。

- `RGB`：基于红（Red）、绿（Green）、蓝（Blue）三种原色光，通过不同强度的这三种颜色光的混合来表示各种颜色。这三种颜色被称为**三基色**，它们相互独立，任何一种颜色都不能由其他两种颜色混合而成，而其他所有颜色都可以由这三种颜色按不同的比例混合得到。广泛应用于显示设备，如电脑显示屏、手机屏幕、电视屏幕等。在这些设备中，每个像素点都是由红、绿、蓝三个子像素组成，通过控制这三个子像素的亮度来显示出不同的颜色。

- 

  <img src=".\assets\三基色.png" style="zoom:50%;" />

  `RGB`中每一种颜色的取值分别是从0到255，一共256个等级(256是2的8次方)，通过这种方式可以表达多少种颜色呢？

  很显然，答案是256 * 256 * 256 = 16,777,216种，因此也简称为**1600万色**，这个颜色范围已经超过了人眼可见的全部色彩，又称为**真彩色**。而在计算机中表示时，`RGB`三色，每一种颜色用8个bit表示，那存储一个像素点就需要24位色，占用24个`bit`。

  

  <img src=".\assets\image-20250410113037776.png" alt="image-20250410113037776" style="zoom:50%;" />

  

  

- `YUV`：主要用于**视频编码、传输以及图像信号处理**等领域。`YUV` 颜色空间将颜色信息分为亮度（Y）和色度（U、V）两个部分。其中，“Y” 表示**亮度分量**，代表图像的明亮程度（`Luma`），它包含了图像的黑白信息，类似于灰度图像。“U” 和 “V” 是**色度分量**（`Chroma`），用于表示颜色的色调和饱和度等信息，其中 **U 表示蓝色色度分量，是蓝色通道与亮度的差值**，**V 表示红色色度分量，是红色通道与亮度的差值**。视频通信系统中采用`YUV`，而不是`RGB`，主要是因为**`RGB`信号不利于压缩**。

  在最近几十年中，视频工程师发现，眼睛对于亮和暗的分辨要比对颜色的分辨更精细一些，也就是说，**人眼对色度的敏感程度要低于对亮度的敏感程度**。所以，工程师认为，在我们的视频存储中，没有必要存储全部颜色信号。我们可以把更多带宽留给黑—白信号（被称作“亮度”，人眼对亮度敏感），将稍少的带宽留给彩色信号（被称作“色度”，人眼对色度不敏感），这样可以通过压缩UV的分辨率，在不影响观感的前提下，**减小视频的体积**。于是，就有了 `YUV`。 大家偶尔会见到的 `Y'CbCr`，也称为 `YUV`，是 `YUV` 的压缩版本，不同之处在于 `Y'CbCr`用于数字图像领域，`YUV` 用于模拟信号领域，`MPEG`、`DVD`、摄像机中常说的 `YUV` 其实就是 `Y'CbCr`。

  

  ![image-20250410112455330](.\assets\image-20250410112455330.png)

  主流的采样方式有三种：
  
  - `YUV4:4:4`
  - `YUV4:2:2`
  - `YUV4:2:0`
  - 
  
  ![image-20250410112628726](.\assets\image-20250410112628726.png)
  
  这里我们简单介绍一下。具体解释起来有点繁琐，大家只需记住，通常用的是 `YUV4:2:0` 的采样方式，能获得 1/2 的压缩率。

​	以上两种空间是可以转换的，有具体的**转换公式**：

<img src=".\assets\convert.png" style="zoom: 33%;" />



- `HSV`：将颜色用色调（Hue）、饱和度（Saturation）和明度（Value）三个参数来表示。

  **色调**表示颜色的种类，如红色、绿色、蓝色等，其取值范围为0°～360°，从红色开始按逆时针方向计算，红色为 0°，绿色为 120°,蓝色为 240°。它们的补色是：黄色为 60°，青色为 180°,品红为 300°；

  **饱和度**描述颜色的鲜艳程度，饱和度越高，颜色越鲜艳，反之则越暗淡；通常取值范围为 0%～100%，值越大，颜色越饱和。

  **明度**表示颜色的明亮程度，从黑色（明度为 0）到白色（明度为 1）。在一些图像处理和计算机图形学应用中较为常用，**因为它更符合人类对颜色的感知方式**。例如，在图像编辑软件中，用户可以通过调整 `HSV` 参数来快速地改变颜色的外观，而不需要像在 `RGB` 颜色空间中那样分别调整红、绿、蓝三个通道的值。

  
  
  ![](.\assets\HSV.png)

**总结一下**：

- `RGB`：是一种相加混色的颜色空间，适用于自发光的**显示设备**，如电脑显示屏、手机屏幕等。颜色混合方式符合光学原理，混合后的颜色更明亮。广泛应用于各种电子显示设备，如电脑显示器、电视、手机屏幕等，以及图像编辑软件、计算机图形学、视频游戏等领域，是数字图像和视频中最常用的颜色空间之一。
- `CMYK`：是一种相减混色的颜色空间，适用于印刷领域。由于**印刷过程**中使用的油墨会吸收光线，所以通过青、品红、黄三种油墨混合得到的颜色会越来越暗，为了弥补这一不足，引入黑色油墨来增强暗部效果。主要应用于印刷行业，如书籍、报纸、海报、宣传单页等印刷品的设计和制作。在印刷前，需要将图像从 `RGB` 颜色空间转换为 `CMYK` 颜色空间，以确保印刷出的颜色符合预期。
- `YUV`：利用人眼对亮度信息敏感而对色度信息相对不敏感的特性，将亮度和色度分离，便于对图像进行处理和压缩，在**视频处理**中可以减少数据量，同时保证一定的图像质量。在视频编码、传输和存储中应用广泛，如常见的视频格式（如 `H.264`、`H.265` 编码的视频）通常采用 `YUV`颜色空间，以提高编码效率和压缩比，减少视频数据量，便于在网络上传输和存储。
- `HSV`：更符合人类对颜色的感知方式，能够直观地描述颜色的外观特性。在图像处理和计算机图形学中，**方便用户**通过调整色调、饱和度和明度来选择和修改颜色。常用于颜色选择器、图像处理软件中的颜色调整工具以及一些需要根据颜色特性进行图像分析和处理的算法中。例如，在图像分割、目标识别等领域，可以根据颜色的 `HSV` 值来提取特定颜色的物体。



#### 视频

不知道大家小时候是否玩过一种动画小人书，连续翻动的时候，小人书的画面就会变成一个动画，类似现在的 `gif` 格式图片。请注意：翻动速度一定要够快。本来是一本静态的小人书，通过翻动以后，就会变成一个有趣的小动画，如果画面够多，翻动速度够快的话，这其实就是一个小视频。

而视频的原理正是如此，由于人类眼睛的特殊结构，画面快速切换(高于每秒10到12帧)时，画面会有残留，感觉起来就是连贯的动作，此现象称之为**视觉暂留**。所以，视频就是由一系列图片构成的。

**视频**（Video）泛指将一系列静态影像以电信号的方式加以捕捉、纪录、处理、储存、传送与重现的各种技术。连续的图像变化每秒超过 24 帧（frame,fps）画面以上时，**根据视觉暂留原理，人眼无法辨别单幅的静态画面**；看上去是平滑连续的视觉效果，这样连续的画面叫做视频。**视频技术**最早是为了电视系统而发展，但现在已经发展为各种不同的格式以利消费者将视频记录下来。网络技术的发达也促使视频的纪录片段以串流媒体的形式存在于因特网之上并可被电脑接收与播放。视频与电影属于不同的技术，后者是利用照相术将动态的影像捕捉为一系列的静态照片。

#### 视频帧

简单的理解**帧**就是视频或动画中的一张画面，如上面的翻页动画书中的一页，就是一帧。一个视频就是由许许多多帧组成的。视频帧又分为I帧、P帧和B帧。

**I帧**：帧内编码帧，大多数情况下I帧就是**关键帧**，它包含了一个完整的图像信息，无需参考其他帧就能被解码和显示。它是视频随机访问的基础，也是视频播放时的起始点。在视频播放时，播放器可以直接从 I 帧开始解码和显示图像，而无需先解码其他帧。同时，I 帧也为后续的 P 帧和 B 帧提供了参考帧，是视频编码中重要的参考点。在视频编辑、快进快退等操作中，I 帧也起到了关键的定位作用。

**P帧**：P 帧是基于前向预测编码的帧，它通过参考前面最近的 I 帧或 P 帧来进行编码。**P 帧只存储当前帧与参考帧之间的差异信息**，即运动向量和残差数据。运动向量表示图像中物体的运动方向和位移，残差数据则是当前帧与参考帧经过运动补偿后仍存在的差异部分。因此，P 帧的编码数据量相对较小。通过利用视频中的时间相关性，P 帧能够有效地减少视频数据量，提高编码效率。在解码时，解码器根据 P 帧的运动向量和残差数据，结合参考帧来重建当前帧的图像。

**B帧**：B 帧是基于双向预测编码的帧，它同时参考前面和后面的 I 帧或 P 帧来进行编码。B 帧不仅考虑了前向的运动信息，还利用了后向的运动信息，能够更准确地描述视频中的运动变化。它存储的是当前帧与前后参考帧之间的差异信息，包括前向运动向量、后向运动向量和残差数据。由于 B 帧可以利用前后两个方向的参考信息，其**编码效率最高**，**数据量通常比 P 帧更小**。不过，由于 B 帧需要参考前后帧进行解码，在视频播放时，它的解码顺序和显示顺序可能不一致，需要额外的缓存和处理来保证正确的播放。

#### 帧率

帧率指的是视频中每秒钟所包含的静止画面（帧）的数量，是用于衡量视频画面更新速度的一个指标，通常用 “**fps**”（Frames Per Second，即每秒帧数）来表示。例如，帧率为 `30fps` 的视频，意味着每秒钟会播放 30 个独立的画面，这些画面以快速连续的方式播放，**利用人眼的视觉暂留现象**，形成流畅的动态视觉效果。

帧率越高，视频画面的流畅度就越高，动作看起来更加自然平滑。高帧率能够捕捉到更多的细节和中间状态，减少画面的卡顿和闪烁感。例如，一些体育赛事的转播或游戏画面，为了呈现更清晰、流畅的动作，可能会采用更高的帧率，如 `60fps` 甚至 `120fps`。

常用帧率：

- `24fps`：一般电影的拍摄及播放帧数是每秒24帧。
- `60fps`：这个帧率对人眼识别来说已经具备较高的平滑度
- `85fps`：人类大脑处理视频的极限，人眼无法分辨更高频率的差异。

在在做页面性能优化时，常用 `60fps` 作为一个基准，所以需要尽量让每一帧的渲染控制在 `16ms` 内，这样才能达到一秒 60 帧的流畅度。

#### 分辨率

分辨率是用于衡量图像、视频或显示屏等视觉内容精细程度的一个指标，它描述了单位长度或单位面积内所包含的像素数量。

**图像分辨率**：通常用**水平像素数**乘以**垂直像素数**来表示，如常见的 1920×1080、3840×2160 等。其中，1920 和 3840 表示水平方向上的像素数量，1080 和 2160 表示垂直方向上的像素数量。像素数量越多，图像就越清晰，能够呈现的细节也就越丰富；反之，像素数量较少的图像在放大后可能会出现模糊、锯齿等现象。

**屏幕分辨率**：指的是显示屏在水平和垂直方向上能够显示的像素点数，同样以**水平像素数** × **垂直像素数**来表示。例如，一台电脑显示器的分辨率为 1920×1080，意味着该显示器在水平方向上可以显示 1920 个像素，垂直方向上可以显示 1080 个像素。高分辨率的屏幕可以显示更多的内容，图像和文字也更加清晰锐利，能够提供更好的视觉体验，但同时也需要更强的图形处理能力来支持。

**打印分辨率**
一般用每英寸点数（Dots Per Inch，DPI）来衡量。它表示在打印输出时，每英寸长度上所打印的点数。例如，300DPI 的打印分辨率意味着在每英寸的长度或宽度上，均匀分布着 300 个打印点。打印分辨率决定了打印图像的质量和细节程度，较高的 DPI 可以使打印出的图像更加细腻、清晰，适合打印高质量的照片、海报等；而对于一些对精度要求不高的文档打印，较低的 `DPI`（如 `150DPI` 或 `200DPI`）通常就可以满足需求。

**视频分辨率**：是指**视频画面**在水平和垂直方向上所包含的像素数量，通常表示为**水平像素数** × **垂直像素数**，它是衡量视频画面精细程度的重要指标。常见的视频分辨率标准

- **标清**（`SD`）：通常指的是 720×576（PAL 制）或 720×480（NTSC 制）的分辨率。这种分辨率在早期的电视广播和一些老的视频设备中较为常见，画面细节相对较少，现在已逐渐被高清及更高分辨率的视频所取代。
- **高清**（`HD`）：主要包括 1280×720(`720P`) 和 1920×1080(`1080P`) 两种分辨率，其中 1920×1080 也被称为全高清（Full HD）。高清分辨率能够提供更清晰的画面和更多的细节，是目前大多数电视节目、网络视频和蓝光光盘所采用的标准分辨率。
- **超高清**（`UHD`）：常见的有 3840×2160（`4K UHD`）和 7680×4320（`8K UHD`）。`4K`分辨率的视频画面非常细腻，能够呈现出极其丰富的细节和逼真的视觉效果，已经广泛应用于电影制作、高端电视节目制作以及一些视频网站的内容播放中。`8K` 分辨率则提供了更高的清晰度和更广阔的视觉体验，但由于其数据量巨大，目前的应用相对较少，主要用于一些专业的影视制作和高端展示领域。

**注意**：**视频精细程度**并不只取决于视频分辨率，还取决于屏幕分辨率。当`720P`的视频在`1080P`的屏幕上播放时，需要将图像放大，放大操作也叫**上采样**。视频播放器可能会自动将 `720P` 的视频画面进行拉伸，以填满 `1080P` 屏幕的整个显示区域。这样做虽然能让视频充满屏幕，但会导致画面在水平和垂直方向上都被放大，画面中的物体和图像会出现变形，影响视觉效果。



## 三、为什么要编码？（压缩编码）

有了视频之后，就涉及到两个问题：存储和传输。那一个视频文件有多大呢？我们来估算一下：

> 以一个分辨率1920×1080，帧率为`30fps`的视频为例：
>
> 一帧数据有1920 * 1080 = 2073600个像素，每个像素点是`24bit`（`RGB`空间）, 则每帧有`2073600*24 / 8 = 6220800byte`，约等于`6MB`，再乘以帧率30，那一秒视频的大小就是`180MB`，1分钟大约是`10800MB`，即`10.5G`，以此类推，一部90分钟的电影大约就是`950GB`.
>
> 

就算咱们现在电脑硬盘是`4TB`的，也放不下几部大片了。这还只是存储一部视频需要的空间，如果再加上要进行传输呢？我们也可以来估算一下：

> 为了传输上述那一个`950GB`(准确的应该是`972000MB`)的视频，我们按照`100Mbps`的网速(`12.5MB/s`)来进行下载，则需要的时间为972000 / 12.5 / 3600 = 21.6小时

我相信大家看到这个数据时，肯定是不能接受的，所以工程师就提出了必须**对视频进行压缩编码**的需求。

在视频领域，**编码**是指将原始视频数据按照一定的规则和算法进行压缩、转换和组织，以便于存储、传输和播放的过程。编码的目的是什么呢？

- **压缩数据**：原始视频数据量非常大，例如一部未经压缩的高清电影可能需要几十甚至上百 GB 的存储空间。通过编码技术，可以去除视频中的冗余信息，如相邻帧之间的相似部分、人眼不易察觉的细节等，将视频数据压缩到原来的几分之一甚至几十分之一，大大减少了存储和传输成本。
- **提高传输效率**：在网络传输中，有限的带宽限制了视频数据的传输速度。编码后的视频数据量变小，能够在相同的带宽条件下更快速地传输，降低了视频卡顿和加载延迟的可能性，提高了用户观看体验。
- **适应不同设备和平台**：不同的播放设备和平台对视频格式和编码标准有不同的要求。编码可以将视频转换为适合各种设备和平台播放的格式，确保视频能够在多种终端上正常播放。

## 四、视频编码相关

#### 视频编码方式

是指将原始视频信号（如未经压缩的 raw 视频数据）通过特定的算法和技术规则，转换为适合存储、传输或播放的数字格式的具体方法。其核心目标是在保证视频质量的前提下，尽可能压缩数据量，同时适配不同设备、网络环境和应用场景的需求。以下是常见的视频编码方式及其分类：

1. **H.26x 系列（`ITU-T `标准）**
  - **H.264/AVC**（Advanced Video Coding）
    特点：高压缩效率、良好的网络适应性（支持流媒体传输）、抗误码能力强，广泛兼容各类设备（手机、电视、电脑等）。
    应用：网络视频（YouTube、Netflix）、蓝光光盘、监控系统、视频会议等，是目前应用最广泛的编码标准。
  - **H.265/HEVC**（High Efficiency Video Coding）
    特点：相比 `H.264` 压缩效率提升约 50%，相同画质下文件体积更小，支持 4K/8K 超高清分辨率和高动态范围（HDR）。
    应用：4K 流媒体（如 `YouTube 4K`）、蓝光 UHD、广播级视频传输，但对设备解码性能要求较高。
  - `H.261/H.263`
    特点：早期标准，压缩效率较低，主要用于视频会议（如 `H.323` 协议）、低带宽场景（如老式监控），目前已逐步被 H.264 取代。

2. **`MPEG` 系列（`ISO/IEC` 标准）**
   - `MPEG-2`
     特点：早期高清标准，支持 `DVD（720p/1080i）`和数字电视广播，压缩效率较低，目前逐渐被 `H.264` 取代。
   - `MPEG-4 Part 2`
     特点：支持动态视频对象编码（如动画、合成内容），曾用于早期网络视频（如 `DivX`、`XviD`），现多用于特定领域（如游戏视频录制）。
   - `MPEG-4 Part 10`（即 `H.264/AVC`）：与 `H.264` 标准合并，属于同一技术的不同命名。

3. **`AV1`（`AOMedia` 开发的开源标准）**
**特点**：目前压缩效率最高的编码标准之一（比 `H.265` 高 20%-30%），完全开源、免专利费，支持多线程处理，适合超高清视频和低带宽传输。
**应用**：`Netflix`、`YouTube` 等平台的 `4K/8K` 流媒体，未来有望成为主流，尤其在需要节省带宽的场景（如移动端、5G 视频）。

4. `VPx` 系列（Google 主导的开源标准）
   - `VP8`
     **特点**：开源、免费，压缩效率接近 `H.264`，编码复杂度较低，适合实时通信（如 `WebRTC`）和网络视频。
     **应用**：早期 `YouTube` 视频、Web 浏览器（`Chrome`）内置支持。
   - `VP9`
     **特点**：在 `VP8` 基础上提升压缩效率 30% 以上，支持 `4K/8K` 和 `HDR`，开源且无专利费。
     **应用**：`YouTube 4K` 视频、Google 旗下服务（如 Google Meet），部分智能电视和流媒体设备支持。



#### 视频文件格式

我们知道 Windows 系统中的文件名都有后缀，例如 `1.doc`，`2.wps`，`3.psd` 等等。Windows 设置后缀名的目的是让系统中的应用程序来识别并关联这些文件，让相应的文件由相应的应用程序打开。例如你双击 `1.doc` 文件，它会知道让 `Microsoft Office` 去打开，而不会用 `Photoshop` 去打开这个文件。所以常见的视频文件格式如 `1.avi`，`2.mpg` 这些都叫做视频的文件格式，它由你电脑上安装的视频播放器关联。



#### 视频封装格式

视频封装格式（Video Container Format）是一种用于将编码后的**视频数据、音频数据、字幕、元信息**（如分辨率、帧率、编码格式） 等组合成一个完整文件的 “容器” 规范。它定义了数据在文件中的存储结构和组织方式，但 不直接涉及音视频的压缩编码过程（区别于 H.264、H.265 等编码格式）。不同封装格式的设计目标不同，有的侧重兼容性，有的侧重多轨道支持，有的适合流媒体传输。以下是常见的视频封装格式及其特点：

1. **`MP4`**（MPEG-4 Part 14）
    **特点**：基于 `MPEG-4` 标准，体积小、兼容性极佳，支持几乎所有设备（手机、电脑、电视）和平台（iOS、Android、网页）。
    支持多种编码（如 `H.264/H.265` 视频、`AAC` 音频）、字幕、元数据，适合在线视频、短视频、移动设备播放。
    **应用**：`YouTube`、抖音、手机拍摄视频默认格式，常见扩展名 `.mp4`。
2.  **`AVI`**（`Audio Video Interleave`）
    **特点**：微软早期推出的经典格式，结构简单，支持多种编码（如 `XviD、DivX`），但不支持高清（最大约 `1080p`）和多轨道（如字幕）。文件体积较大，兼容性较好，但逐渐被 MP4、MKV 取代。
    **应用**：早期网络视频、家庭录像，扩展名 `.avi`。
3.  **`MKV`**（`Matroska Video`）
    **特点**：开源免费，支持几乎所有编码格式（H.264/H.265/AV1 等）、多音轨（如中 / 英双语）、多字幕、章节信息，可封装无损或有损数据。适合存储高清 / 4K 视频、蓝光原盘，文件体积灵活（支持分段、索引），但部分老旧设备兼容性一般。
    **应用**：高清影视资源、DIY 压制视频，扩展名 `.mkv`。
4. **`MOV`**（`QuickTime Movie`）
    特点：
    苹果公司开发的格式，支持高质量视频（如 `ProRes` 编码）、多轨道编辑，常用于影视后期制作（配合 Final Cut Pro）。
    兼容性良好（Windows 需安装 `QuickTime` 组件），扩展名 `.mov`。
5.  **`FLV`**（Flash Video）
    **特点**：早期专为 Flash 播放器设计，适合流媒体实时传输（如直播），文件体积小，支持 `H.264` 编码。
    随着 Flash 淘汰，逐渐被 `MP4`、`WebM` 取代，扩展名 `.flv`。
6. **`WMV`**（Windows Media Video）
    **特点**：微软开发，支持自家`VC-1` 编码，早期用于 Windows Media Player 和在线视频，兼容性局限于 Windows 生态，现逐渐淘汰，扩展名 `.wmv`。



#### 封装格式与编码的关系

视频封装格式是组织音视频数据的 “**容器**”，不负责压缩，而是决定数据如何存储和交互。

>  **封装格式 ≠ 编码格式**：
> 例如，一个 `.mp4` 文件可以包含 `H.264` 编码的视频和 `AAC` 编码的音频；同理，`.mkv` 文件可能包含 `H.265` 视频 + `FLAC` 音频 + 多个字幕轨道。
> **选择依据**：
>
> 1. 兼容性：优先选 `MP4`（通用）、`MOV`（苹果设备）、`MKV`（高清多轨）。
> 2. 用途：流媒体选 `WebM/FLV`，专业后期选 `MOV/MXF`，存储高清选 `MKV`。
> 3. 附加功能：需多字幕 / 音轨选 `MKV`，需交互菜单选 `DVD` 的 `VOB/IFO`。

####  常见视频格式及对应的文件格式

|             视频封装格式             | 视频文件格式 |
| :----------------------------------: | :----------: |
|   `AVI`（Audio Video Interleave）    |    `.avi`    |
|      `WMV`(Windows Media Video)      |    `.wmv`    |
| `MPEG`(Moving Picture Experts Group) |    `.mp4`    |
|              `Matroska`              |    `.mkv`    |
|             `Real Video`             | `.rm .rmvb`  |
|       `QuickTime File Format`        |    `.mov`    |
|            `Flash Video`             |    `.flv`    |



## 五、视频工具

工欲善其事，必先利其器。音视频知识比较复杂，掌握几款常用的音视频工具，对于初学者非常有帮助，可以做到事半功倍。

- `VLC`是一款功能很强大的开源播放器，支持多种常见音视频格式，支持多种流媒体传输协议，也可当作本地流媒体服务器使用。
- `MediaInfo`用来分析视频和音频文件的编码和内容信息。
- `FlvAnalyser`非常简单方便，功能也很强大，包括文件格式分析、数据分析、十六进制分析、时间戳分析、码率分析、音视频同步分析、日志记录、语法指南（`FLV`基本语法）、视频或音频ES提取文件、时间信息提取等。
- `Elecard Stream Analyzer`是一款简单小巧的码流分析工具，通过该软件，用户可以快速地分析视频序列码流。
- `FFmpeg`是一个跨平台的音视频处理库，是一套可以用来记录、转换数字音视频，并能将其转化为流的开源计算机程序。

`vlc`: https://www.videolan.org/vlc/

`MediaInfo`: https://mediaarea.net/en/MediaInfo/Download

`FlvAnalyser`: https://github.com/zymill/flvAnalyser/releases

`FFmpeg`: https://www.videohelp.com/software/ffmpeg/old-versions



# `FFmpeg`版本选择

`FFmpeg `作为开源多媒体框架，不同版本在功能、API 和稳定性上存在差异。以下是关键信息整理及版本选择建议：

---

## 一、`FFmpeg `主要版本差异
1. **`FFmpeg 3.x `系列**  
   - 发布于 2016–2018 年，支持基础编解码器和滤镜，但缺乏现代格式（如 `AV1`）的完整支持。
   - 逐渐被淘汰，但仍存在于旧系统中。

2. `FFmpeg 4.x `系列（2018–2020）
   - 引入 `AV1` 解码、硬件加速（如 `NVIDIA NVENC/NVDEC`）、更多滤镜（如 `libvmaf` 视频质量评估）。
   - `API `调整，部分旧接口被标记为过时。
   - **4.3/4.4 版本** 因长期维护和广泛测试，**成为企业环境的主流选择**。

3. **`FFmpeg 5.x`（2022）及 `6.x`（2023）**  
   - 新增对` H.266/VVC`、`AV1 `硬件编码、更高效的线程模型。
   - 默认启用新` API`（如 `libavcodec` 的新接口），旧代码可能需要适配。
   - 性能优化显著，但初期版本可能存在兼容性问题。

---

## 二、当前最常用版本

- **主流选择**：  
  - **`FFmpeg 4.4`**：因长期支持（`LTS`）和稳定性，广泛用于生产环境（如流媒体服务器、转码工具）。  
  - **`FFmpeg 6.x`**：**新项目首选**，支持最新编解码器（如 `H.266`），但需注意` API `变化。  
  - **`FFmpeg 5.x`**：过渡版本，部分发行版（如` Ubuntu 22.04`）已集成。

- **`FFmpeg 4.3 `是否最稳定？**  
  - 4.3 曾是稳定版本，但后续 4.4 修复了更多关键问题，**推荐优先选择 4.4**。若系统限制（如旧 Linux 发行版），4.3 仍可靠。

---

## 三、版本选择建议
1. **生产环境/企业场景**  
   - 选择 **4.4 LTS**：成熟稳定，兼容性最佳，社区支持广泛。  
   - 避免最新版（如 `6.x`）的初期小版本（如 6.0），等待 6.1 后的小版本修复。

2. **需要最新功能**  
   - 选 **`6.x`**：支持 `H.266`、`AV1` 硬件编码等，适合实验性项目或前沿应用。

3. **兼容性优先**  
   - 检查依赖库（如 `libavcodec`）的 `API `版本，若第三方工具（如 OBS、HandBrake）依赖旧版 FFmpeg，需匹配其要求。

4. **开发新项目**  
   - 使用 **`5.x/6.x`**：避免未来因 `API `过时而重构代码，同时利用性能优化。

5. **Linux 发行版适配**  
   - `Debian/Ubuntu LTS`：默认提供 `4.x`，可通过官方静态构建或源码编译升级。  
   - Arch/Fedora：通常集成较新版本（如 `6.x`）。

---

## 四、升级注意事项
- **测试为重**：升级前在非生产环境验证核心功能（如转码、流切片）。  
- **`API` 变更**：检查 `ffmpeg -h` 参数变化及代码中的弃用警告。  
- **静态构建**：若系统版本过旧，可从 [`FFmpeg `官网](https://ffmpeg.org/download.html) 下载预编译二进制文件，避免依赖冲突。

---

## 总结
- **最稳定版本**：`FFmpeg 4.4`（长期维护） > 4.3。  
- **推荐选择**：  
  - 企业级应用：`4.4 LTS`。  
  - 新项目/开发者：`6.x `最新稳定版（如 6.1）。  
  - 兼容旧系统：`4.3`（次要版本选最高，如 4.3.6）。  

根据实际需求权衡功能与稳定性，并在升级前充分测试。



# `FFmpeg`简介

FFmpeg是一个跨平台的音视频处理库，是一套可以用来记录、转换数字音视频，并能将其转化为流的开源计算机程序，支持Windows、Linux、Mac等。FFmpeg采用LGPL或GPL许可证，提供了录制、转换及流化音视频的完整解决方案，包含非常先进的音视频编 解 码 库 libavcodec 。 为 了 保 证 高 可 移 植 性 和 编 解 码 质 量 ，libavcodec中很多编解码算法都是全新开发的。

### `FFmpeg`的组成

#### 第一部分：工具

`FFmpeg`可以使用已经编译好的程序，一共有三个：

- `ffmpeg.exe` ：这是 `FFmpeg` 工具集中最核心、使用最频繁的工具。它能够对音视频进行格式转换、编解码、剪辑、拼接、添加滤镜特效等多种操作，几乎涵盖了音视频处理的各个方面。
- `ffplay.exe`：这是一个简单且轻量级的音视频播放器，它基于 `FFmpeg` 的库开发而成。能播放多种格式的音视频文件，同时还支持网络流播放，并且可以对播放过程进行一些基本控制，例如暂停、快进、快退等。
- `ffprobe.exe`：主要用于分析音视频文件的元数据信息。这些元数据包括文件的格式、时长、帧率、分辨率、音频采样率、声道数、编码格式等，对于了解音视频文件的基本属性和技术参数非常有帮助。

我们以`ffmpeg-4.4.1`版本的目录结构为例：

![image-20250408110632859](.\assets\image-20250408110632859.png)

#### 第二部分：`SDK`

`FFmpeg` 提供了多个强大的 `SDK`（软件开发工具包），这些 `SDK` 以库的形式存在，为开发者在不同的应用场景下进行音视频开发提供了丰富的功能。以下是主要的几个 `SDK`：

- `libavutil`：这是一个通用的工具库，提供了许多基础的功能和数据结构，如内存管理、数学运算、随机数生成、时间处理、日志系统等。它是其他 `FFmpeg` 库的基础支撑库，为音视频处理提供了底层的辅助功能。
- `libavcodec`：这是 `FFmpeg` 的<span style=color:red;background:yellow>**核心编解码库**</span>，支持大量的音视频编码和解码标准，包括 `H.264`、`H.265`、`AV1`、`MP3`、`AAC`等。它可以对原始的音视频数据进行编码，将其转换为适合存储或传输的压缩格式；也可以对压缩后的音视频流进行解码，还原为原始的音视频数据。
- `libavformat`：该库负责处理音视频文件和流的格式。它支持众多的音视频格式，如常见的 MP4、AVI、MKV、FLV 等，以及流媒体协议，像 `RTMP`、`RTSP`、`HTTP` 等。能够完成文件的打开、关闭、解析文件头和元数据，还能进行流的复用（将编码后的音视频流合并到一个文件或流中）和解复用（将一个文件或流中的音视频流分离出来）操作。
- `libavdevice`：该库是 `FFmpeg` 中用于处理音视频输入输出设备的库。它允许开发者直接与各种硬件设备进行交互，实现从设备采集音视频数据，或者将处理后的音视频数据输出到设备上。
- `libavfilter`：该库提供了丰富的音视频滤镜功能，可以对音视频进行各种特效处理，如添加水印、调整亮度对比度、进行模糊处理、音频降噪等。它支持链式滤镜操作，允许开发者将多个滤镜组合在一起，实现复杂的音视频处理效果。
- `libswscale`：该库主要用于图像的缩放、色彩空间转换等操作。在不同的设备和应用场景中，图像的分辨率和色彩空间可能不同，`libswscale` 可以将一种分辨率和色彩空间的图像转换为另一种，以满足不同的需求。
- `libswresample`：该库主要用于音频的重采样和声道转换。不同的音频设备和应用可能对音频的采样率、声道数等参数有不同的要求，`libswresample` 可以将一种采样率和声道数的音频数据转换为另一种。
- `libpostproc`：该库是 `FFmpeg` 中用于进行后期处理的库。它主要对解码后的音视频数据进行一系列的优化和增强处理，以提高音视频的质量。

![image-20250408110055000](.\assets\image-20250408110055000.png)

#### 第三部分：源码

`FFmpeg`也提供了源码，无论是第一部分的工具软件，还是第二部分的`SDK`，都是由这些源码编译出来的。`FFmpeg`的源代码是由C语言实现，主要在Linux平台上进行开发，它不是一个孤立的工程，它还存在多个依赖的第三方工程来增强它自身的功能。当前的课程中，我们暂时不会涉及太多源码相关的内容，主要以`FFmpeg`的工具软件和`SDK`调用为主。

我们以`ffmpge-4.4.5`版本的源代码目录结构为例：

![image-20250408110256788](.\assets\image-20250408110256788.png)

进入`libavcodec`目录，就能看到其相关的源文件

![image-20250408110357997](.\assets\image-20250408110357997.png)



### `FFmpeg`主要功能

多媒体视`FFmpeg` 是一个强大且广泛使用的开源音视频处理工具集，由一系列命令行工具和库组成，在音视频开发、流媒体、影视制作等领域都有重要应用。以下是它的主要功能：

1. 视频采集功能：ffmpeg视频采集功能非常强大，不仅可以采集视频采集卡或摄像头的图像，还可以进行屏幕录制，同时还支持以RTP方式将视频流穿给支持RTSP的流媒体服务器，支持直播应用。

2. 格式转换功能：

   - 音频格式转换：能够将常见的音频格式如 `WAV`、`MP3`、`AAC`、`FLAC` 等相互转换。例如，将无损的 WAV 格式转换为便于网络传播的 `MP3` 格式。比如：

     ```shell
     ffmpeg -i input.wav -c:a libmp3lame -q:a 2 output.mp3
     ```

   - 视频格式转换：支持 `AVI`、`MP4`、`MKV`、`FLV `等多种视频格式之间的转换。比如，将 `AVI` 格式视频转换为 `MP4` 格式：

     ```shell
     ffmpeg -i input.avi -c:v libx264 -c:a aac output.mp4
     ```

     

3. 视频剪辑和拼接功能：

   - 视频剪辑：可以根据时间点对视频进行裁剪，只保留所需的片段。例如，截取视频从第 10 秒到第 30 秒的片段。

     ```shell
     ffmpeg -i input.mp4 -ss 00:00:10 -to 00:00:30 -c copy output.mp4
     ```

   - 视频拼接：将多个视频文件按顺序拼接成一个完整的视频。需要先创建一个包含所有视频文件路径的文本文件 `list.txt`，内容如下：

     ```shell
     file 'video1.mp4'
     file 'video2.mp4'
     ```

     然后使用以下命令进行拼接：

     ```shell
     ffmpeg -f concat -safe 0 -i list.txt -c copy output.mp4
     ```

     

4. 流媒体处理：

   - 推流：将本地的音视频文件推送到流媒体服务器，如使用 `RTMP` 协议将视频推送到直播平台。

     ```shell
     ffmpeg -re -i input.mp4 -c copy -f flv rtmp://server/live/stream_key
     ```

   - 拉流：从流媒体服务器获取音视频流并进行处理或保存，例如从 `RTSP` 流中拉取视频并保存为本地文件。

     ```shell
     ffmpeg -i rtsp://server/stream -c copy output.mp4
     ffmpeg -i rtsp://server/stream -t 60 -c copy output.mp4 #录制60秒
     ffmpeg -i rtsp://server/stream -frames:v 1500 -c copy output.mp4#录制1500帧
     ```
     
     

5. 音频处理：

   - 音频提取：从视频文件中提取音频，例如从 `MP4` 视频中提取音频保存为 `MP3` 格式。

     ```shell
     ffmpeg -i input.mp4 -vn -c:a libmp3lame -q:a 2 output.mp3
     ```

   - 音频混音：将多个音频文件混合成一个音频文件。

   - 音频增益调整：可以增加或减小音频的音量。

   

6. 图像与视频互转功能：

   - 视频转图像序列：将视频的每一帧提取出来保存为图像文件，如将视频转换为 `JPEG` 图像序列：

     ```shell
     ffmpeg -i input.mp4 output_%03d.jpg
     ```
   - 图像转视频序列：将一系列图像文件合成一个视频，例如将 JPEG 图像序列合成 MP4 视频：

     ```shell
     ffmpeg -framerate 25 -i input_%03d.jpg -c:v libx264 -pix_fmt yuv420p output.mp4
     ```



### `FFmpeg`下载和安装（Windows版本）

下载页面：

1. https://www.videohelp.com/software/ffmpeg/old-versions
2. https://github.com/ShiftMediaProject/FFmpeg/releases



# 视频播放原理

视频播放涉及到声音的采集，编码压缩，封装格式，协议传输，到用户侧的时候，选择播放器和播放形式，一个完整的视频过程是端到端的，如下图所示：

![](.\assets\stream_media.png)



| 环节                 | 概括                                                 |
| :------------------- | :--------------------------------------------------- |
| **音视频源**         | 摄像头、麦克风、胶片等采集原始信号                   |
| **编码/压缩**        | 把原始信号压缩成 H.264/AAC 等格式（变小）            |
| **封装**             | 把压缩后的音视频打包成 MP4/FLV/TS 等容器格式（装盒） |
| **协议**             | 通过 RTMP/HLS/RTSP 等协议传输（上路）                |
| **服务器/软件/厂商** | 接收流并转发、存储、转码（中转站）                   |
| **云平台**           | 分发到用户（送达）                                   |
| **播放器**           | 解协议 → 解封装 → 解码 → 渲染显示（拆包还原）        |





**视频播放器**播放本地视频文件或互联网上的流媒体文件大概需要解协议、解封装、解码、同步、渲染等几个步骤：

![image-20250410180532495](.\assets\image-20250410180532495.png)





## 一、解协议

解协议是指将流媒体协议的数据，解析为标准的封装格式数据。音视频在网络上传播的时候，常采用各种流媒体协议，例如`HTTP`、`RTMP`、`RTSP`等。这些协议在传输音视频数据的同时，也会传输一些信令数据。这些信令数据包括对播放的控制（播放、暂停、停止），或者对网络状态的描述等。在解协议的过程中会去除信令数据而只保留音视频数据。例如采用`RTMP`协议传输的数据，经过解协议操作后，输出`FLV`格式的数据。**注意**：“文件”本身也是一种协议。

## 二、解封装

解封装是指将输入的封装格式的数据，分离成为音频流压缩编码数据和视频流压缩编码数据。

封装格式种类有很多，例如`MP4`、`MKV`、`RMVB`、`TS`、`FLV`、`AVI`等，其作用就是将已经压缩编码的视频数据和音频数据按照一定的格式放到一起。

例如FLV格式的数据，经过解封装操作后，输出`H.264`编码的视频码流和`AAC`编码的音频码流。

## 三、解码

解码是指将视频/音频压缩编码数据，解码成为非压缩的视频/音频原始数据。音频的压缩编码标准包含`AAC`、`MP3`、`AC-3`等，视频的压缩编码标准则包含`H.264`、`MPEG-2`、`VC-1`等。解码是整个系统中最重要也是最复杂的一个环节。通过解码，压缩编码的视频数据输出成为非压缩的颜色数据，例如`YUV420P`、`RGB`等。压缩编码的音频数据输出成为非压缩的音频抽样数据，例如`PCM`数据。

## 四、同步

根据解封装模块在处理过程中获取的参数信息，同步解码出来的视频和音频数据，被送至系统的显卡和声卡播放出来。为什么需要音视频同步呢？媒体数据经过解复用流程后，音频/视频解码便是独立的，也是独立播放的，而在音频流和视频流中，其播放速度是由相关信息指定的，例如视频是根据帧率，音频是根据采样率。从帧率及采样率即可知道视频/音频播放速度。**声卡**和**显卡**均是以一帧数据来作为播放单位，如果单纯依赖帧率及采样率进行播放，在理想条件下，应该是同步的，不会出现偏差。

下面以一个`44.1kHz`的`AAC`音频流和`24fps`的视频流为例来说明。如果一个`AAC`音频frame每个声道包含1024个采样点，则一个frame的 播 放 时 长 为 `(1024/44100)×1000ms≈23.27ms` ， 而 一 个视频frame播放时长为`1000ms/24≈41.67ms`。理想情况下，音视频完全同步，但实际情况下，如果用上面那种简单的方式，慢慢地就会出现音视频不同步的情况，要么是视频播放快了，要么是音频播放快了。可能的原因包括：一帧的播放时间难以精准控制；音视频解码及渲染的耗时不同，可能造成每一帧输出有一点细微差距，长久累计，不同步便越来越明显；音频输出是线性的，而视频输出可能是非线性的，从而导致有偏差；媒体流本身音视频有差距（特别是TS实时流，音视频能播放的第1个帧起点不同），所以解决音视频同步问题引入了时间戳，它包括几个特点：首先选择一个参考时钟（要求参考时钟上的时间是线性递增的），编码时依据参考时钟给每个音视频数据块都打上时间戳。播放时，根据音视频时间戳及参考时钟来调整播放，所以视频和音频的同步实际上是一个动态的过程，同步是暂时的，不同步则是常态。





# `FFmpeg`命令行实践

查看`ffmpeg`的版本

```shell
C:\Users\34776>ffmpeg -version                                                                          ffmpeg version 4.4.1-full_build-www.gyan.dev Copyright (c) 2000-2021 the FFmpeg developers              built with gcc 11.2.0 (Rev1, Built by MSYS2 project)                                                                    
configuration: --enable-gpl --enable-version3 --enable-static --disable-w32threads --disable-autodetect --enable-fontconfig --enable-iconv --enable-gnutls --enable-libxml2 --enable-gmp --enable-lzma --enable-libsnappy --enable-zlib --enable-librist --enable-libsrt --enable-libssh --enable-libzmq --enable-avisynth --enable-libbluray --enable-libcaca --enable-sdl2 --enable-libdav1d --enable-libzvbi --enable-librav1e --enable-libsvtav1 --enable-libwebp --enable-libx264 --enable-libx265 --enable-libxvid --enable-libaom --enable-libopenjpeg --enable-libvpx --enable-libass --enable-frei0r --enable-libfreetype --enable-libfribidi --enable-libvidstab --enable-libvmaf --enable-libzimg --enable-amf --enable-cuda-llvm --enable-cuvid --enable-ffnvcodec --enable-nvdec --enable-nvenc --enable-d3d11va --enable-dxva2 --enable-libmfx --enable-libglslang --enable-vulkan --enable-opencl --enable-libcdio --enable-libgme --enable-libmodplug --enable-libopenmpt --enable-libopencore-amrwb --enable-libmp3lame --enable-libshine --enable-libtheora --enable-libtwolame --enable-libvo-amrwbenc --enable-libilbc --enable-libgsm --enable-libopencore-amrnb --enable-libopus --enable-libspeex --enable-libvorbis --enable-ladspa --enable-libbs2b --enable-libflite --enable-libmysofa --enable-librubberband --enable-libsoxr --enable-chromaprint                                                                                                                  
libavutil      56. 70.100 / 56. 70.100              
libavcodec     58.134.100 / 58.134.100                                                                  libavformat    58. 76.100 / 58. 76.100                                                                  libavdevice    58. 13.100 / 58. 13.100                                                                  libavfilter     7.110.100 /  7.110.100                                                                  libswscale      5.  9.100 /  5.  9.100                                                                  libswresample   3.  9.100 /  3.  9.100                                                                  libpostproc    55.  9.100 / 55.  9.100
```



## 一、主要选项

```shell
-f  fmt(input/output) 指定输入或输出文件格式（封装格式，视频容器）。
	常规可省略，而使用依据扩展名(文件的前几百K的内容，智能分析)的自动指定
	但一些选项需要强制明确设定。
	(视频: mp4/avi/mov)
	(音频: mp3/wav/aac)
	(流媒体格式: flv/rtsp/hls)
	(图形序列格式: image2)
-i 	filename(input) 指定输入文件。
-y  (global) 默认自动覆盖输出文件，而不再询问确认。
-n 	(global) 不覆盖输出文件，如果输出文件已经存在则立即退出。
-t	duration (input/output) 限制输入/输出的时间。
	如果是在 -i 前面，就是限定从输入中读取多少时间的数据；
	如果是用于限定输出文件，则表示写入多少时间数据后就停止。 
	duration可以是以秒为单位的数值或以 hh:mm:ss[.xxx]格式的时间值。
	注意 -to 和 -t 是互斥的，-t有更高优先级。
-to position(output) 只写入position时间后就停止。
	position 可以是以秒为单位的数值或以 hh:mm:ss[.xxx]格式的时间值。
-ss position(input/output)
	当在 -i 前，表示定位输入文件到 position 指定的位置。
	注意可能一些格式是不支持精确定位的，
	所以ffmpeg可能是定位到最接近 position（在之前）的可定位点。
	position 可以是以秒为单位的数值或以 hh:mm:ss[.xxx]格式的时间值。
-codec[:stream_specifier] codec (input/output,per-stream) 
	为特定的文件选择编解码模式。
	对于输出文件就是编码器，对于输入或者某个流就是解码器。
	选项参数中 codec 是编解码器的名字，或者是 copy（仅对输出文件），则意味着流数据直接复制而不再编码。
```



## 二、视频选项

```shell
-vframes number(output) 设置输出文件的帧数，是-frames:v的别名
-r[:stream_specifier] fps (input/output, per-stream) 设置帧率（一种Hz值，缩写或者分数值）
-s[:stream_specifier] size (input/output, per-stream) 设置帧的尺寸
-vn(output) 禁止输出视频 n:no
-vcodec codec(output) 设置视频编码器，这是 -codec:v的一个别名
aspect[:stream_specifier] aspect(output,per-stream) 指定视频的纵横比（长宽显示比例）。
	aspect是一个浮点数字符串或者num:den格式字符串（其值就是num/den）
	例如"4:3","16:9","1.3333"及"1.7777" 都是常用参数值
	
```



## 三、音频选项

```shell
-aframes number(output)	设置 number 音频帧输出，是 -frames:a 的别名。
-ar[:stream_sepcifier] freq (input/output, per-stream) 设置音频采样率，默认是输出同于输入。
	对于输入进行设置，仅仅通道是真实的设备或者raw数据分离出并映射的通道才有效。
	对于输出则可以强制设置音频量化的采样率。
-aq q(output) 设置音频品质（编码指定为VBR），它是 -q:a的别名。
-ac[:stream_specifier] channnels (inupt/output, per-stream) 设置音频通道数，默认输出会有输入相同的音频通道。
	对于输入进行设置，仅仅通道是真实的设备或者 raw 数据分离出并映射的通道才有效。
-an(output) 禁止输出音频。
-acodec codec(input/output) 设置音频解码/编码的编、解码器，是 -codec:a 的别名。
```



## 四、字幕选项

```shell
-scodec codec(input/output) 设置字幕解码器，是 -codec:s的别名。
-sn(output) 禁止输出字幕
	s: subtitle 字幕的意思
	字幕格式：srt, ass, ssa, ...
```

## 五、测试用例

### 1. 获取视频信息

```shell
ffmpeg -i test.mp4
```

![image-20250413153034070](.\assets\image-20250413153034070.png)

### 2. 将`MP4`视频编转码成`flv`视频

```shell   
ffmpeg -i test.mp4 -vcodec copy -acodec copy -f flv test.flv
```

![image-20250413155058132](.\assets\image-20250413155058132.png)

### 3. 抽取视频中的音频

```shell
ffmpeg -i test.mp4 -vn -y -acodec copy test.aac
```

![image-20250413160845846](.\assets\image-20250413160845846.png)

### 4. 将`AAC`音频格式转换成`MP3`格式

```shell
# 直接对音频文件进行格式转换
ffmpeg -i test.aac -acodec libmp3lame test.mp3
```

![image-20250413162529788](.\assets\image-20250413162529788.png)

```shell
# 将视频文件中的音频格式由AAC格式转换为MP3格式
ffmpeg -i test.mp4 -vcodec copy -acodec libmp3lame output.flv
```



###  5. 抽取视频中的视频

```shell
# 只要视频，不要音频
ffmpeg -i test.mp4 -vcodec copy -an output_an.mp4
ffmpeg -i test.mp4 -vcodec copy -an -y output2_an.avi

# 只要视频中的一部分，从视频中的10s位置开始，截取20s的视频，保存到文件中
ffmpeg -ss 10 -i test.mp4 -t 20  -vcodec copy -acodec copy test10s1_t.mp4
ffmpeg -ss 10 -i test.mp4 -to 20  -vcodec copy -acodec copy test10s2_to.mp4
```



### 6. 音频+视频合成

```shell
ffmpeg -i output2_an.mp4 -i test.mp3 -acodec copy -vcodec copy -f mp4 output_mix.mp4
```

![image-20250413165540967](.\assets\image-20250413165540967.png)

```shell
# 希望某个输入流延迟输出，可以使用itsoffset(input timestamp offset)参数
ffmpeg -i input_test_1.mp4 -itsoffset 10 -i input_test_2.mp3 -vcodec copy -acodec copy
output_test.mp4

# 视频从20s之后开始，播放5秒，音频持续播放
ffmpeg -ss 20 -t 5 -i input_test_1.mp4 -i input_test_2.aac -vcodec copy -acodec copy
output_test.mp4

# 音频从20s之后开始，播放5秒，视频持续播放
ffmpeg -ss 20 -t 5 -i input_test_1.mp3 -i input_test_2.mp4 -vcodec copy -acodec copy
output_test.mp4
```



### 7. 视频分离成图片

```shell
# -r  	设置帧率，表示每秒处理或输出的帧数
# -r 1 	表示1帧每秒，即会每秒从视频中提出1帧画面。
# %02d 	是一个格式化字符串，d 表示以十进制整数形式输出序号，
#		02 表示这个序号会用两位数字表示，不足两位时前面补 0。
ffmpeg -i testtime_t.mp4 -r 1 -f image2 output_image_%02d.jpeg
```



### 8. 图片合成视频

```shell
# 默认情况下是25fps
ffmpeg -f image2 -i output_image_%02d.jpeg output_image2video_1.mp4

# 指定帧率 5fps
ffmpeg -r 5 -f image2 -i output_image_%02d.jpeg output_image2video_2.mp4
```



### 9. 图片生成`gif`动图

```shell
# 这个好玩，大家可以多多尝试
ffmpeg -f image2 -i output_image_%02d.jpeg -r 5 output_image.gif
```



### 10. 修改视频分辨率

```shell
ffmpeg -i lldq-tkdt.mp4 -s 640x480 -vcodec libx264 -acodec aac lldq-test480.mp4
```



# 推流拉流协议选择

在音视频流媒体领域，**`RTMP`（实时消息传输协议）**和**`RTSP`（实时流传输协议）**是两种常用的协议，分别适用于不同的场景。以下从功能对比、应用场景（推流/拉流选择）等方面详细解析：

### 一、`RTMP`（Real-Time Messaging Protocol）  

`RTMP`是`Real Time Messaging Protocol`（实时消息传输协议）的首字母缩写。该协议基于`TCP`，是一个协议族，包括`RTMP`基本协议及`RTMPT/RTMPS/RTMPE`等多种变种。`RTMP`是一种设计用来进行实时数据通信的网络协议，主要用来在`Flash/AIR`平台和支持`RTMP`协议的流媒体/交互服务器之间进行音视频和数据通信。支持该协议的软件包括`Adobe Media Server/Ultrant Media Server/red5`等。`RTMP`与`HTTP`一样，都属于`TCP/IP`四层模型的应用层。



#### **核心功能与特点**  
1. **设计目标**：  
   - 由`Adobe`开发，专为**实时音视频传输**设计，支持**推流（上传流到服务器）**和**拉流（从服务器获取流）**，广泛用于**直播、短视频平台**（如早期的抖音、`YouTube`直播）。  
   - 基于**TCP**传输，保证数据可靠性，延迟较低（通常在1-3秒），适合实时互动场景（如弹幕、连麦）。  

2. **核心功能**：  
   - **推流支持**：客户端（如`OBS`、摄像头）通过`RTMP`将音视频数据推送到流媒体服务器（如`Nginx RTMP、Wowza`）。  
   - **拉流支持**：播放器（如`Flash Player`、部分`HTML5`播放器）通过`RTMP`从服务器拉取流并播放。  
   - **简单易用**：无需复杂的会话建立过程，一次握手即可传输，协议封装了音视频编码数据（常搭配`H.264/AAC`）。  

3. **优缺点**：  
   - **优点**：兼容性强（支持主流`CDN`和工具）、低延迟、适合直播推流。  
   - **缺点**：依赖`Adobe Flash`（浏览器支持逐渐淘汰，需插件或转码）、不支持时移（快进/后退）、加密能力较弱。  

#### **典型应用**  
- 推流端：直播软件（`OBS`）、摄像头设备向服务器推送流。  
- 拉流端：`PC`端`Flash`播放器、部分移动端`APP`（通过`SDK`支持）。  

### 二、`RTSP`（Real-Time Streaming Protocol）  

RTSP（Real Time Streaming Protocol），`RFC2326`，实时流传输协议，是`TCP/IP`协议体系中的一个应用层协议，由哥伦比亚大学、网景和`RealNetworks`公司提交的`IETF RFC`标准。该协议定义了一对多应用程序如何有效地通过IP网络传送多媒体数据。`RTSP`在体系结构上位于`RTP`和`RTCP`之上，它使用`TCP`或`UDP`完成数据传输。`HTTP`与`RTSP`相比，`HTTP`请求由客户机发出，服务器作出响应；使用`RTSP`时，客户机和服务器都可以发出请求，**即RTSP可以是双向的**。`RTSP`是用来控制声音或影像的多媒体串流协议，并允许同时多个串流需求控制，传输时所用的网络通讯协定并不在其定义的范围内，服务器端可以自行选择使用`TCP`或`UDP`来传送串流内容，它的语法和运作跟`HTTP 1.1`类似，但并不特别强调时间同步，所以比较能容忍网络延迟。而前面提到的允许同时多个串流需求控制（`Multicast`），除了可以降低服务器端的网络用量，更进而支持多方视讯会议（`Video Conference`）。因为与`HTTP1.1`的运作方式相似，所以代理服务器〈`Proxy`〉的快取功能〈`Cache`〉也同样适用于`RTSP`，并因`RTSP`具有重新导向功能，可视实际负载情况来转换提供服务的服务器，以避免过大的负载集中于同一服务器而造成延迟。

#### **核心功能与特点**  
1. **设计目标**：  
   - 基于**客户端-服务器架构**的**控制协议**，定义了如何建立、控制流媒体会话（如播放、暂停、快进），类似`HTTP`的“请求-响应”模式，但专为音视频流设计。  
   - **RTSP**不直接传输数据，需搭配**RTP（实时传输协议）**和**RTCP（实时控制协议）**：  
     - **RTP**负责传输音视频数据（通常基于**UDP**，低延迟但不保证可靠）；  
     - **RTCP**负责监控传输质量、同步时钟。  

2. **核心功能**：  
   - **会话控制**：支持播放（PLAY）、暂停（PAUSE）、停止（TEARDOWN）、 Seek（定位时间点）等操作，适合**点播、时移电视、监控视频**。  
   - **多传输方式**：可选择UDP（低延迟）或TCP（可靠传输，适合防火墙环境）。  
   - **灵活的媒体协商**：通过SDP（会话描述协议）协商编码格式、分辨率等参数。  

3. **优缺点**：  
   - **优点**：支持交互控制、适合按需拉流（如视频点播）、低延迟（UDP模式）、无插件依赖（VLC等播放器原生支持）。  
   - **缺点**：协议复杂度高（需处理RTP/RTCP）、推流功能较弱（主要用于拉流控制）、部分CDN不支持。  

#### **典型应用**  
- 拉流端：VLC播放器、监控系统（如摄像头通过**RTSP**提供实时流）、视频会议系统。  
- 控制场景：需要快进/后退、暂停的点播场景，或实时查看监控画面。  


### 三、推流与拉流的协议选择对比  
|         **场景**         |                          **`RTMP`**                          |                          **`RTSP`**                          |
| :----------------------: | :----------------------------------------------------------: | :----------------------------------------------------------: |
| **推流（上传到服务器）** | **首选**：简单高效，主流直播工具（`OBS`）默认支持，`CDN`广泛兼容。 | 较少使用：`RTSP`主要用于拉流控制，推流需额外配置（如`RTSP`转`RTMP`）。 |
| **拉流（从服务器获取）** | 适合**实时直播**（低延迟、无交互控制），但依赖客户端支持（如`APP`或特定播放器）。 | **首选**：适合**需要交互控制的场景**（点播、监控、时移），支持Seek等操作，兼容性强（`VLC`、`FFmpeg`原生支持）。 |
|         **延迟**         |               较低（1-3秒，`TCP`保证可靠性）。               | 可变：`UDP`模式延迟更低（约`500ms-1`秒），`TCP`模式稍高但可靠。 |
|        **兼容性**        | 传统直播场景（如`CDN`）支持好，但浏览器`Flash`淘汰后需转`HLS/RTSP/WebRTC`。 |   播放器（`VLC`、`FFmpeg`）和监控设备支持极佳，无需插件。    |
|     **适用场景举例**     |             网络直播、短视频实时上传、电商直播。             | 视频点播（如拖进度条）、安防监控（实时查看/控制摄像头）、视频会议。 |


### 四、总结：如何选择？  
1. **推流优先选`RTMP`**：  
   - 如果你需要将音视频从设备（摄像头、直播软件）上传到服务器，`RTMP`是最便捷的选择，尤其是对接主流`CDN`和直播平台（如抖音、B站直播推流地址通常为`RTMP URL`）。  

2. **拉流分场景选择**：  
   - **实时直播（低延迟、无交互）**：`RTMP`或`RTSP`（`UDP`模式），但`RTMP`更简单。  
   - **点播/交互场景（快进、暂停）**：`RTSP`是更好的选择，支持精准的时间定位和会话控制。  
   - **浏览器播放**：`RTMP`依赖`Flash`已过时，建议转用**`HLS（.m3u8）`**或**`WebRTC`**（无插件，超低延迟），但`RTSP`可通过`FFmpeg`等工具转码后播放。  

3. **特殊场景**：  
   - 监控设备（如`IP`摄像头）通常原生支持`RTSP`拉流，直接通过`RTSP URL`获取实时画面。  
   - 旧系统兼容：若遗留系统依赖`RTMP`，可继续使用；新项目建议考虑更现代的协议（如`WebRTC`用于实时互动，`HLS`用于点播）。  

总之，**推流选`RTMP`，拉流根据是否需要交互控制和延迟要求选`RTSP`或`RTMP`**，同时结合具体设备、平台的兼容性和功能需求综合判断。



# `FFmpeg`处理流媒体

## 组合一：基于`RTSP`，`VLC`推流 + `FFmpeg`拉流

### `VLC`推流服务器的设置

#### 1. 打开`vlc`播放器

打开`vlc`播放器后，在菜单栏中选择“媒体”-> “流”，打开流设置窗口

<img src=".\assets\image-20250414100519357.png" alt="image-20250414100519357" style="zoom:50%;" />

#### 2. 流设置窗口

流设置窗口一共有4种方式获取媒体信息，分别是从文件、光盘、网络、捕获设备中获取信息。我们选择第一种，通过文件获取即可。

<img src=".\assets\image-20250414101345796.png" alt="image-20250414101345796" style="zoom: 50%;" />

选择一个媒体文件后，点击“打开”按钮，进入以下的“串流”界面。

#### 3. 点击“串流”

<img src=".\assets\image-20250414101547990.png" alt="image-20250414101547990" style="zoom:50%;" />

选择好文件后，点击“串流”，进行流的设置。

<img src=".\assets\image-20250414101912567.png" alt="image-20250414101912567" style="zoom:50%;" />

#### 4. 设置流的格式

<img src=".\assets\image-20250414102112255.png" alt="image-20250414102112255" style="zoom:50%;" />

选择了`RTSP`的流协议后，再点击右边的“添加”，会看到如下的窗口：

<img src=".\assets\image-20250414102336094.png" alt="image-20250414102336094" style="zoom:50%;" />

点击“下一步”后，会看到“转码设置”界面

<img src=".\assets\image-20250414102617820.png" alt="image-20250414102617820" style="zoom:50%;" />

点击“下一个”后，会看到“选项设置”界面

>  这里的转码参数分别代表的含义如下：
>
> vcodec=h264    		 视频流为h264
>
> vb=800  				视频的码率为800kb/s
>
> acodec=mpga	   	音频的编码，这里是MP3
>
> ab=128			 	音频的码率为128kb/s
>
> channels=2			2个声道
>
> samplerate=44100	 采样率为44.1kHz
>
> scodec=none		    无字幕



<img src=".\assets\image-20250414102901904.png" alt="image-20250414102901904" style="zoom:50%;" />

点击“流”之后，流媒体服务器就设置成功了。

<img src=".\assets\image-20250414103015096.png" alt="image-20250414103015096" style="zoom:50%;" />



### `FFmpeg`充当客户端拉流播放

```shell
ffplay -i rtsp://127.0.0.1:8554/rtsp_test1
```

![image-20250414112543549](.\assets\image-20250414112543549.png)



### `FFmpeg`拉流存储到本地

```shell
ffmpeg -i rtsp://localhost:8554/rtsp_test1 -c copy -f mpegts ts10.ts
```



### `VLC`充当客户端拉流播放

<img src=".\assets\image-20250414112746590.png" alt="image-20250414112746590" style="zoom:50%;" />

点击后出现以下新的窗口：

<img src=".\assets\image-20250414112935460.png" alt="image-20250414112935460" style="zoom:50%;" />

如果`rtsp`地址是正确的，就可以看到视频流可以正常播放了。



## 组合二：基于`RTP`， `VLC`推流 + `FFmpeg`拉流

### `VLC`推流服务器设置

#### 设置流的格式

前三个步骤与上面的基于`RTSP`的设置是一样的，只是在“设置流的格式”时有所不同：

<img src=".\assets\image-20250414121422166.png" alt="image-20250414121422166" style="zoom:50%;" />

设置`RTP`的信息

<img src=".\assets\image-20250414121702653.png" alt="image-20250414121702653" style="zoom:50%;" />

点击下一步后，操作与之前的保持一致。

<img src=".\assets\image-20250414121946411.png" alt="image-20250414121946411" style="zoom:50%;" />



### `FFmpeg`充当客户端拉流播放

```shell
ffplay -i rtp://127.0.0.1:5004/rtp1
```

![image-20250414121137361](.\assets\image-20250414121137361.png)



## `FFmpeg`直接播放摄像头的`rtsp`流

```shell
ffplay -i rtsp://admin:admin@192.168.1.111/live/chn=1
```



## `FFmpeg`直接播放摄像头的`rtmp`流

```shell
ffplay -i rtmp://192.168.1.111:1935/hlsram/live1
```

