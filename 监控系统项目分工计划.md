# 智能家居监控系统__小组分工计划

> 制定日期：2026 年 9 月  
> 适用前提：已有 Reactor + ThreadPool 基础代码，Ubuntu 22.04、MySQL、Qt 5.14.2、FFmpeg 4.4 开发环境已准备好。

## 一、交付目标

只完成能够现场演示的 MVP 主链路：

```text
Qt 注册/登录
   → Server + TLV + MySQL
   → 获取设备列表
   → Server 转发一路 RTSP/RTMP
   → Qt FFmpeg 实时预览
   → 云台控制
   → 录像开关、查询和回放
```

### 本次必须完成

- 服务器配置、日志、Reactor + ThreadPool、TLV 通信；
- MySQL 用户表、设备表、录像表；
- 注册、登录、设备列表；
- 一路实时流经过服务器传输到 Qt；
- 云台八方向控制请求经过服务器；
- 录像切片、元数据索引和至少一段录像回放。

### 本次暂缓

多路高并发、复杂 HLS、多用户权限、NAS/RAID、智慧厨房真实硬件接入等功能，作为后续扩展。

---

## 二、四个人最终分别完成什么

> 每个人都必须有“后端代码 + 客户端代码 + 联调成果”，不能只负责 Qt 页面。

| 成员 | Linux服务端任务 | QT客户端任务 | 最终交付结果 |
|---|---|---|---|
| **A：服务器架构与网络** | 配置、日志、Reactor、ThreadPool、任务分发、连接生命周期、流会话管理 | Qt/Linux C 网络层、TLV 收发、超时、断线重连、资源释放 | Server 可启动和稳定运行，客户端可正常连接、重连和退出 |
| **B：协议、数据库与认证** | TLV 消息定义、MySQLClient、用户/设备/录像表、注册、登录、设备和录像接口 | Qt 注册登录、登录状态、设备列表、录像查询模型 | 完成注册 → 登录 → 设备列表 → 录像查询闭环 |
| **C：FFmpeg、实时流与录像** | FFmpeg 拉流、媒体包序列化、RingBuffer、实时转发、TS 切片、断流重连 | Qt 媒体包重组、FFmpeg 解码、帧队列、实时显示、录像播放 | 至少一路“摄像头 → Server → Qt”可播放，并生成录像文件 |
| **D：HTTP、云台与质量交付** | libcurl、JSON、token、摄像头 HTTP 转发、云台控制、Mock Server | Qt 云台控制、状态提示、回放入口、异常提示 | 八方向云台可调用，全流程测试和演示包可复现 |

### 个人验收标准

- **A**：服务器能启动、处理并发连接、分发任务，并正确处理断线和退出。
- **B**：数据库可初始化，注册、登录、设备列表和录像查询可用。
- **C**：实时流和录像至少各完成一路，FFmpeg 线程能停止且无明显泄漏。
- **D**：HTTP/云台链路可用，Qt 有明确状态反馈，并完成最终回归测试。

---

## 三、6 天具体分工

每个任务单元都包含后端、客户端或联调内容，下午推送到个人 `dev-xxx` 分支并提交到 `dev-integration` 联调。

| 天数 | A：服务器架构与网络 | B：协议/数据库/认证 | C：FFmpeg/流媒体 | D：HTTP/云台/测试 |
|---|---|---|---|---|
| **第 1 天**<br>基础统一 | **后端**：整理 Server 工程、配置、日志、启动入口。<br>**客户端**：Qt/Linux C TCP 网络骨架。<br>**联调**：连接、断开、日志验证。 | **后端**：冻结 TLV 头部、消息 ID、错误码；创建用户/设备/录像表。<br>**客户端**：Qt TLV 编解码。<br>**联调**：正常包、半包、粘包测试。 | **后端**：检查 FFmpeg 环境和测试流，完成媒体探测工具。<br>**客户端**：Qt 引入 FFmpeg，完成本地一帧解码。<br>**联调**：确定 RTSP/RTMP 备用流。 | **后端**：libcurl/JSON 最小封装和 Mock 响应。<br>**客户端**：Qt 主窗口、登录/设备/预览/录像入口。<br>**联调**：确认运行依赖。 |
| **第 2 天**<br>注册与基础服务 | **后端**：接入 Reactor + ThreadPool 和任务分发。<br>**客户端**：Qt 异步请求、超时、重连。<br>**联调**：注册消息进入任务队列。 | **后端**：MySQLClient、用户注册、盐值和密码哈希。<br>**客户端**：Qt 注册请求和结果处理。<br>**联调**：成功、重复、非法参数测试。 | **后端**：建立媒体任务和流会话对象。<br>**客户端**：Qt 播放线程 `open/read/decode/stop`。<br>**联调**：验证线程停止和资源释放。 | **后端**：HTTP 请求超时、状态码和 JSON 错误处理。<br>**客户端**：Qt 请求状态、错误提示。<br>**联调**：Mock Server 验证异常流程。 |
| **第 3 天**<br>登录与设备列表 | **后端**：登录状态机、会话超时、未登录拦截。<br>**客户端**：Qt/Linux C 登录状态处理。<br>**联调**：登录请求和设备请求串联。 | **后端**：登录查询、设备表接口、设备列表 TLV。<br>**客户端**：Qt `DeviceModel` 和设备树。<br>**联调**：注册 → 登录 → 设备列表。 | **后端**：媒体元数据、AVPacket 序列化和包长度限制。<br>**客户端**：Qt 媒体包重组测试。<br>**联调**：大包、截断包测试。 | **后端**：接口冒烟测试、日志和耗时统计。<br>**客户端**：设备为空、超时、断线提示。<br>**联调**：整理第 3 天测试记录。 |
| **第 4 天**<br>实时流与云台 | **后端**：流会话启动/停止、连接绑定、超时回收。<br>**客户端**：Qt 流接收和播放状态。<br>**联调**：一路流连接管理。 | **后端**：冻结流、控制、录像消息字段。<br>**客户端**：Qt 组装设备、通道和控制参数。<br>**联调**：统一错误码。 | **后端**：FFmpeg 拉流、RingBuffer、媒体包转发。<br>**客户端**：Qt 重组、解码、`sws_scale`、显示。<br>**联调**：摄像头 → Server → Qt。 | **后端**：libcurl、token、JSON、云台八方向接口。<br>**客户端**：Qt 云台控制和状态反馈。<br>**联调**：Qt → Server → 摄像头/Mock。 |
| **第 5 天**<br>录像与回放 | **后端**：录像开关、任务绑定、文件创建/关闭、目录检查。<br>**客户端**：Qt 录像按钮和状态。<br>**联调**：重复开启、停止和异常退出。 | **后端**：录像表、文件索引、按时间查询接口。<br>**客户端**：Qt 查询条件和录像列表。<br>**联调**：数据库记录与文件对应。 | **后端**：TS 切片、断流重连、文件时长/大小。<br>**客户端**：Qt TS/HLS/FFmpeg 回放线程。<br>**联调**：至少播放一段录像。 | **后端**：回放地址、文件存在性和路径安全校验。<br>**客户端**：Qt 回放入口、加载和错误提示。<br>**联调**：准备演示数据和清理脚本。 |
| **第 6 天**<br>集成与交付 | **后端**：线程、socket、日志、重启和部署检查。<br>**客户端**：Qt 断线、关闭、重复操作修复。<br>**联调**：全链路稳定性测试。 | **后端**：协议、数据库、认证一致性检查。<br>**客户端**：注册、登录、设备、录像回归。<br>**联调**：整理 SQL、协议和错误码文档。 | **后端**：FFmpeg 资源释放、断流和录像完整性检查。<br>**客户端**：解码、暂停、停止、回放修复。<br>**联调**：至少 10 分钟一路流测试。 | **后端**：HTTP、JSON、云台和 Mock 回归。<br>**客户端**：完整演示流程。<br>**联调**：测试报告、演示脚本、运行依赖和交付包。 |

---

## 四、每天的统一验收

每天 18:00 前必须完成：

1. 代码提交到个人 `dev-xxx` 分支，并通过 Pull Request 合入 `dev-integration`；
2. 至少完成一次后端与客户端联调；
3. 提交测试日志、截图或可复现命令；
4. 记录未解决问题和第二天负责人；
5. 禁止将真实密码、token、摄像头账号、地址和密钥提交到仓库。

### 六天最终验收

- [ ] Server 可读取配置、写日志并稳定启动；
- [ ] TLV 可处理正常包、半包、粘包和非法长度；
- [ ] Qt 注册、登录和设备列表可用；
- [ ] 至少一路实时流经过 Server 到 Qt 显示；
- [ ] 云台八方向请求经过 Server 转发；
- [ ] 录像文件、数据库索引和回放结果对应；
- [ ] 服务器、Qt 客户端、Linux C 测试客户端有运行说明；
- [ ] 有协议文档、数据库 SQL、测试报告和演示脚本。

## 五、GitHub 协同开发方式

### 5.1 分支规划

| 分支 | 负责人 | 用途 |
|---|---|---|
| `master` | 全员维护，项目负责人保护 | 最终稳定版本，只用于最终演示和发布 |
| `dev-lhc` | A | 服务器架构、网络层、连接管理 |
| `dev-lqw` | B | TLV、MySQL、注册登录、设备/录像接口 |
| `dev-pyj` | C | FFmpeg、实时流、RingBuffer、录像 |
| `dev-xgq` | D | HTTP、JSON、云台、Qt 联调和测试 |
| `dev-integration` | 全员协作 | 每日集成和联调分支，推荐设置 |

### 5.2 开发流程

1. 每个人只在自己的 `dev-a`、`dev-b`、`dev-c` 或 `dev-d` 分支开发。
2. 每天开发完成后，将代码推送到 GitHub 对应分支。
3. 完成本地测试后，提交 Pull Request：`dev-xxx → dev-integration`。
4. 至少由另一名成员审核代码、测试日志和接口影响，再合并到 `dev-integration`。
5. 每天从 `dev-integration` 拉取最新代码进行联调，发现冲突由相关功能负责人共同解决。
6. 第 6 天完成全流程测试后，提交最终 Pull Request：`dev-integration → master`。
7. `master` 合并后作为最终演示版本，建议打标签 `v1.0-demo`。

### 5.3 GitHub 使用要求

- `master` 设置保护规则，禁止直接 push。
- Pull Request 必须写明：修改内容、涉及模块、测试方法和已知问题。
- 涉及 TLV、数据库、媒体包的修改，必须附协议说明、SQL 或测试数据。
- 提交信息建议使用：`feat:`、`fix:`、`test:`、`docs:`，例如：`feat: add device list protocol`。
- 不提交真实账号、密码、token、摄像头地址和密钥；使用 `server.conf.example` 等脱敏配置。

---

## 六、建议的协作方式

- **A+B**：负责 Server、TLV、MySQL、认证和设备接口联调；
- **C+D**：负责 FFmpeg、媒体协议、Qt 播放和录像回放联调；
- **A+D**：负责 HTTP、云台、Qt 网络状态和异常处理；
- **B+C**：负责录像元数据、媒体包字段和文件索引一致性。

> 六天版本的核心原则：不追求所有功能都做深，优先保证每个人都有后端任务，并每天完成一个可运行的前后端闭环。



