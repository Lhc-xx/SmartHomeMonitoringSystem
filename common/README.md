# Common 模块

放置服务器和客户端共同使用的代码：

- `include/common/`：通用类型、错误码、时间工具、字符串工具等头文件；
- `include/protocol/`：TLV 消息类型、协议头、序列化/反序列化声明；
- `src/`：公共代码实现；
- `tests/`：公共模块测试。

跨模块共享的协议或数据结构只在这里定义一份，避免服务器和客户端各写一套导致不一致。
